# Decisions (ADR-lite)

Append-only. Format: context / choice / why / alternatives. Newest last.

---

## D-001 · 2026-07-16 · Phase 1 covers all three feed formats

- **Context:** the plan lists semt.002 + MT535 + camt.053 for Phase 1; starting with fewer parsers was considered as a smaller first slice.
- **Choice:** build all three in Phase 1.
- **Why:** the point of the phase is the multi-format story — the *same* portfolio expressed in three wire formats, parsed to one common model; doing it in one pass keeps that comparison honest.
- **Alternatives:** semt.002 + MT535 first with camt.053 as a fast follow; semt.002 alone.

## D-002 · 2026-07-16 · Seed portfolios from real SEC 13F filings

- **Context:** synthetic positions can be random tickers or modelled on real institutional books.
- **Choice:** seed from 13F-HR filings of 2–3 well-known filers.
- **Why:** positions look like a real book rather than noise; the EDGAR pull doubles as proof of the scheduled-fetch path; public-domain data, small volume.
- **Alternatives:** fully synthetic (simpler, looks artificial); defer 13F until the fetch path exists.

## D-003 · 2026-07-16 · Quarkus + jOOQ for the serving layer

- **Context:** the serving stack could be Quarkus or Spring Boot; either works with jOOQ.
- **Choice:** Quarkus + jOOQ.
- **Why:** fast startup and low memory in containers (with native image as a further option), which directly cuts hosting cost; jOOQ keeps the SQL explicit and reviewable instead of hidden behind an ORM — in a data platform, the queries *are* the product.
- **Alternatives:** Spring Boot + jOOQ (larger ecosystem, heavier footprint).

## D-004 · 2026-07-16 · Reference data = small real slice

- **Context:** the securities master could be invented, a small real slice, or a bulk pull.
- **Choice:** real data for ~50–100 securities: OpenFIGI mappings + SEC ticker/CIK + a handful of GLEIF LEIs.
- **Why:** real identifier-mapping pain at trivial volume, licence-safe; a fully synthetic master would mean reconciling against invented data.
- **Alternatives:** fully synthetic; full GLEIF golden copy (more scale, little extra value here).

## D-005 · 2026-07-16 · Serving runs on real AWS via Terraform; PaaS dropped

- **Context:** the original plan hosted the live site on a PaaS (Railway) with Terraform merely *describing* an AWS production target that would never be applied. Expected usage is tiny (a single owner plus occasional demos), so cost is not the binding constraint.
- **Choice:** RDS Postgres `db.t4g.micro` + AWS App Runner for the Quarkus container, frontend on Vercel free, all provisioned by Terraform. Deploy path: GitHub Actions → ECR → App Runner.
- **Why:** infrastructure-as-code that is never applied is fiction; running the real thing keeps the Terraform honest, uses one cloud instead of two, and App Runner covers the PaaS conveniences (public TLS endpoint, no load balancer to manage).
- **Alternatives:** Railway as planned (simplest ops, Terraform stays unapplied); hybrid PaaS + apply/destroy AWS demo cycles (two platforms to maintain).
- **Guardrails:** AWS budget alert before the first resource; no NAT gateway, no ALB, no always-on Aurora Serverless — each is a fixed monthly cost the workload doesn't justify. Verify App Runner availability on the account's plan before writing Terraform; fall back to ECS Fargate.

## D-006 · 2026-07-16 · External data fetching runs on GitHub Actions

- **Context:** Databricks Free Edition serverless compute has a restricted outbound-internet allowlist, so pipeline jobs cannot call external sources (SEC EDGAR, price APIs) directly. Something with open egress must fetch data and land it where Databricks can read it.
- **Choice:** GitHub Actions cron workflows on GitHub-hosted runners in this repo, pushing raw files into a Unity Catalog volume via the Databricks CLI/REST API. Databricks Workflows still schedules all lakehouse processing.
- **Why:** zero standing infrastructure and zero cost (free minutes for public repos); cron, secrets management, and run logs are built in; the fetch code lives beside the rest of the codebase and works from day 1 — before any AWS infra exists.
- **Alternatives:** fetching from Databricks Workflows directly (blocked by the egress restriction); AWS EventBridge Scheduler + Lambda (the cloud-native equivalent — viable, but requires AWS infra and Terraform earlier than needed; a reasonable later migration); an always-on personal machine (unreliable).

## D-007 · 2026-07-16 · Environments: local dev + CI + one live environment; no standing staging

- **Context:** production platforms typically run dev/staging/prod. This is a solo-maintained reference build with demo-scale traffic and no users at risk.
- **Choice:** three layers — local Docker dev (Postgres pinned to the RDS major version), CI on every PR (lint + tests, required before merge), and a single live AWS environment.
- **Why:** a standing staging stack would roughly double infra cost and maintenance for no risk reduction that CI + local parity doesn't already provide at this scale.
- **Alternatives:** permanent staging (cost/upkeep unjustified); ephemeral staging via Terraform workspaces — documented as the scale-up path: the same modules can stand up an identical stack briefly, then destroy it.

## D-008 · 2026-07-16 · Python toolchain: uv + ruff + pytest + Pydantic, pinned to 3.12

- **Context:** the first Python code (ingest/) needs environment management, linting, and testing that behave identically on a dev machine and in CI; the canonical model carries untrusted feed data.
- **Choice:** `uv` for environments + lockfile, `ruff` for lint + format, `pytest` for tests, Pydantic v2 for models. Python pinned to 3.12 via `.python-version` to stay close to Databricks serverless runtimes.
- **Why:** uv is a single fast binary with lockfile-first discipline; ruff replaces three legacy tools; Pydantic validates every record on construction — load-bearing in a data-quality project, not a convenience.
- **Alternatives:** pip + venv + requirements.txt (no lockfile discipline); Poetry (heavier, slower); dataclasses/attrs for models (no validation).

## D-009 · 2026-07-16 · Models validate shape, not sense

- **Context:** where should bad data be rejected? Feeds deliberately contain defects (missing cost basis, mistyped ISINs, implausible dates) that the reconciliation and data-quality layers exist to catch.
- **Choice:** the canonical model enforces *shape* only — types, formats, required fields (e.g. an ISIN must look like an ISIN). Business plausibility stays representable: checksum validity is a helper method (`has_valid_checksum`), cost basis is optional, no cross-field date rules.
- **Why:** rejecting defective data at parse time destroys the evidence the platform's whole value chain (detect → route → resolve → audit) is built on. The boundary guarantees integrity of *representation*; downstream layers judge *quality*.
- **Alternatives:** strict parse-time validation (simpler, but the wrong layer owns the rules); no validation anywhere (silent corruption).

## D-010 · 2026-07-16 · Wire formats as spec-shaped subsets, XSD validation deferred

- **Context:** full ISO 20022 messages are enormous (hundreds of optional elements, deep nesting); our model carries a focused field set. Somewhere between "toy XML" and "schema-perfect" a line must be drawn.
- **Choice:** renderers/parsers use the real message structure and element names for the fields we carry (e.g. `SctiesBalCtdyRpt`, `BalForAcct/FinInstrmId/ISIN`, `AcctBaseCcyAmts/HldgVal/Amt`), with documented simplifications (flattened nesting where the spec stacks identical wrappers). Validation against official XSD schemas is a recorded backlog item, not silently skipped.
- **Why:** the learning and the parsing work are genuine at subset fidelity; schema-perfect output would consume days on optional elements nothing downstream reads. Honesty is preserved by documenting the line (module docstrings) rather than pretending.
- **Alternatives:** full schema fidelity + XSD validation now (high cost, low marginal value); invented XML tags (would falsify the "real formats" premise).

## D-011 · 2026-07-16 · Raw landing layout and generation policy

- **Context:** the generator turns from library into data factory; the layout and corruption policy shape everything downstream.
- **Choice:** (a) Hive-style `date=YYYY-MM-DD/` directories so Spark partition-prunes naturally; (b) business days only; (c) the two holdings renditions (semt.002, MT535) are corrupted **independently** — the same day's views genuinely disagree sometimes, which is what cross-feed reconciliation exists to catch; (d) everything derives deterministically from the calendar date, so any historical day regenerates byte-identically; (e) ground-truth manifests are written **outside** the raw landing directory — the pipeline must never read them; only detection-quality evaluation may.
- **Why:** each choice removes a future argument: pruning comes free, weekends don't fake volume, cross-format breaks exist by construction, investigations can reproduce any file exactly, and the DQ layer can't accidentally cheat.
- **Alternatives:** flat directories (no pruning); same corruption for both holdings formats (reconciliation would only ever catch format-coverage gaps); random non-reproducible corruption (untestable).

## D-012 · 2026-07-17 · CI authenticates to Databricks with a personal access token

- **Context:** the scheduled delivery job must authenticate to Databricks with no human present. Local dev uses a cached OAuth browser login (user-to-machine), which by definition needs a person. Machine auth offers two routes: a **personal access token** (PAT) tied to my user, or **OAuth machine-to-machine** against a **service principal** (client id + secret).
- **Choice:** a PAT, stored as a GitHub Actions repository secret alongside the workspace host. Verified end to end before committing: token creation is enabled on Free Edition, and `make land` uploads successfully with `DATABRICKS_AUTH_TYPE=pat` forced (so a cached OAuth session could not mask a failure).
- **Why:** it works on Free Edition today, needs one secret and no second identity to manage, and keeps the CI upload command byte-identical to the local one. The tradeoff is real and worth stating plainly: a PAT is a long-lived bearer credential carrying **my user's full permissions**, which is strictly weaker than a scoped service principal. It is acceptable *here* because the workspace holds nothing but synthetic data, and it is the only credential in play.
- **Mitigations:** create it with a bounded lifetime (~90 days) and rotate; GitHub masks secret values in run logs; a scheduled run failing on expiry is loud (GitHub emails on failed scheduled workflows), which is the right failure mode — better a broken job than an immortal credential.
- **Alternatives:** *OAuth M2M service principal* — the correct answer once anything real is at stake: a non-human identity, scoped, revocable without disturbing my own login. Rejected for now because Free Edition's service-principal support is unverified and it buys nothing at this scale; this is the documented upgrade path. *OIDC workload-identity federation* — no stored secret at all, the ideal end state; Databricks supports it for service principals on some tiers, unverified here and moot until a service principal exists.

## D-013 · 2026-07-17 · Bronze runs on a file-arrival trigger, defined as a bundle

- **Context:** landing files was automated (D-006), but parsing them into bronze still required a human to open the notebook and run it — half a pipeline. Automating it raises two separate questions: *what starts the job*, and *how the job is defined*.
- **Choice (what starts it):** a **file-arrival trigger** watching `/Volumes/workspace/parvum/landing/raw/`, with `wait_after_last_change_seconds: 60` and `min_time_between_triggers_seconds: 300` — not a cron.
- **Why:** a timer would have to *guess* the gap between the feed landing and the job starting, and guessing wrong is silent — the job runs, finds nothing, reports success, and the data is simply a day late. The trigger follows the data instead of a clock, so the fetch schedule can move without anything downstream needing to know. The `wait_after_last_change` window coalesces a three-file delivery into one run rather than three; `min_time_between_triggers` is a floor on frequency, protecting a free-tier compute quota from a pathological upload loop. Verified by real firing before being committed to, not merely accepted by the API.
- **Choice (how it's defined):** a **Databricks Asset Bundle** (`databricks.yml`) deployed with `make deploy-job`, with the job pulling code via `git_source` from `main` rather than from bundle-synced files.
- **Why:** a job clicked together in the Workflows UI is invisible to review, unreproducible, and absent from git — the same objection as un-applied Terraform in D-005. As for `git_source`: the notebook imports the repo's own parsers through a relative path (`../ingest/src`), so it needs the whole repo tree, not just `spark/`; a checkout gives it exactly the layout it already expects. And because `main` is branch-protected and CI-gated, "the job runs `main`" *already has* a review gate in front of it — a separate code-deploy step would add ceremony without adding control. File sync is switched off (`sync: paths: []`) so the deploy carries the job definition alone, leaving no third copy of the code to wonder about.
- **Alternatives:** *Quartz cron ~07:00 UTC* (simple and predictable, but couples to the fetch's timing and fails silently when that assumption breaks); *bundle-synced files* (the canonical bundle pattern, and the right answer if the job's code ever diverges from the repo layout — but here it would deploy a third copy of code that git already versions and CI already gates); *the UI* (rejected: not code); *Lakeflow/DLT declarative pipelines* (a heavier abstraction than a single idempotent notebook needs today).
- **Note:** the trigger fires on arrival, so it will not reprocess the 65 days already in the volume — history was loaded by the runs that preceded it, and the registry anti-join makes that distinction moot anyway.

## D-014 · 2026-07-17 · 13F seed is committed, and identifiers are derived only where the rule is real

- **Context:** D-002 committed to seeding portfolios from real SEC 13F filings. Two questions had to be settled before any of it could be used: *when* the data is fetched, and *what identifier* the securities carry — 13F reports CUSIPs, while custodial feeds carry ISINs.
- **Choice (when):** the parsed extract is **committed to the repo** (`ingest/seed/holdings_13f.json`) and refreshed by an occasional, reviewed `make fetch-13f`. Generation never touches the network.
- **Why:** the generator must produce byte-identical files for a given date (D-011), which is impossible if its input depends on a network call that can change or fail. This also makes a filing change arrive as a reviewable diff rather than a silent shift in every downstream file. The extract records the **accession number** and no retrieval timestamp: the accession pins the exact immutable filing, while a timestamp would make the file differ on every fetch and turn the scheduled check (PR 13) into a generator of meaningless pull requests.
- **Choice (identifiers):** derive the ISIN for US-domestic CUSIPs; **exclude CINS codes and record the exclusion**, with an explicit `country` parameter that callers must supply rather than a hidden assumption.
- **Why:** deriving a North American ISIN is applying ISO 6166's own arithmetic — country code + 9-character CUSIP + check digit — not guessing a mapping, so it doesn't violate D-004's ban on invented reference data. The rule stops precisely where knowledge stops: a CINS (letter-leading, e.g. Chubb's `H1467J104`) belongs to a foreign issuer whose real ISIN is `CH0044328745`, obtainable only by lookup; `US` + CINS would mint an identifier that exists nowhere, which is worse than a gap because it looks correct. The same trap is why `country` is a parameter: numeric CUSIPs are issued to Canadian issuers too, and the code cannot tell you the domicile. Phase 2's OpenFIGI mapping is what brings the excluded names back.
- **Alternatives:** *fetch at generation time* (kills reproducibility); *carry CUSIPs through the feeds and defer all mapping to Phase 2* (arguably the most faithful — real feeds do carry mixed schemes — but it would mean teaching all three renderers a second identifier scheme, and ISINs are what a custodian would actually send); *derive ISINs for everything by assuming US* (fabricates identifiers for foreign issuers — rejected outright); *hardcode a CUSIP→ISIN lookup table* (invented reference data by another name).
- **Note:** this PR reads 13F but does **not** yet swap the seed book — doing so changes every historical file, and bronze's idempotency key is `file_path`, so it would not notice the content changed. That reprocess is handled deliberately in its own step rather than smuggled in here.

## D-015 · 2026-07-17 · The seed book keeps 13F's weights and divides its scale

- **Context:** the 13F extract carries institutional quantities — 227,917,808 Apple shares, a $263bn book. A wealth-management client does not hold that. The book had to become *real* without becoming *absurd*.
- **Choice:** divide every share count by a single constant (10,000), keeping prices as filed. The result is ~28 positions and a **~$25.2M account with Apple at 23.0%** — Berkshire's genuine concentration at a plausible private-client scale. Prices come from `value / shares` (the real quarter-end price); cost basis, which 13F does not report, is synthesized deterministically from each CUSIP and left absent for ~20% of positions.
- **Why:** the *shape* of a portfolio — how many names, how concentrated, which sectors — is what makes it look like a real book rather than noise (D-002), and scaling shares uniformly preserves every relative weight exactly. The scale is honest fiction and documented as such.
- **Why a share divisor rather than scaling by value**, which sounds more principled: NVR trades near $6,590, so its 11,112 shares are $73M of the filing yet round to *zero* on any value-based scale that keeps Apple's 227.9M shares sensible. Share counts and position values are not proportional, and the data says so.
- **Alternatives:** keep the filing's raw quantities (real, but a private client holding 227M Apple shares undermines the whole fixture); scale by value (rounds small positions out of existence); fractional share quantities (avoids the rounding but reads oddly on a custody statement). A zero-share position now raises rather than being silently dropped, so a future filer that doesn't suit the divisor forces a conscious decision.
- **Note:** duplicate `security_name`s are deliberate — Alphabet, Lennar and Liberty Live are each held in two share classes: one issuer name across two distinct ISINs. Anything downstream keying on name rather than identifier should break here rather than in production.

## D-016 · 2026-07-17 · Bronze detects restatements by content, not by path

- **Context:** swapping the seed book rewrites all 195 raw files with new content **at the same paths**. Bronze's idempotency was an anti-join on `file_path` — "have I seen this path?" — which silently assumes a file's bytes never change once written. Under that check the swap would be invisible: the job would recognise every path, skip every file, report success, and leave bronze permanently disagreeing with the volume. No error, just two systems quietly diverging.
- **Choice:** compare each file against the `sha256` the registry has stored since PR 8 and never read. Path unknown → new. Digest differs → **restated**: delete that file's rows from all four tables, then re-parse. Digest matches → skip.
- **Why:** real custodians restate constantly — a corrected file for a past date, same name — so content-keyed idempotency isn't a workaround for our seed change, it's what the pipeline needed anyway. The seed swap simply became the first restatement it ever saw, handled by the mechanism rather than by a human dropping tables. Deletion precedes re-parsing (an append alone would duplicate every row) and the registry row is deleted with the rest, so a crash mid-supersede leaves the file looking un-ingested and the next run redoes it cleanly — the same ordering argument that already puts the registry write last.
- **Cost, stated plainly:** every file is hashed on every run. At hundreds of small files that is nothing; at millions you would track a source-side version or modification time rather than reading the data to discover it changed.
- **Limitation, stated plainly:** landing overwrites the raw file, so the superseded *bytes* are already gone by the time bronze notices. A platform that must prove what it received on a given day would land restatements to a versioned path and keep both. Bronze remains "raw as received" only for the *current* version of each file.
- **Alternatives:** drop the four tables and rebuild (~90s — simple and legitimate for a synthetic universe, since bronze is a rebuildable projection of the volume; rejected because it is a human fixing by hand what the pipeline should handle, and the next restatement would need the same intervention); keep both versions with an `is_current` flag or a version column (proper SCD-2 history, but pointless while the volume itself keeps only one copy of the raw file).

## D-017 · 2026-07-17 · 13F filings are pipeline input, selected point-in-time — the committed seed is gone

- **Context:** D-014 committed a parsed 13F extract to the repo. Two things were wrong with that once examined. First, reference data in git means every refresh is a source-code change — PRs proposing data, which is the wrong medium (that friction is what surfaced the issue). Second, one frozen extract meant a refresh changed the book for *every* date, past included — a mass restatement per quarter, when real books evolve forward only.
- **Choice (where the data lives):** a local **filing store** outside git (`data/edgar/`, gitignored; synced by `make fetch-13f`; fetched fresh per run in CI, which needs `SEC_USER_AGENT` as a repository secret). One directory per filing, keyed by accession number; sync is incremental because filings are immutable — an existing directory is never re-fetched, and a half-written one can't be mistaken for complete (written to a `.partial` name, renamed atomically).
- **Choice (how it's read):** **point-in-time** — the statement for `as_of` is built from the latest filing with `filing_date <= as_of`. *Filed*, not period end: holdings become knowable when the filing lands (~45 days after quarter end), and a custodian statement can only reflect what exists. This is the as-of/filed-at (bitemporal) distinction in its simplest usable form.
- **Why determinism survives leaving git:** D-014 argued the seed had to be committed for byte-identical regeneration (D-011). The property actually rests on something stronger — EDGAR filings are immutable and pinned by accession, so "the set of filings public by date D" is a fixed historical fact. Same store state, same bytes; and a new filing changes only dates at and after its own filing date. History stops being rewritable *by construction*, which retires the quarterly mass restatement (D-016 remains for genuine corrections).
- **Verified live:** with four real filings cached, the generated pile now steps at 2026-05-15 — Berkshire's Q4-2025 book (37 securities after CINS exclusions; Amazon, Diageo, Domino's…) until 2026-05-14, the Q1-2026 book (28) from the filing date on. Bronze reprocessed exactly the pre-boundary days as restatements and skipped the rest as unchanged.
- **Limitations, stated:** amendments (`13F-HR/A`) are still skipped — a faithful store would supersede the original from the amendment's own filing date (backlog); dates before the earliest cached filing refuse loudly rather than guess.
- **Alternatives:** keep the committed extract (rejected: data as source code, mass restatements); fetch at generation time with no store (network-dependent generation, kills replay); land filings only in the lakehouse volume (right for the platform's own reference data — that leg is planned — but the generator also needs local, offline access).

## D-018 · 2026-07-17 · File-arrival triggers don't see overwrites; restatement runs are chased explicitly

- **Context:** after landing 195 *overwritten* files (the PR-12 seed swap), the file-arrival trigger never fired — while a control probe at a *new* path fired in 86 seconds the same morning, and PR-10's original probe in 118. The trigger is alive; it watches for paths appearing, not bytes changing.
- **Finding:** the trigger has exactly the blind spot D-016 removed from bronze — path-based change detection — one layer up. The same bug class can hide at several layers of a pipeline, and fixing it at one layer says nothing about the others.
- **Choice:** accept and document, with an explicit operational rule: normal daily flow needs nothing (a new `date=` directory is a new path — fires; new EDGAR accessions likewise), but **any re-land of existing paths is followed by `make run-job`**. The job is idempotent and content-keyed, so the chase is always safe.
- **Alternatives:** a periodic cron alongside the trigger as a safety net (adds scheduled runs that usually no-op; may be adopted if manual chasing ever misses); landing restatements to versioned paths so every delivery is a new path (also fixes D-016's "superseded bytes are gone" limitation — the recorded upgrade if restatements become routine).

## D-019 · 2026-07-17 · The account universe: five accounts, opaque ids, one consolidated cash file

- **Context:** one account rendered three ways was enough to prove the pipeline, but it isn't what a custodian sends. A custodian services many accounts for a recipient and knows nothing about the recipient's clients; feeds are account-scoped, and the client → entity → account mapping is the wealth manager's own reference data.
- **Choice (the universe):** five accounts across three real 13F filers — Berkshire Hathaway (×2, different scales), Gates Foundation Trust (×2), Pershing Square (×1) — giving five genuinely distinct books: different sizes (~$1.6M to ~$25M), concentrations (Pershing's 11 names vs Berkshire's 28), and one **EUR-based account**, so multi-currency enters bronze now rather than being retrofitted when Phase 4's FX conversion needs it. Client grouping (3 clients) is deliberately *absent* from the custodian-side registry — it arrives as WM-side reference data in Phase 2.
- **Choice (identifiers):** account ids are **opaque custodian-style numbers** (`60011234`, `FQ5521`, `X4478210`…). Custodians issue identifiers, not descriptions; "what is account FQ5521?" is precisely the question reference data exists to answer, and losing that mapping is how real feed onboarding starts.
- **Choice (file shapes — the formats' own asymmetry):** semt.002 and MT535 are per-safekeeping-account messages → one file per account per day. camt.053 explicitly supports repeated `Stmt` blocks → **one consolidated cash file per day** carrying every account's statement. The daily delivery is 11 files (5×2 holdings + 1 cash). Parsers and bronze now handle one-file-many-statements — an assumption real feeds break, learned on a format designed for it.
- **Corruption:** independent per (account, format) — cross-account and cross-format disagreements both exist by construction (extends D-011).
- **Alternatives:** readable account ids like `ACC-GROWTH-001` (self-documenting, but hides the mapping problem feeds actually pose); per-account cash files (uniform, but would keep "one file = one statement" baked in); synthetic variations of one filer (cheaper, but the books would be visible siblings and Phase 2 would meet fewer real identifier edge cases).

## D-020 · 2026-07-17 · Issuer domiciles: a curated slice, because CUSIPs don't reveal countries

- **Context:** adding real filers made the identifier trap fire in production data: a CUSIP does not encode its issuer's domicile. CINS codes announce themselves (leading letter), but **Canadian issuers get ordinary numeric CUSIPs** — and our new filers' *top* holdings are exactly that: Canadian National Railway (Gates) and Brookfield (Pershing). Deriving their ISINs with the default "US" would mint `US1363751029` — plausible-looking, checksum-valid, and existing nowhere (the real ISIN is `CA1363751027`).
- **Choice:** a hand-curated `ISSUER_DOMICILES` map (CUSIP → country) for the non-US cross-listed names our filers hold, pinned in tests against the issuers' real published ISINs; everything else defaults to US. ADRs deliberately need no entry — a depositary receipt is a genuine US instrument whatever the underlying company's home (Diageo, InBev, Coca-Cola FEMSA).
- **Why this doesn't violate D-004:** it is a *small slice of real facts*, not invented data — the same rule that admits real ISINs admits real domiciles. The default-to-US for unlisted CUSIPs is the recorded risk; the mitigation is an audit at fetch time over every cached filing, flagging what falls through.
- **The audit already paid for itself:** it caught **Waste Connections** (`94106B101`, redomiciled to Ontario in 2016) which the initial map had missed — one character away from the genuinely-US Waste **Management** (`94106L109`). Both are pinned in a test.
- **Alternatives:** derive US for everything (fabricates identifiers — rejected outright); exclude everything not provably US (would drop the filers' largest holdings); build the securities master now (Phase 2's job — OpenFIGI + SEC/GLEIF replace this map wholesale).

## D-021 · 2026-07-17 · Ownership modelled as a validated graph; reference data stays in the ingest package for now

- **Context:** Phase 2 opens with the reference layer. The first question a wealth platform answers that the custodial feeds cannot is *who owns account FQ5521?* — feeds carry opaque account numbers and no client, entity, or legal structure. That mapping is the wealth manager's own data. Real client structures observed in the target product: transactions are owned by *legal entities* (trusts, foundations, LLCs), not people; reports roll up by "Top Level Legal Entity"; ownership views show a *percentage* per node.
- **Choice (shape):** model ownership as a validated **client → legal-entity → account DAG** with percentage edges, and resolve *effective ownership* on demand (product of edge percentages along a path, summed across paths) rather than storing a flat account → client column. Invariants enforced at construction: known endpoints, acyclic, every owned node closes at exactly 100%, every universe account reachable.
- **Why a graph:** real ownership is layered and shared — a family holds accounts through several entities, and an investment LLC is jointly owned by two families. A flat column cannot express "60% of this account's assets are ultimately the Reyes family's"; the graph makes that a traversal, and the effective-ownership calculation is the analogue of the platform's visual ownership map — the thing silver needs to attribute a position to its owners.
- **Why resolve on demand, not materialise:** the graph is the source of truth and the computation is trivial at this scale. Pre-computing effective percentages would be a cache to invalidate for no benefit; if it ever needed to scale, materialising into a table is the recorded upgrade.
- **Choice (home):** the ownership model lives in the **`parvum_ingest` package** (`parvum_ingest.ownership`), not the empty `reference/` directory, even though it is reference data. It binds tightly to the account registry (`accounts.UNIVERSE`, already in that package) and validates against it. `reference/` is reserved for the **securities master** (OpenFIGI + SEC + GLEIF — external APIs, own dependencies, licence concerns), which is genuinely separable and arrives next. That is the moment to establish `reference/` as its own package and move the reference-flavoured modules (ownership, domicile, accounts) together — splitting a ~180-line module into its own package with a uv workspace now would be ceremony ahead of need.
- **Alternatives:** flat account → client mapping (cannot express layered/shared ownership — the whole point); a graph database (correct at enterprise scale, absurd at five accounts); building `reference/` as its own package immediately (premature split; the securities master is the natural trigger); putting ownership in the canonical `model.py` (that module is feed vocabulary — ownership is WM reference data that feeds never carry, a deliberate separation).

## D-022 · 2026-07-17 · Securities master via OpenFIGI, with the unmapped kept as a visible "Unknown" bucket

- **Context:** silver needs a *what-is-this-instrument* layer to complement the ownership *whose-is-it* layer. Feeds carry ISINs — proprietary-ish, issuer-scoped; joining positions across accounts and formats, and grouping them by sector/type in gold, wants a single canonical instrument key.
- **Choice:** map the universe's ISINs to **FIGI** (Bloomberg's open instrument id) plus name/type/sector via the free OpenFIGI v3 API, producing a securities master keyed by ISIN. Every identifier OpenFIGI cannot map becomes an explicit **`mapped: false` entry — the "Unknown" bucket** — never a dropped row.
- **Why FIGI:** it's open, free, and join-friendly, exactly what a normalisation key should be; the ISIN→FIGI hop is the real reference-data work and the metadata is what gold groups by. **Why the Unknown bucket is first-class:** a security the master can't identify still sits in a client's account and must stay visible, flagged, and routable to whoever curates reference data — the target product surfaces an "Unknown" asset class rather than hiding it, and dropping the row would be silent data loss (the exact failure D-009 guards against, one layer up). A miss is data.
- **A free cross-check fell out of it:** all 76 universe ISINs mapped, *including* the Canadian names whose ISINs D-020 derived from a curated domicile map. Had that derivation fabricated `US…` ISINs, OpenFIGI would have failed to map them and dropped them into Unknown — so a clean map is independent confirmation the domicile derivation is right.
- **Storage:** the built master is a gitignored file (`data/reference/securities_master.json`, `make build-master`), fetched occasionally and reviewed — same posture as the 13F store (D-017): reference data is pipeline input, not source code, and its stability comes from FIGIs being immutable, not from committing it. Landing it into the Unity Catalog volume for silver to read is the next slice.
- **Alternatives:** commit the master to git (reviewable, but reference-data-as-source-code, the pattern D-017 retired); a paid/bulk identifier source (unjustified at 76 instruments); drop unmapped ISINs (silent loss — the thing the Unknown bucket exists to prevent); map on FIGI's SEDOL/CUSIP endpoints too (unneeded — feeds carry ISINs).

## D-021 amendment · 2026-07-17 · `reference/` package extraction deferred to the silver slice

- D-021 planned to extract `reference/` into its own package the moment the securities master arrived, moving ownership/domicile/accounts with it. In practice the securities master needs the universe's ISINs, which come from `accounts` + the 13F store in `parvum_ingest` — so it was built as `parvum_ingest.openfigi` + `parvum_ingest.securities_master` alongside them. **Revised trigger:** extract `reference/` as its own package during the silver build, when the ingest-vs-reference boundary is being drawn for real and the move is one focused refactor rather than a split done ahead of need. No behaviour changes; only where the modules live.

## D-021 amendment II · 2026-07-18 · `reference/` extracted as `parvum-reference` (uv workspace)

- The trigger set in amendment I (the silver build) arrived; the split is done. `accounts`, `domicile` (renamed from `reference.py`), `ownership`, `openfigi`, `securities_master` + their tests now live in the `parvum-reference` package; **the dependency rule is one-directional: ingest → reference, never back**. `accounts` moved *with* reference (the universe is the firm's account master — reference data; `ownership` validates against it). `parvum-build-master` stays in ingest: it reads the 13F store, so it is pipeline orchestration that *calls* reference.
- Both packages are members of a uv workspace with a virtual root and a single lockfile — version skew between them is unrepresentable. Shared `ruff.toml` at the root so both packages are first-party to the import sorter and style cannot drift.
- CI: a second explicit job (`reference`), not a matrix — a matrix renames status checks and would detach the branch-protection requirement on `ingest`. The `reference` check should be added to the required list.
- The bronze notebook's `sys.path` gained `reference/src` in the same commit, verified on a bare interpreter (not the workspace venv, which would mask a path bug by having the packages installed).

## D-023 · 2026-07-18 · Silver: one grain per table, full rebuild, format preference over reconciliation

- **Two tables, not one.** `silver_positions` keeps one row per (date, account, security); the owner attribution is a separate `silver_position_owners` (plus the tiny materialised `silver_account_owners` bridge). The rejected alternatives: exploding owners into the positions table makes a naive SUM double-count every shared account; nesting owners as an array makes the owner roll-ups — the whole point of ownership — require an explode step in every query. One clean grain per table means the safe query is the obvious one.
- **Full rebuild every run.** Silver is a pure projection of bronze × reference, so the notebook `CREATE OR REPLACE`s its tables from scratch: idempotent and restatement-proof by construction, no incremental bookkeeping. At this scale (thousands of rows) the tradeoff is free; the incremental path (MERGE on changed dates) is the documented scale-up.
- **Bronze's two holdings copies → deterministic preference, not reconciliation.** Every position arrives in both semt.002 and MT535; silver keeps one row per grain by preferring semt.002 (the richer message), file path as tie-break. Whether the copies *agree* — which is where the seeded defects live — is a data-quality slice of its own, deliberately not smuggled into the conforming join. (Live probe while building: 14,930 bronze rows → 7,629 grains, 328 of them single-copy — the defects visibly splitting pairs, exactly the surface that later slice will measure.)
- **Silver runs as a downstream task of the bronze job**, same file-arrival trigger, `depends_on: bronze` — no second trigger to go dark (D-018), and the job's alerting (failure email, duration warning) covers the whole chain unchanged.
- **The master is data; the graph is code.** The securities master reaches the workspace by being landed into the volume (`make land-master` — no egress, D-006); the ownership graph arrives with the git checkout like the parsers do. Overwriting the master's path does not fire the trigger (D-018) — deliberate: a master refresh alone shouldn't rerun bronze; it takes effect on the next feed arrival or an explicit `make run-job`.

## D-024 · 2026-07-18 · Silver cash: separate task, native currency, and visible collapse of feed duplicates

- **Separate notebook + job task (`silver_cash`), not an extension of the positions notebook.** The positions notebook is live and proven; the two share only the ownership bridge (each builds it from code). Both depend on bronze alone and may run in parallel. Renaming or extending live, working pieces for tidiness is how "the job points at a notebook that moved" failures happen.
- **Native currency, no FX.** FQ5521's EUR stays EUR. Conversion needs a real rates source (D-004 forbids inventing one); silver states per-currency facts. Trigger to revisit: gold's cross-family totals.
- **Feed duplicates are collapsed, visibly.** Probing the real bronze before writing the CTAS found the assumed grain violated: 80 duplicate (as_of, account, transaction_id) groups — the seeded DUPLICATE_TRANSACTION defect. A transaction reference is unique by definition, so collapsing IS the conformance; hiding the collapse would starve the quality layer. Hence `source_row_count` (>1 = collapsed) and `source_disagrees` (TRUE when the copies conflicted). The probe also caught a defect *collision* nobody designed for: 6 of the 80 pairs differ on settlement_date by one day — DUPLICATE_TRANSACTION and SETTLEMENT_SHIFT landing on the same movement. Where copies conflict, the pick is deterministic (earliest settlement date, the conservative claim) and flagged rather than silently resolved.
- Same D-023 shape otherwise: one grain per table (`silver_cash_balances`, `silver_cash_transactions`), owner-attributed variants with proration done once (`silver_cash_balance_owners`, `silver_cash_transaction_owners`), full rebuild, column comments reapplied per rebuild.

## D-025 · 2026-07-18 · Reconciliation: findings as rows, dual integrity verdicts, and comparing only what both sides say

- **`dq_holdings_recon` is a long findings table** — one row per disagreement (presence gaps + null-safe field mismatches), not a wide per-position scorecard: findings are countable, groupable by class, and empty means clean. **`cost_basis` is deliberately not compared**: semt.002 structurally never carries it (0 of 7,465 rows), so the comparison would manufacture thousands of fake findings. Stated consequence: MISSING_COST_BASIS defects are invisible to cross-format recon on this feed pair — a check that would catch them (day-over-day continuity) is future work, recorded. Knowing which defects a check *cannot* see is part of the check.
- **`dq_cash_integrity` renders two verdicts per account-day** — the invariant (opening + movements = closing) evaluated against raw bronze *and* conformed silver. The pair is the point: raw-breaks/conformed-passes days prove the duplicate collapse right (63 of them); conformed breaks isolate genuinely missing movements (76). One verdict would conflate the feed's errors with ours.
- **Movement amounts are unsigned with direction in the type** (camt.053's CdtDbtInd convention, kept by the parser). The first integrity draft summed raw amounts and "found" 100% of account-days broken — a 100% failure rate indicts the check, not the data. Follow-on conformance fix in the same slice: `silver_cash_transactions` now carries `signed_amount` (and the owner proration uses it), so downstream sums stop re-deriving sign rules. The debit set mirrors `DEBIT_TYPES` in the camt renderer — the one authority on which types debit.
- **The defect manifests are grading material, not platform input.** The recon must catch defects without the answer key; the manifests (ground truth of every injection) are used at verification time to prove the catches complete. This slice's proof: 84 duplicates injected, 80 caught — and the manifests show exactly 4 DROPPED_TRANSACTION injections removed one copy of a duplicated pair, cancelling both defects. The same 4 events reconcile the collapse count (84−4=80) and the conformed break count (80−4=76), and raw breaks tie out to 84+80−21 both-defect days−4 cancels = 139. Every number closed.

## D-026 · 2026-07-18 · FX via ECB reference rates: store what was published, interpret at consumption

- Gold must add USD and EUR wealth; inventing a rate violates D-004. The **ECB's euro foreign exchange reference rates** are the cheapest real source that exists — free, keyless, stable URL, full history — and reference rates are exactly the right instrument for valuation reporting (indicative, not tradeable quotes). We slice the USD column only, from 2026 on: one real series, small on purpose.
- **Full-history file over the rolling 90-day one**: the pile window is a config knob (DAYS), and a source that silently stops covering the earliest days when someone raises the knob is a trap. History removes the edge for one ~700 KB download.
- **The store holds exactly what the ECB published** — TARGET business days only, gaps included. Calendar-completion (a rate for every day) is the *consumer's* interpretation, done by `fill_forward` at read time, tested offline, and transparent: it returns each day's rate *with the publication date it came from*, so a Saturday valuation can say "Friday's rate" instead of pretending. Refuses to start before the series does — valuing at nothing must be loud.
- **Fetched and landed by the daily workflow, non-fatally**: an ECB outage must not block the feed delivery, and gold degrades gracefully (carry-forward from the last landed file). The rates land before the feeds so the day's job run sees the day's rate. Same D-018 note as the master: overwriting the rates path fires no trigger, by design.
- The `parvum-fetch-fx` CLI lives in the **reference** package — first script there — because it touches nothing of ingest's; a CLI belongs to the package whose data it maintains.

## D-027 · 2026-07-18 · Gold: four report tables, USD headlines with labelled conversion, quality as a column

- **The four views a wealth report actually opens with** (user-chosen scope): `gold_client_wealth` (client × day headline), `gold_asset_allocation` (client × day × class, cash as its own 'Cash' class and 'Unknown' kept visible per D-022), `gold_income` (client × month × type, DIVIDEND and INTEREST only — fees and trades are flows, not income), `gold_top_holdings` (client × rank, top 10 on the latest day, one row per security summed across the client's accounts, weight on the conventional positions-value basis).
- **Headlines are USD, converted at each day's ECB reference rate, and every wealth row names both the rate and the day it was published** — a weekend valuation visibly carries Friday's rate (D-026's fill_forward). A currency the notebook cannot convert aborts the run rather than passing through at par: 1:1 is the one conversion nobody ever notices being wrong.
- **Quality is a column, not a caveat**: `books_reconcile` on the wealth table is `every(conformed_consistent)` over the client's accounts for that day, straight from `dq_cash_integrity`. A report number and the verdict on its inputs travel together.
- **Gold only sums and shapes.** All proration happened once in silver (D-023/D-024); the FX fill is the only computation gold adds, and it is imported from the tested reference package, not re-derived in SQL.
- Fifth job task, `depends_on: [silver, dq_recon]` — the chain ends: file → bronze → silver ∥ cash → recon → reports.

## D-028 · 2026-07-18 · Serving tenancy: schema-per-tenant, two fictional advisory firms

- **Context:** the serving layer should demonstrate multi-tenancy the way real wealth platforms do it, but the data universe has three client families and no firms. Schema-per-tenant proves nothing with one tenant.
- **Choice:** tenant = advisory firm. Two fictional firms partition the families — **Aldergate Wealth Management** (Hartwell) and **Stonefield Family Office** (Okafor + Reyes) — each with its own Postgres schema (`tenant_aldergate`, `tenant_stonefield`) carrying an identical layout, plus a data-free `tenant_template` schema that exists solely for jOOQ code generation. The same Flyway migration set is applied programmatically to every schema at startup; tenant ids are validated against `[a-z][a-z0-9_]*` because a schema name cannot be a bound JDBC parameter.
- **Why:** firm-level tenancy mirrors the real model (the platform's customer is the advisor, not the family); isolation becomes structural — a query rendered against Stonefield's schema *cannot* see Hartwell — rather than a WHERE clause every query must remember. Stonefield holding both owners of the shared 60/40 account keeps the interesting cross-owner views inside one tenant.
- **Alternatives:** tenant = client family (three schemas, but conflates firm with client and splits the shared account's owners across tenants); row-level tenancy with a tenant_id column (one schema, but isolation by discipline instead of structure — the thing this phase is supposed to demonstrate the alternative to).

## D-029 · 2026-07-18 · Gold reaches Postgres via a Python exporter; projection is truncate-and-reload

- **Context:** Databricks Free Edition cannot open connections to arbitrary hosts (D-006), so the pipeline cannot push to Postgres. Something with open egress must move gold.
- **Choice:** a Python exporter CLI (new `export/` workspace package) pulls the gold tables over the SQL Statements API — the already-proven access path — and loads Postgres: from GitHub Actions in production, against the docker-compose Postgres locally. Each load **truncates and reloads** each tenant's projection tables in one transaction. The Postgres DDL is owned by the Flyway migrations in `serving/`; because those are plain SQL files, the exporter's tests apply the very same files to a test database — one source of truth, exercised from both sides.
- **Why:** pull-from-Actions is D-006's pattern reused, with zero new infrastructure; truncate-and-reload matches gold's restatement-proof full-rebuild semantics exactly (an upsert that never deletes would silently keep rows a gold rebuild removed) and is trivially idempotent at this volume. The serving app stays read-only — its availability never depends on Databricks being reachable.
- **Alternatives:** the Quarkus app importing on startup (mixes ETL into the serving path, bloats the memory footprint D-003 cares about); file handoff via the landing volume (an extra hop that buys nothing at hundreds of rows); MERGE/upsert (accumulates, but risks drift against a full-rebuild source of truth).

## D-030 · 2026-07-18 · jOOQ code generation from the migration files (offline), and one class set across every tenant schema

- **Context:** the endpoints need jOOQ's typed classes, which are generated from a schema. jOOQ can read that schema from a live database or from the DDL directly. Separately, schema-per-tenant (D-028) means the same tables exist in `tenant_aldergate`, `tenant_stonefield`, … — but generating one class set per schema would be absurd.
- **Choice:** generate with jOOQ's **`DDLDatabase`**, which parses the Flyway migration scripts in `serving/` directly — no database runs at build time. Generated sources land in `target/` (regenerated every build, never committed, exactly as Quarkus treats its own generated code). At runtime the `DSLContext` is configured `renderSchema=false`, so every table renders as a bare name; which tenant's copy that resolves to is decided by a per-request `SET LOCAL search_path` (see the endpoints below). One generated class set therefore serves all tenants.
- **Why:** codegen-from-DDL keeps the build hermetic and fast (CI's `serving` job needs no Postgres, and `mvn generate-sources` works with Docker cold), and it makes the migration the *single* schema source of truth — codegen and Flyway read the identical DDL, the same principle as a committed lockfile. `renderSchema=false` + `search_path` is what turns structural tenant isolation into a one-line routing concern instead of a schema qualifier threaded through every query.
- **Cost paid:** `DDLDatabase` interprets the DDL in an in-memory H2, where PostgreSQL `text` becomes a non-indexable CLOB, so the projection's string columns are declared `varchar` (unbounded) — the identical type in PostgreSQL, chosen so the primary keys build under the interpreter. The accommodation is a one-line comment in the migration, not a second source of truth.
- **Alternatives:** generate against a live/testcontainers Postgres migrated by Flyway (the "most faithful" route, but adds a database to every build for a schema jOOQ can already read from the files — and the app's own tests already prove the DDL runs on real Postgres); commit the generated sources (drifts from the DDL silently, and reviewers diff machine output); one codegen run per tenant schema (multiplies identical classes to no benefit); route tenants by fully-qualifying the schema in each query (defeats the point of generating once).

## D-031 · 2026-07-18 · The ownership graph is a structural gold projection, not a monetary one

- **Context:** the serving API should let a firm see *who owns which accounts* — including the signature shared account split 60/40 between two clients — but the four existing gold tables carry only prorated money, not the account→owner edges. The edges live in `silver_account_owners`.
- **Choice:** add a fifth gold table, `gold_ownership`, that projects those edges as-is: one row per (account, owning client) with the effective fraction, an `owner_count`, and an `is_shared` flag (derived with a window over the account). It carries **no money** — the monetary attribution is already prorated into wealth/allocation/holdings. It flows to Postgres and the `/tenants/{id}/ownership` endpoint through the same exporter and schema-per-tenant machinery as the rest.
- **Why:** the ownership graph answers a structural question ("who owns what, and what is shared"), so mixing account values back in would duplicate — differently rounded — numbers the other endpoints already own. Keeping it edges-only makes it the natural place to *see* why proration exists at all. Because each edge routes to its owning client's tenant, a shared account whose owners sit in one firm (Stonefield holds both) shows both edges; one split across firms shows `is_shared` true on each side even though the co-owner is not visible — structural isolation stated honestly rather than papered over.
- **Alternatives:** enrich the graph with each account's current USD value (re-derives money the wealth endpoint already reports, at a second rounding); serve it straight from `silver_account_owners` (breaks D-029's rule that only gold reaches Postgres, and skips the derived shared-account columns); a dedicated reference/graph store like a property graph DB (real overkill for a five-account universe, and it would sit outside the one lakehouse-of-record).

## D-032 · 2026-07-18 · The frontend is a static SPA that consumes the API; dev proxies instead of CORS

- **Context:** the serving layer's read endpoints needed a face — the one part of the project a non-engineer sees. The API is a separate Quarkus service, so the question is what the frontend *is* relative to it.
- **Choice:** a **static single-page app** (Vite + React + TypeScript, charts with Recharts), built to plain files with no server of its own. It calls the API over `fetch`. In development, Vite **proxies** `/tenants` to the local Quarkus app, so the browser only ever calls its own origin and CORS never enters the picture; in production the app is served behind the same origin as the API (or pointed elsewhere by a build-time `VITE_API_BASE`). Charts use the project's validated data-viz palette, always with a legend and direct labels, and the UI is theme-aware.
- **Why:** the data is already shaped by the API — the frontend's whole job is presentation, so a static SPA is the honest amount of machinery (no SSR, no backend-for-frontend, nothing to run or scale but a CDN). The dev proxy keeps this PR free of a CORS configuration on the serving app: cross-origin is a deployment concern, decided when the two are actually hosted apart, not a thing to hardcode now. Typed API models mirror the Java record shapes, so a projection change that reaches the JSON surfaces as a TypeScript error rather than a blank cell.
- **Alternatives:** Next.js (SSR/app-router earns nothing here — the data comes from a separate service, so Next would be a static client with extra ceremony, and a Node server to run); server-rendered templates from Quarkus itself (couples the API's availability and release cadence to the UI, and throws away the typed client); enabling CORS on the API now (a real deployment need later, but hardcoding dev origins today is config that outlives its context — deferred to the deploy slice).

## D-033 · 2026-07-19 · AWS CLI auth uses a dedicated IAM user via `aws login`, not static keys or Identity Center

- **Context:** the AWS deploy (D-005) needs the CLI and Terraform authenticated against a real account. AWS offers three paths: root user keys, a static IAM-user access key, or IAM Identity Center (SSO) with short-lived tokens. Identity Center was tried first for its auto-expiring credentials, but enabling it prompted creating an AWS Organization — which immediately converts this account from the Free Plan to pay-as-you-go pricing and **forfeits the free-tier credits**. That trade is wrong for a personal account whose whole point is spending the credit deliberately.
- **Choice:** a dedicated IAM user (`parvum-terraform`, not root) with console access enabled, using AWS's newer **`aws login`** flow (managed by the `SignInLocalDevelopmentAccess` policy) instead of a permanent access key: it authenticates via a browser against the user's console password and issues temporary credentials that auto-rotate every 15 minutes and expire within the session (up to 12h). Because Terraform's AWS SDK doesn't understand that `login_session` credential type, a second CLI profile (`parvum-tf`) wraps it via `credential_process = aws configure export-credentials --profile parvum --format process` — a supported SDK-wide mechanism that re-emits the session as plain temporary keys on demand.
- **Why:** gets the safety property that motivated Identity Center (nothing permanent on disk, short-lived credentials) without its Organization prerequisite or the credit loss. `AdministratorAccess` is attached directly rather than a scoped policy — Terraform needs to create IAM roles for App Runner/RDS, and hand-scoping further buys little on a single-owner account where the same person already controls everything root can do.
- **Gotcha recorded:** `credential_process` failed silently (`exit status 1`, no visible stderr) when the command path was quoted with spaces (`"C:/Program Files/Amazon/AWSCLIV2/aws.exe" ...`) — Terraform's invocation doesn't handle that quoting the way a shell does. Fixed by pointing at the Windows short (8.3) path instead: `C:\PROGRA~1\Amazon\AWSCLIV2\aws.exe`. Also: the S3 **backend** block resolves credentials independently of the `provider "aws"` block, so `profile = "parvum-tf"` had to be set in both places, not inherited.
- **Alternatives:** static IAM access key (simplest, but a permanent secret sitting in `~/.aws/credentials` indefinitely); IAM Identity Center/SSO (the more standard answer at organizational scale, rejected here solely because enabling it on this account forfeits the free-tier credit); root user access keys (never — root is the account's superuser and AWS explicitly recommends against generating keys for it).

## D-034 · 2026-07-19 · Terraform state on S3 with native locking; a budget alert is the first resource created

- **Context:** the AWS deploy needed a place for Terraform state to live before any other resource could be provisioned, per the D-005 guardrail that a budget alert must exist before anything billable does.
- **Choice:** state lives in a versioned, encrypted, public-access-blocked S3 bucket (`parvum-tfstate-<account-id>`), using Terraform's native S3 locking (`use_lockfile = true`, needs Terraform ≥1.10) instead of a DynamoDB lock table. That bucket is created by a small, separate `infra/terraform/bootstrap/` config that keeps its own state **local** — the chicken-and-egg problem of "state needs a bucket; the bucket's creation needs to be tracked in state" is avoided by keeping that one piece out of the remote backend entirely. The main config's very first applied resource is an `aws_budgets_budget` (50%/80% of a $20/month threshold, email alert) reusing the same `ALERT_EMAIL` already configured for Databricks job failures.
- **Why:** native S3 locking removes what used to be a mandatory extra always-on resource (DynamoDB) for a solo project with no concurrent applies to guard against; versioning means a bad apply's state is recoverable. One alert email for both Databricks and AWS keeps "who gets paged" a single fact instead of two addresses to keep in sync.
- **Alternatives:** local-only state for the main config too (simplest, but no backup/recovery and a single point of failure on one laptop for something meant to stay up, unlike the bootstrap module's one trivial resource); DynamoDB-backed locking (the pre-1.10 standard pattern, now an unnecessary resource); skipping the budget alert (violates the D-005 guardrail directly, and the account's Free Plan hard cap doesn't communicate *why* spend crossed a threshold before it does).

## D-035 · 2026-07-19 · App Runner is replaced by ECS Express Mode (amends D-005)

- **Context:** D-005 named AWS App Runner for the Quarkus container, and its own guardrail said to verify App Runner's availability before writing Terraform. That check came back positive only in the abstract — the concrete failure showed up on the first `terraform apply`: `SubscriptionRequiredException: The AWS Access Key Id needs a subscription for the service`. Research confirmed why: **AWS closed App Runner to new customers on 2026-04-30** (existing customers keep working; it's in maintenance mode). This account, having only just started using AWS, is a new customer by that definition, so provisioning is blocked outright — not a quota or config issue.
- **Choice:** **ECS Express Mode** (`aws_ecs_express_gateway_service`, added to the AWS Terraform provider in v6.23.0 — this project moved off the `~> 5.0` constraint to get it). It is AWS's own direct successor, announced for exactly this transition: point it at a container image, get a public HTTPS endpoint, with its own managed Application Load Balancer, ACM certificate, and auto scaling — the infrastructure role (`AmazonECSInfrastructureRoleforExpressGatewayServices`, AWS-managed) provisions and owns all of it. Tasks run in the default VPC's public subnets (Express Mode's own default), each getting a public IP, so ECR pulls and CloudWatch logging work without a NAT gateway.
- **Why:** preserves D-005's original intent (a PaaS-like public endpoint with nothing to hand-manage) at no extra charge over the underlying Fargate compute, and keeps the "no NAT gateway, no ALB to operate ourselves" guardrail intact — Express Mode's ALB is AWS-managed, not a resource this project owns or pays a separate fixed rate for.
- **Alternatives:** full ECS Fargate + a self-managed Application Load Balancer (the literal fallback D-005 named; more Terraform, more moving parts, and a small fixed ALB cost, worth revisiting only if Express Mode — a very new feature — turns out to have rough edges that matter here); a third-party PaaS (Render, Fly.io, Railway — moves the project off AWS entirely, contradicting D-005's "one cloud" reasoning).
- **Known rough edge:** `terraform plan` on `aws_ecs_express_gateway_service` shows a phantom diff on `primary_container.environment` values and computed fields (`ingress_paths`, `service_revision_arn`) even immediately after a clean apply — confirmed cosmetic by checking the container's actual boot logs (the real deployed value was correct both times). Matches the resource's own CHANGELOG, which already lists a similar "inconsistent result after apply" fix for `network_configuration.security_groups` — this is a very new resource type (November 2025) with rough edges, not a bug in this config.

## D-036 · 2026-07-19 · RDS is publicly accessible, protected by TLS + password (amends this session's original private-VPC design)

- **Context:** RDS was first built private (VPC-only, reachable only from the app's security group) as the safer default. But the exporter (`export/`, D-029) already runs `export-gold` from wherever has open egress to both Databricks and Postgres — originally GitHub Actions' hosted runners, per its own code comment written before this session's AWS work began. Hosted runners are on the open internet with unpredictable, unallowlistable IPs; they cannot reach a private-VPC-only database, and giving them access via a NAT gateway would add exactly the fixed monthly cost D-005 ruled out.
- **Choice:** `publicly_accessible = true` on the RDS instance, with the security group open on 5432 to any source, defended instead by **`rds.force_ssl = 1`** (a DB parameter group forcing every connection to negotiate TLS) and the existing Terraform-generated 32-character password. The Quarkus datasource JDBC URL carries `?sslmode=require` so the app itself refuses a plaintext connection too.
- **Why:** network-perimeter isolation (the original design) and IP-allowlisting both assume a caller with a stable, knowable address — true for the ECS service, not true for GitHub Actions. Given that constraint, app-layer defense (TLS + a strong generated secret, never logged or committed) is the honest tool for the job, not a shortcut; it is also exactly what the exporter's code already assumed before any AWS infrastructure existed.
- **Alternatives:** keep RDS private and run the exporter from inside the VPC (a scheduled ECS task) — ruled out because that task still needs internet egress to reach Databricks, which a private subnet can't provide without the same NAT gateway D-005 already rejected; a bastion host or SSM Session Manager tunnel for manual loads only (adds a standing resource or a manual step for something that should eventually be a scheduled, unattended reload — the parked "automate export-gold on every gold load" idea).
- **Verified:** rebooting the instance was not required (`rds.force_ssl`'s AWS-defined `ApplyType` is `dynamic`, so `apply_method = "immediate"` took effect without one — the parameter was first set as `pending-reboot` by mistake and corrected). Ran `export-gold` from this laptop against the live public endpoint over `sslmode=require`; the API subsequently served the real reloaded figures (Hartwell $41,091,835.83) end to end.

## D-037 · 2026-07-19 · CI deploys via GitHub's OIDC federation, not a static AWS key in a repo secret

- **Context:** the deploy path (build → push ECR → redeploy) needs GitHub Actions to authenticate to AWS. The default recipe most tutorials show is an IAM user's static access key pasted into a repo secret.
- **Choice:** an IAM OIDC identity provider for `token.actions.githubusercontent.com`, and a role (`parvum-github-actions`) that only `ambarshukla/parvum`'s `main` branch can assume (`sub` condition pinned to `repo:ambarshukla@59102691/parvum@1302835881:ref:refs/heads/main` — see the correction below for why it's this exact form), holding only enough permission to push to the one ECR repo and force-redeploy the one ECS service — via `aws-actions/configure-aws-credentials`'s `role-to-assume`, no access key anywhere.
- **Why:** the same reasoning as D-033's `aws login` choice — a credential that's minted per-run and expires beats one that sits in GitHub's secret store indefinitely. It also happens to be narrower by construction: a static key attached to a broad policy is a standing liability even if unused for months, where this role can only ever be assumed by a workflow run on this repo's protected branch.
- **Note:** unlike App Runner, ECS Express Mode does not watch its ECR repository for new image pushes — `auto_deployments_enabled` doesn't exist on `aws_ecs_express_gateway_service` (confirmed against the provider's actual schema, not assumed by analogy with App Runner). The workflow's last step (`aws ecs update-service --force-new-deployment`) is therefore load-bearing, not a redundant nicety.
- **Alternatives:** a static IAM user access key in a GitHub secret (simplest, but a standing credential with no expiry, and GitHub's secret-scanning/rotation story is weaker than AWS's own token lifecycle); a self-hosted runner inside the VPC (solves nothing here — the workflow already only needs ECR/ECS API access, not network-level VPC access).
- **Correction (2026-07-19, first real run):** the merge that landed this decision immediately failed its own deploy workflow — `Not authorized to perform sts:AssumeRoleWithWebIdentity`. Cause: GitHub shipped "immutable subject claims" for OIDC tokens (changelog, 2026-04-23) partway through this project's life, which rewrites the `sub` claim to embed permanent owner/repo IDs (`repo:ambarshukla@59102691/parvum@1302835881:ref:refs/heads/main`) instead of the classic name-based form this decision was written against. Confirmed the exact claim GitHub was actually sending via CloudTrail on the failed run, rather than guessing from documentation, then updated the trust policy's `StringEquals` condition to match. Net effect is arguably an improvement: the new form is keyed to IDs that don't change if the repo is ever renamed, where the old name-based condition would have silently broken on a rename.

## D-038 · 2026-07-19 · The frontend goes to Vercel; CORS is answered for real (closes D-032's deferral)

- **Context:** D-032 built the dashboard as a static SPA and deliberately deferred CORS to "when the two are actually hosted apart." That moment arrived: the API is live on AWS, and the frontend needed a separate host.
- **Choice:** deployed `web/` to Vercel as project `parvum-dashboard` (`vercel link` + `vercel --prod`), giving a stable production domain `https://parvum-dashboard.vercel.app` plus per-PR preview URLs on random subdomains under the same project. `VITE_API_BASE` is set as a Vercel project environment variable (Production and Preview, both pointed at the one live API — there's only one environment to point at today) rather than committed, since it's a deployment fact. On the API side, `%prod.quarkus.http.cors.enabled=true` is now baked into the image (a stable fact about prod — see the correction below for why it isn't the shorter `quarkus.http.cors`), while the actual allowed origins are supplied at deploy time via `QUARKUS_HTTP_CORS_ORIGINS` (Terraform/ECS environment variable) — the production domain plus a regex (`/https://parvum-dashboard-.*\.vercel\.app/`) matching every preview deployment's randomized subdomain.
- **Why:** splitting "CORS is on" (build-time, stable) from "these are the allowed origins" (deploy-time, environment-specific) mirrors the datasource config's own dev/prod split already in `application.properties` — a fact about the environment shouldn't be hardcoded into the artifact that's meant to run in more than one. The preview-URL regex means opening a PR doesn't require a manual CORS update to see it working against the real API.
- **Verified:** confirmed the deployed origin matched the Terraform config *before* the CORS change was even needed, by linking the Vercel project first and reading back its assigned production domain — avoiding a guess-then-fix cycle. Checked the live `Access-Control-Allow-Origin` response header from the production AWS endpoint with `Origin: https://parvum-dashboard.vercel.app` after the ECS canary deployment completed.
- **Alternatives:** a wildcard `*` origin (rejected — defeats the purpose of CORS as an access control, and Quarkus disallows it together with credentialed requests anyway); hardcoding the origin list in `application.properties` (would require a rebuild for a domain change, e.g. adding a custom domain later); S3+CloudFront for the frontend instead of Vercel (ruled out earlier per D-005/D-032 — more Terraform, more resources, for a single-owner static site Vercel already serves for free).
- **Correction:** the first deploy attempt used `quarkus.http.cors=true` (that property's pre-3.4 name) and produced no visible error at all — CORS is enforced entirely by the browser, so a misconfigured server just silently never adds the header and looks perfectly healthy otherwise. Caught by sending a real preflight `OPTIONS` request with `curl` and reading the response headers, reproduced locally against the built image first (ruling out an AWS/ALB explanation) before assuming the app config was wrong. Fixed to `quarkus.http.cors.enabled`, rebuilt, redeployed; confirmed with five consecutive live requests afterward.

## D-039 · 2026-07-19 · `export-gold` runs unattended from GitHub Actions, reading the RDS password from SSM at runtime

- **Context:** RDS has been live since D-036 but every reload since has been a manual `make export-gold` run — exactly the "no manual steps long-term" gap flagged as important and parked in the private TODO.md, revivable once a live consumer existed.
- **Choice:** a new scheduled workflow, `.github/workflows/export-gold.yml` (weekdays 08:00 UTC — a ~1h45m buffer after the daily feed lands at 06:15 for bronze→silver→dq_recon→gold to finish on Databricks Free Edition's serverless compute — plus `workflow_dispatch` for manual runs). It reuses the *same* OIDC role as `deploy-serving.yml` (`parvum-github-actions`) rather than minting a second one — both workflows only ever run from this repo's main branch, so there's no meaningfully different trust boundary to separate on a single-owner project. The one new permission (`ssm:GetParameter`/`kms:Decrypt`, scoped to `/parvum/rds/password`) lets the workflow fetch the RDS password fresh at runtime and mask it before building the connection string, rather than copying it into a second GitHub secret.
- **Why:** a copied secret is a second thing that can drift — rotate the password in SSM (the one place it's generated, by `random_password`) and a duplicated GitHub secret is now silently wrong until someone remembers to update both. Reading it live keeps SSM the single source of truth, the same principle the whole session's credential choices (`aws login`, OIDC) already followed. `DATABRICKS_HOST`/`_TOKEN`/`_WAREHOUSE_ID` stay as existing GitHub secrets (already used by `daily-feeds.yml`) rather than also being fetched dynamically — no equivalent "one true source" exists for those the way SSM is for the RDS password, so there is nothing to deduplicate.
- **Alternatives:** duplicate the RDS password into a GitHub secret too (simplest, but the drift risk above); trigger the reload from the end of Databricks' `gold_reports` task instead of a GitHub cron (ruled out immediately — Free Edition has no outbound internet access at all, D-006, so it structurally cannot call anything outside the workspace); a fixed short interval (e.g. hourly) instead of once daily after the known pipeline schedule (wasteful — gold only changes once a day, on the same cadence as the feed).
- **Verified:** a manually dispatched run completed successfully (green, ~1 minute) on the first attempt — no repeat of D-037's OIDC surprise, since this workflow reuses that already-fixed trust policy. Confirmed the live API still served correct figures afterward, unaffected by the truncate-and-reload.

## D-040 · 2026-07-19 · The cash book carries a ledger: openings chain from a series epoch, external flows become calendar events

- **Context:** the next roadmap slice is performance measurement (time-weighted and money-weighted returns) over gold, and its whole premise is separating market return from client cash flows. Probing the live lakehouse before writing any code — the standing "probe before building on an assumption" rule — showed the fixture couldn't support it honestly: every account's opening and closing balance was byte-identical across all 65 landed days (each day reset to a flat 50,000×scale seed), while the same statements recorded a 25,000×scale "Client cash contribution" *every single day*. Flows that never land in wealth make any flow-adjusted return show a phantom systematic bleed (the measured average would have been −0.06% to −0.11% *per day*, purely from the inconsistency).
- **Choice:** the clean cash book now has day-over-day continuity anchored at a **series epoch** (2026-04-20, the first landed business day): that day opens at the seed, and every later opening is computed as the accumulated closing of the previous business day. External flows moved out of the daily template and became **calendar events** — a 25,000×scale contribution on each month's first business day, a 12,500×scale withdrawal on the first business day on or after the 18th (deliberately mid-month: a flow *inside* a measurement period is what makes time-weighted and money-weighted methodologies genuinely disagree). The daily BUY was resized from 30,420 to 9,850 so internal churn is a small sustainable drain (−195.05×scale/day) rather than a 20,765×scale/day bleed that the old daily contribution had been quietly masking. The opening balance's date is now the previous business day, not the fictional `as_of − 7`.
- **Why:** the alternative — computing returns on the inconsistent book — would have baked a known lie into the flagship report tables, and "the numbers look wrong because the fixture is wrong" is not a defensible answer anywhere. Continuity computed from the *clean* book also upgrades the defect story for free: a corrupted delivery (dropped or duplicated entry) now breaks the statement-to-statement chain *detectably*, giving the planned day-over-day continuity DQ check something real to catch instead of being 100% red by construction.
- **Alternatives:** build the performance engine on the data as-is (mathematically correct, provably distorted outputs); positions-only price returns treating 13F quantity steps as flows (dodges cash entirely, and the flow-vs-valuation inconsistency would still sit underneath every wealth number); keeping daily contributions but accumulating them (solvent only by accident, and unbounded cash growth would dwarf the portfolio within a year).
- **Blast radius, verified before choosing:** only the 65 `CUSTGB2L.camt053.xml` files change bytes — all 650 holdings files regenerate byte-identically (checksummed before/after), manifests never leave the laptop (nothing in `spark/` reads them), and bronze detects same-path/new-bytes restatements by sha256 and re-parses cleanly, which is exactly the machinery D-024 built. Determinism (D-011) holds: regenerating a single day reproduces the identical file hash.

## D-041 · 2026-07-19 · silver_positions dedupes by winning *file*, not winning *row* — a mistyped identifier was double-counting positions

- **Context:** found while validating the upcoming performance slice (TWR/Dietz/IRR) against live gold data before writing any Spark SQL — the daily wealth series had unexplained single-day spikes that fully reverted the next day (Hartwell: +12.2% on 2026-07-01, back down the day after), which would have made any return computed on top of it look broken or manufactured. Traced to `silver_positions`'s dedupe: it deduplicated bronze's two holdings copies (semt.002/MT535) via `ROW_NUMBER() PARTITION BY as_of, account_id, security_scheme, security_id`, preferring semt.002. When the `MISTYPED_ISIN` defect corrupts one row's identifier (a check-digit bump — `US0258161093` → `US0258161092`), that row no longer shares a partition key with its sibling in the other format: the corrupted semt.002 row wins its own partition (a *new* key), and the untouched MT535 row wins the *original* key (semt.002 no longer competes there). Both survive into `silver_positions` as two separate rows for what is really one position. Confirmed live: American Express, account `60011234`, 2026-07-01 — $4,585,899.28 counted twice, $9,171,798.56 total, flowing straight into `gold_client_wealth`'s headline number with no DQ flag (the correctly-designed `dq_holdings_recon`, which compares bronze directly rather than silver, *does* catch the underlying split as two `MISSING_IN_*` findings — but nothing stopped the doubled dollar amount from reaching gold in the meantime).
- **Choice:** dedupe now picks a **winning format per (date, account) as a whole delivery** — semt.002 if it delivered *any* row that account-day, else MT535 — before applying any per-security logic, with file path kept only as a residual tie-break inside the chosen format. The notebook's own prior comment ("semt.002 preferred over MT535 ... file path as a final deterministic tie-break") already described this whole-file intent; the implementation had keyed the preference at the wrong granularity (per matching row, not per file), which is what let a corrupted key slip through as if it were a legitimately-missing counterpart.
- **Why:** the fix makes the double-count structurally impossible rather than patched around: whichever format wins for an account-day, *every* row of the losing format is excluded regardless of whether any individual row happens to collide on identifier — there is no way for a single row's corrupted identifier to let a competing row from the other format sneak in as a second copy. The corrupted row still lands under the wrong ISIN in silver (a real, correctly-flagged data-quality problem — `dq_holdings_recon` catches it as a `MISSING_IN_MT535`/`MISSING_IN_SEMT002` pair, unchanged by this fix), but it can never again inflate the dollar total.
- **Verified live** (SELECT-only probes against the warehouse before touching the notebook, then again after landing the fix): American Express now appears exactly once ($4,585,899.28, not doubled). More importantly, account `60011234`'s total positions value is now **perfectly flat across all 65 days except one** — 2026-05-15, the documented Q4-2025→Q1-2026 filing boundary (D-017) — exactly matching the clean book's own designed invariant ("static within each filing's regime, stepping at boundaries," `book.py`) for the first time. Every prior day's small ±0.1–12% wiggle was entirely this bug; none of it was real.
- **Alternatives:** keep the row-level dedupe but add a post-hoc "same market_value appearing under two different identifiers same account-day" detector to null one out (a patch on the symptom, not the cause — and fragile, since a legitimately different security could coincidentally share a value); switch the tie-break to prefer whichever copy's identifier passes ISIN checksum validation (fixes this one defect type but not any future defect that corrupts identifiers a different way — the whole-file choice generalizes to every defect in the holdings pool, not just this one).
- **Blast radius:** `silver_positions`, and everything built on it (`silver_position_owners`, `gold_client_wealth`, `gold_asset_allocation`, `gold_top_holdings`, `gold_performance*`) all need re-materializing after this merges — same `make run-job` step as D-040, and the two fixes land as one combined re-run rather than two.

## D-042 · 2026-07-19 · Performance is three methodologies side by side, not one "correct" number

- **Context:** the roadmap called for TWR + IRR ("implement daily-linked TWR, Modified Dietz as the contrast, and money-weighted IRR; write the methodology-differences doc"). The design question was how much to build: a single blessed return figure, or the full comparison the brief actually asked for.
- **Choice:** two new gold tables. `gold_performance` (grain: client × date) carries a daily time-weighted return chain and a chain-linked growth-of-$1 index (`EXP(SUM(LN(1+r)))` over a window — exact, no UDF). `gold_performance_summary` (grain: client) carries since-inception TWR, Modified Dietz, and annualized money-weighted IRR side by side, so the three numbers for the same client and the same period sit in one row for direct comparison. IRR is solved by Python bisection (a handful of clients, once per gold rebuild — no external solver dependency justified) and joined back via the same compute-in-Python-then-`createDataFrame`-temp-view pattern the FX section already established in this notebook.
- **Why:** the JD-informed brief for this slice was explicitly about *comparing and documenting methodology differences*, not about picking a winner — that's also the more honest engineering answer, since TWR/Dietz/IRR measure genuinely different things (manager skill vs. approximation vs. investor experience) and collapsing them into one number would hide exactly the distinction a performance report exists to make legible. `docs/PERFORMANCE_METHODOLOGY.md` (same commit) explains the three methods and shows why they diverge — TWR and Dietz track within basis points here (flows are small relative to portfolio size), while annualized IRR reads far more negative purely from the annualization convention on a ~3-month window, not from any error.
- **Boundary convention, decided once and reused three times:** a flow on the client's inception day is treated as already inside `wealth_begin_usd` (mirroring a real statement's opening balance), never double-counted as a separate flow. The daily TWR chain, the Modified Dietz weighted-flow sum, and the IRR cash-flow list all apply the identical `as_of > inception_date` filter — one rule, enforced in three places consistently rather than three independently-derived conventions that could quietly disagree with each other.
- **Blast radius / dependency:** this table is built on `gold_client_wealth` and `silver_position_owners`/`silver_cash_transaction_owners`, so its numbers are only trustworthy once D-040 (cash continuity) and D-041 (holdings dedupe) are both live — this PR is sequenced to merge after both. The example figures in `PERFORMANCE_METHODOLOGY.md` were validated by re-running the corrected dedupe logic as an ad hoc query against live bronze data ahead of merge (same probe-before-materializing discipline as D-040/D-041), not guessed.
- **Alternatives:** report only TWR (the industry-standard "fair to the manager" figure) — simpler, but throws away the exact comparison the brief asked for and the JD's own language ("review, compare and document system calculation methodology differences") explicitly wants; compute IRR via a bundled solver library (e.g. `scipy.optimize.brentq`) instead of hand-rolled bisection — rejected as a dependency for a five-client, once-a-day computation that a 20-line bisection handles correctly.

## D-043 · 2026-07-19 · DQ metrics are a declarative rollup over the existing detail tables, not a new source of truth

- **Context:** the next item in the roadmap's sequencing after the performance slice: a queryable DQ metrics table (freshness/completeness/accuracy/exception counts) plus an ops KPI dashboard. This item had a specific trigger already waiting in the code: D-040's writeup explicitly promised "the planned day-over-day continuity DQ check" once the cash book had real continuity to check against — that check had never been built.
- **Choice:** two new tables in `dq_recon.py`. `dq_cash_continuity` (grain: account × date) is a new *detail* table answering a question the existing `dq_cash_integrity` doesn't: not "does today's own arithmetic add up" (intra-day) but "does today's opening equal yesterday's closing" (inter-day) — computed via `LAG(closing) OVER (PARTITION BY account_id ORDER BY as_of)` against `silver_cash_balances`, `NULL` on each account's first date per the same boundary convention D-042 already established for performance. `dq_metrics` (grain: date × dimension × metric) is a *rollup*: one `UNION ALL` branch per named check, aggregating `dq_holdings_recon`, `dq_cash_integrity`, `dq_cash_continuity`, and `bronze_file_registry` into a single consistent shape — freshness (one row per rebuild, dated at the run date, since staleness is a fact about *now* not about a historical day), completeness (files-landed rate), accuracy (three rates: cross-format match, intra-day cash arithmetic, day-over-day continuity), and exceptions (the raw counts behind those three rates, for trend/aging charts a rate alone would hide).
- **Why:** "declarative" means adding a future check costs one more `SELECT` in the `UNION ALL`, never a schema migration — the whole point of a rollup table over a growing set of independently-evolving detail tables. Rollup, not replacement: every number in `dq_metrics` still traces to a detail-table row with full lineage, matching this project's standing "findings are rows, not logs" principle rather than collapsing detail into an opaque score. `passed` is populated only where a threshold honestly applies (rate metrics against a target); exception counts stay `NULL` there deliberately — a count is trend data for a chart, forcing a pass/fail verdict onto it would manufacture a false signal.
- **An honest number, not a green dashboard:** accuracy rates sit around 40–100% day to day in this fixture, because defects are injected at a known, deliberate rate (D-011) — a KPI dashboard reporting "accuracy SLA attainment: ~60%" here is the *correct* reading for a fixture built to exercise detection, not a bug to chase to 100%. Verified live before writing the notebook: 65/65 business days have all 11 expected files (completeness is a clean 100%, as expected — the defect pool never drops a whole file); the day-over-day continuity check reports 0 breaks against the *clean* `silver_cash_balances` chain (D-040 already guarantees that), confirming the new check is correctly wired before it ever sees a corrupted delivery to catch.
- **Alternatives:** a wide table (one row per date, one column per metric) instead of the long/declarative shape — rejected because every new check would then be a schema migration, defeating the stated goal; computing freshness per historical `as_of` to match the other three dimensions' grain — rejected because "was 2026-05-01 fresh" isn't a coherent retrospective question the way "was it complete/accurate" is; a day that arrived late shows up as a *completeness* gap on its own date, not a freshness fact about a day in the past. A single freshness row per rebuild, dated at the rebuild's own run date, is the only framing that is actually true.

## D-044 · 2026-07-19 · DQ metrics is an ops page in the existing app, not a separate deployment; unscoped tables ride along in every tenant schema

- **Context:** `dq_metrics` (D-043) is pipeline-wide data — freshness, completeness, accuracy, exceptions for the whole platform — not scoped to any one advisory firm's clients. Every existing piece of serving/export plumbing assumes the opposite: `TenantSchemas`' schema-per-tenant model, the exporter's `client_id`-filtered loader, the `/tenants/{id}/...` endpoint pattern. The question was how much new infrastructure a pipeline-wide KPI dashboard actually needs.
- **Choice, two parts:**
  1. **Data**: `dq_metrics` is duplicated into every tenant schema via the exact same `V4__dq_metrics.sql`/Flyway/jOOQ/`TenantQuery` machinery every other table already uses, rather than standing up a second non-tenant schema-management path. Export-side, `gold_source.py` gets a new `UNSCOPED_TABLES` constant fetched like `GOLD_TABLES` but never filtered by client (`dq_metrics` has no `client_id` column); `export_gold.py` appends the same unfiltered rows to every tenant's load list. The API surface is a normal `/tenants/{id}/dq-metrics` endpoint, documented as returning identical rows regardless of which tenant is selected.
  2. **Frontend**: a new top-level view inside the *existing* `web/` app (a `view` state toggled from the topbar — "Clients" vs. "Ops" — no router dependency added for one extra page) rather than a second app on a second Vercel deployment. Same API, same build, same deploy.
- **Why:** weighed directly against the user before building — a fully separate app would need a new non-tenant Postgres schema, a new non-tenant-filtered exporter code path, new serving endpoints outside the tenant pattern, and a new Vercel project with its own CORS origin (extending D-038's regex). None of that is justified for ~450 rows of data and a solo demo project with no need for independent access control or deploy cadence. Duplicating a small, identical-by-construction table across two tenant schemas is a smaller, more honest cost than building a second schema-management path for it — the tradeoff is named explicitly in the migration's own comment, not left for a future reader to puzzle out.
- **Alternatives:** a genuinely separate `ops` Postgres schema + non-tenant Flyway config — more architecturally "correct" in the sense that the data wouldn't pretend to belong to a tenant, but real added engineering for a demo project's KPI page, revisit if this ever needs its own access control; a hardcoded tenant id for the ops API call instead of reusing whichever tenant is currently selected — rejected as an unnecessary magic string when the currently-selected tenant's data is already being fetched anyway and is guaranteed identical.

## D-045 · 2026-07-19 · The web dashboard deploys on push, not by hand

- **Context:** `web/`'s Vercel project (D-032/D-038) was created via `vercel link` + manual `vercel --prod` — that links a local machine to a project, but never connected the project to GitHub as a build source. Surfaced as a real gap: the Performance tab and Ops page had been merged to `main` for hours, but the live site was still serving a build from before either existed — a stale bundle hash confirmed it, not a guess.
- **Choice:** connected the Vercel project to `ambarshukla/parvum` via `vercel git connect`, with **Root Directory set to `web`** (the one setting that matters for a monorepo — without it, Vercel tries to build from the repo root and finds no matching project). Verified via a Deploy Hook trigger (not a real push, to test the pipeline without waiting for the next PR): the hook-triggered build completed in 12s and produced a bundle byte-identical to a manual `vercel --prod` from the same commit, confirming the Git-integration path builds the same artifact the CLI path already had.
- **Why:** a dashboard nobody redeploys isn't actually continuously delivered — every previous "deploy" was a person remembering to run a command, which is exactly the kind of manual step this project's own CI/CD discipline (D-037's OIDC-authenticated `deploy-serving.yml`, D-039's scheduled `export-gold.yml`) already avoids everywhere else. The frontend was the one piece still manual.
- **A one-time manual step neither the CLI nor a script could complete:** `vercel git connect` requires the Vercel account to have an authorized GitHub OAuth "Login Connection" — an account-level, browser-based authorization with no CLI or API path around it. Done once, in the dashboard, by the user; everything else (root directory, the connection itself, the verification trigger) was scriptable.
- **Verified:** production alias (`parvum-dashboard.vercel.app`) confirmed serving the hook-triggered build's exact bundle hash post-deploy.

## D-046 · 2026-07-20 · A separate internal app, with real (if minimal) auth — reopening D-044's reasoning

- **Context:** Phase 6 (alts HITL) needs a human review queue with write actions and an audit trail — a genuinely different audience and risk profile from the public, read-only client dashboard. D-044 explicitly declined a second app for the Ops page for exactly the opposite reason: no auth need, no justification for the extra infrastructure. That reasoning cuts the other way once a real access-control boundary is actually needed, so this decision revisits it rather than silently overriding it.
- **Choice, three parts:**
  1. **A new app, `internal/`** — same Vite/React/TS/Vitest toolchain as `web/`, its own Vercel project, calling the same Quarkus API under a new `/internal/**` path prefix. No new backend service: the API is reused, not duplicated (`parvum.tenants`/`TenantSchemas` untouched).
  2. **Auth is one shared credential, not a user table.** `AuthResource` compares a submitted password to a single server-held secret via constant-time equality (`MessageDigest.isEqual`), the same trust model the RDS password already uses (D-036: an SSM `SecureString`, resolved only at container start, rotated by updating the parameter). Bcrypt/Argon2 exist to defend a stored table of *many* hashes against a database leak; there is no such table here, so hashing one comparison value buys nothing a constant-time compare doesn't already give, and skips a new dependency.
  3. **Sessions are a stateless signed cookie** (`SessionToken`: HMAC-SHA256 over an expiry timestamp, verified with `MessageDigest.isEqual`), not a session table — no database round-trip to check a request is authenticated, and nothing to clean up on expiry. The cost: logout can only tell the browser to drop the cookie, not revoke a token server-side; a captured token stays valid until its 12-hour expiry regardless. Acceptable for one internal credential behind a private URL, revisit (a session table, or short-lived tokens + refresh) if this ever needs real multi-user revocation.
- **CSRF, cheaply:** `InternalAuthFilter` requires a custom header (`X-Parvum-Internal`) on every `/internal/**` request, including login/logout. A cross-site form submission cannot set a custom header; only same-origin `fetch` calls from the internal app's own code can (`internal/src/api.ts`) — a standard mitigation that costs one header check, chosen over a CSRF-token handshake as disproportionate for a single-credential internal tool.
- **Why now, not later:** building the auth boundary first (this slice) before anything sits behind it means every subsequent HITL/Ops endpoint is authenticated from the moment it exists, rather than retrofitted — the exact gap D-044 flagged as the reason *not* to build a second app prematurely no longer applies once there's a real write-action surface coming.
- **Verified:** `mvn verify` green (16 tests, incl. `AuthResourceTest` covering the missing-header/wrong-password/valid-session/logout paths); full manual run against the live dev server and through `internal/`'s own Vite proxy — 403 (no header) → 401 (no cookie) → 401 (wrong password) → 204 (correct password, real `Set-Cookie` captured) → 204 (session check with that cookie) → 204 (logout, empty cookie returned).
- **Alternatives:** Clerk or Auth0 (hosted, managed auth) — considered and set aside for this single-shared-credential use case; still the natural upgrade path if the internal app ever needs real per-user accounts or SSO. Folding HITL into `web/` as a third unauthenticated tab (D-044's own precedent) — rejected because it would make write/approve actions on financial-document extractions publicly reachable, undercutting the audit-trail story that is the point of Phase 6.

## D-047 · 2026-07-20 · The alts document generator: a small deterministic fund waterfall, four defect types, real PDFs

- **Context:** Phase 6's first real building block — synthetic capital-call/distribution/capital-account documents to eventually extract, validate, and route to human review. Unlike the custodial feeds (Phase 1), there's no open wire-format spec to build against; the shape has to be designed from how these documents actually work.
- **Choice, four parts:**
  1. **A tiny fund universe (2 funds) tied to the existing account universe.** `FUND-PE01`/`FUND-VC01` each roll up to an existing custody account (`X4478210`, `FQ5521` from `parvum_reference.accounts.UNIVERSE`) rather than inventing a parallel client universe — a real LP's fund interests sit alongside their brokerage accounts on one relationship, and this keeps that true here too.
  2. **A deterministic waterfall (`book.py`), not a calibrated PE/VC model.** Four calls (60% of commitment, unevenly spaced), two distributions (return of capital, then a capital gain), ten quarterly capital account statements — every statement's `ending_balance` reconciles exactly against its own beginning balance and that period's flows *by construction*, and chains into the next statement's beginning balance. No randomness in the clean book at all (a fixed, hand-written unrealized-gain-percent walk, not an RNG) — the only randomness anywhere in this generator is *whether* a defect fires, in `generate.py`, so the clean book a corrupted document is compared against during eventual validation is never itself ambiguous.
  3. **Four defect types**, all semantic (corrupt the model before rendering, so the PDF stays well-formed — the same split `parvum_ingest.defects` established): `MISSING_FIELD`, `COMMITMENT_MISMATCH`, `AMOUNT_TRANSPOSITION` (call/distribution notices), `ARITHMETIC_ERROR` (capital account statements, breaking the reconciliation invariant #2 relies on). `AMOUNT_TRANSPOSITION` swaps the *leading* two digits, not the trailing ones — this fixture's amounts are round to the nearest hundred/thousand, so a trailing-digit swap is usually a silent no-op (caught by a failing test before this shipped, not assumed).
  4. **Real PDFs via reportlab** (`render.py`), not styled HTML or a text file with a `.pdf` extension — a later extraction step should do genuine document-reading work, the same reason the custodial feeds are real ISO 20022/SWIFT rather than a convenient JSON shape.
- **Why:** this is deliberately a smaller, more mechanical slice than the custodial feed generator — two funds, sixteen documents each, one defect per category — because the interesting engineering in Phase 6 is downstream (extraction confidence, deterministic validation, the review queue's audit trail), not in the synthetic-data generator itself. Keeping the fund book intentionally simple (no J-curve calibration, no real vintage benchmarks) is a conscious scope cut, not an oversight.
- **Verified:** 23/23 tests green (deterministic book construction, every statement's reconciliation invariant, defect injection recorded with real targets, PDF text extraction confirming the real figures — including a corrupted one — are actually present in the rendered document). Real end-to-end run, not just tests: generated the full 32-document set, read a real corrupted PDF back with `pypdf`, and confirmed both a `COMMITMENT_MISMATCH` ("Cumulative Called $1,751,000.00" where the clean book says $1,750,000.00) and an `AMOUNT_TRANSPOSITION` ("Call Amount $100,000.00" where the clean book says $1,000,000.00) are legible in the extracted text exactly as injected.
- **Alternatives:** seed the fund universe from real GP/LP data — no such open data exists for private funds, which is the entire premise for going synthetic here (see the project brief); a syntactic ("garbled/unopenable file") defect category mirroring `TRUNCATED_FILE` — deferred, since a corrupted PDF file doesn't demonstrate "realistic messiness" the way a truncated XML/text file does, it just fails to open; more defect types (e.g. a call-sequence gap) — deferred to keep this slice reviewable, additive later the same way `ingest`'s defect pool grew over multiple phases.

## D-048 · 2026-07-20 · Alts bronze is a separate job, registration-only, deployed after this merges — not with it

- **Context:** landing the synthetic alts PDFs and registering them in bronze, following the same shape as `bronze_ingest.py`/`bronze_file_registry`.
- **Choice, three parts:**
  1. **A new landing path and a new job, not a task on the existing `bronze_ingest` job.** `landing/alts/raw/` is separate from `landing/raw/`, and `alts_bronze_ingest` (`spark/bronze_alts_ingest.py` → `bronze_alts_documents`) is a separate Databricks Asset Bundle job resource. The existing job is the one thing in this project that must never break (it's the whole unattended daily chain); giving alts its own job/trigger means this slice's deploy history can never touch that one's.
  2. **Registration only — no parser.** `bronze_ingest.py` parses each format into typed bronze rows because a deterministic parser exists for ISO 20022/SWIFT text. There is no equivalent for a PDF's prose; extraction is inherently an LLM step (a later slice), and per the same fetch/process split that already governs this platform (Databricks Free Edition can't reach the open internet, so an LLM call has to run in GitHub Actions, not here), it can't live in this notebook anyway. `bronze_alts_documents` therefore only ever answers "what documents landed, and where" — the same job `bronze_file_registry` does for the custodial feeds, deliberately not more.
  3. **Deploying and first-running this job is explicitly deferred to *after* this PR merges to `main`.** The bundle's `git_source` always executes `main`'s code (`targets.prod`); deploying `databricks.yml`'s new job resource while `spark/bronze_alts_ingest.py` exists only on a feature branch would arm a file-arrival trigger pointed at a notebook `main` doesn't have yet — exactly the failure mode already documented as a standing rule (a red run, an alert email, for a notebook that was never actually broken). `databricks bundle validate` was run locally to confirm the config is syntactically sound; `databricks bundle deploy` was not.
- **What *was* verified live:** landing itself is a pure data operation (`databricks fs cp`, no job, no trigger involved) — the real 32-document set was uploaded to `landing/alts/raw/` in the live volume this session, confirmed via `databricks fs ls`. That's safe regardless of merge state and gives the eventual first job run real data to register immediately once deployed.
- **Why register-only bronze is the right cut, not a shortcut:** it keeps this slice's blast radius to "one new empty-ish table and one new job," reviewable on its own, while still proving the landing→registry pattern end-to-end for a document type that has no parser — the interesting engineering (LLM extraction, confidence, deterministic validation) is still ahead and deserves its own slices rather than being rushed into this one.
- **Follow-up, concretely:** after merge, `make deploy-job` (bundle now includes both jobs) then `make run-alts-job` (the trigger only fires on *new* arrivals after it's armed, not retroactively on what's already landed — same behavior as the existing job, D-018) — then confirm `bronze_alts_documents` shows 32 rows split correctly across `fund_id`/`doc_type`.
- **Alternatives:** a single combined job with alts as new tasks on `bronze_ingest` — rejected per point 1; parsing PDF text with a classical library (e.g. regex over `pdfplumber` output) instead of waiting for an LLM step — rejected, because the whole point of this document type (per the project brief and D-046) is that it requires exactly the extraction-with-confidence-scoring capability a deterministic parser can't provide; that's the differentiator this phase exists to build.

## D-049 · 2026-07-20 · LLM extraction: forced tool-use, hybrid confidence, ground truth is the *rendered* document not the clean book

- **Context:** the first real "AI-native" step in the pipeline — read each alts PDF back to structured fields via Claude, scored against the generator's own ground truth. `docs/ARCHITECTURE.md`'s fetch/process split already says where this has to run: Databricks Free Edition can't reach the open internet, so this is a GitHub Actions step, not a notebook.
- **Choice, four parts:**
  1. **Forced tool-use, one tool per document type**, not a free-text prompt parsed afterward. `tool_choice={"type": "tool", "name": ...}` guarantees the response is a JSON-schema-conformant object — the failure mode this avoids is a model that writes a reasonable-looking paragraph instead of the exact shape the pipeline needs, which would need its own brittle parser to recover from.
  2. **Ground truth is the *rendered*, possibly-corrupted document, not the clean book.** `generate.py`'s manifest now carries each document's full `fields` (the corrupted model's own `model_dump`), not just the injected-defect diff. Extraction's job is to read what is actually printed — a `COMMITMENT_MISMATCH`-corrupted number is genuinely printed wrong on the page, and a faithful extraction should report exactly that wrong number. Reconciling or flagging it is deterministic validation's job (a later slice), not extraction's — conflating the two would make it impossible to tell "the LLM misread a correct document" apart from "the LLM correctly read an incorrect document."
  3. **Confidence is hybrid, not purely self-reported.** The model states its own confidence (asked explicitly, honestly, in the system prompt); `self_consistency_ok` independently checks the *extracted* values for internal arithmetic/presence problems (does beginning + flows = ending; are required amounts present) and caps confidence at 0.5 if that check fails — a floor a self-reported score alone can't provide, since the model has no way to independently verify its own read. This check is single-document only (it needs nothing beyond the one extraction result), so it runs in `extract.py` itself and works in production, not just eval; cross-document checks (commitment continuity across a fund's whole call history, call-sequence gaps) need the whole fund's documents together and belong in the later Databricks validation slice.
  4. **Claude Haiku, not a larger model.** Structured extraction from a one-page document with a forced schema is exactly the kind of task a fast/cheap model handles well; there's no reasoning-depth requirement here that would justify Sonnet/Opus cost for 32 documents (or more, as the fund universe grows).
- **Eval scope, deliberately narrower than the full north-star metric:** `evaluate.py` reports document exact-match rate and field-level accuracy — a read-fidelity measure. The roadmap's north-star metric for this phase ("straight-through-processing rate at a fixed escaped-defect budget") needs a confidence threshold and an auto-accept-vs-review routing decision to be meaningful, and that routing logic doesn't exist until deterministic validation (the next slice) does. Shipping a narrower, honest metric now rather than a wider one that can't yet be computed correctly.
- **Verified — and one genuine live blocker found, not hidden:** 37/37 tests pass, all against a mocked Anthropic client (the test suite has zero API cost by design — `alts-extract`/`alts-eval` are real, billed runs, deliberately kept out of default CI, matching `export-gold`'s precedent for anything with a real-world side effect). A single real extraction call was attempted before landing this to confirm the wiring actually works end to end; it failed with `Your credit balance is too low to access the Anthropic API` — the Anthropic Console account (separate from a Claude Pro subscription, which carries no API credit) has none loaded. This is recorded honestly rather than claimed as verified: the code is believed correct from the mocked tests and the schema/response handling, but a real document has not yet been successfully extracted. Follow-up: once credits are added, rerun the single-document smoke check before trusting a full-batch `make alts-extract`.
- **Alternatives:** a free-text prompt + a regex/JSON-extraction parser on the response — rejected in favor of forced tool-use, which removes an entire class of "the model almost got the format right" failures for a marginal prompt-engineering cost; purely self-reported confidence — rejected per point 3; running extraction inside a Databricks notebook via `%pip install anthropic` — technically possible but violates the platform's own stated constraint (Free Edition's restricted egress) and would be inconsistent with why every other external call in this project runs in GitHub Actions.

## D-050 · 2026-07-20 · Cross-document validation lives in a tested package, not inline notebook code — and two real bugs it caught in its own test

- **Context:** `extract.py` (D-049) deliberately stops at single-document self-consistency; the cross-document checks it named as out of scope — commitment continuity across a fund's whole call history, call-sequence gaps, capital-account statement chaining — need this slice's silver step.
- **Choice, three parts:**
  1. **The logic lives in `parvum_alts_hitl.validate`, a plain, pytest-tested Python module — not inline in the Databricks notebook.** `spark/silver_alts_documents.py` imports it (same sys.path trick `bronze_alts_ingest.py` already uses for `naming.py`) and is orchestration only: load `bronze_alts_extractions`, group by fund, call `validate_fund_documents`, write the result. This follows the project's own standing rule (brief rule 9: data-quality logic needs a test that shows a seeded break being caught) in a way inline notebook code structurally can't — a notebook cell isn't a unit a test can import and call directly.
  2. **`bronze_alts_ingest.py` grew a second responsibility** (registering `bronze_alts_extractions` alongside the existing `bronze_alts_documents`) rather than a third notebook — both are "register what landed in the alts volume," a natural single job, now with a shared `discover`/`supersede`/`register` helper trio instead of two copies of the same restatement logic.
  3. **The output is a routing decision (`auto_accept` / `needs_review`), never a correction.** `validate.py` never edits an extracted value — fixing a flagged document is the human reviewer's job (the review queue, next slice), not this notebook's. `route()` requires self-consistency AND cross-document validity AND confidence ≥ 0.85 for `auto_accept`; any one failing sends it to review.
- **Two real bugs, caught by the same integration test, not by inspection.** Writing `test_validate_integration.py` (run `validate.py` against the *real* generator output via `generate.generate()`, simulating faithful extraction, and assert every seeded defect gets caught) surfaced two wrong assumptions in the test itself before it validated anything:
  1. **"No injected defect → never flagged" was wrong.** A running-sum check cascades: if call #1's `call_amount` is corrupted, every later call's `cumulative_called` check also stops tying out, even though calls #2–4 are individually clean on the page. That's the *correct*, honest behavior of a real running-total check — a human reconciling a fund's calls by hand would flag every subsequent cumulative too, not just the first bad one — not a false positive to suppress.
  2. **Hardcoding `self_consistent=True` for the simulated extraction was wrong.** A faithfully-extracted statement whose own printed numbers don't reconcile (`ARITHMETIC_ERROR`) should compute `self_consistent=False` via `extract.py`'s own single-document check — that defect is caught *there*, not by `validate.py`'s chaining check, which only compares a statement's `beginning_balance` to the *prior* statement's `ending_balance` and has no reason to notice one statement's own internal arithmetic being wrong. The fix: compute `self_consistent` in the test via the real `extract.self_consistency_ok`, not a hardcoded value — which also made explicit, for the first time in code (not just in D-049's prose), exactly which defect type each of the two mechanisms is responsible for.
- **Verified:** 53/53 tests pass (16 new: unit tests per validator function demonstrating a specific seeded break each, plus the integration test against real generator output). `databricks bundle validate` confirms the updated job config (a new `silver_alts` task, chained after `bronze_alts`) is syntactically sound.
- **Not yet verified live, for two compounding reasons:** (1) the same deploy-after-merge rule as D-048 — `silver_alts_documents.py` and the extended `bronze_alts_ingest.py` only exist on this branch; and (2) even after merging and deploying, there is no real `bronze_alts_extractions` data to validate yet, since D-049's live extraction is itself still blocked on the Anthropic Console credit balance. Follow-up, in order: add credit → verify one real extraction → `make alts-extract` the full batch → `make land-alts-extracted` → merge this branch, deploy, run `alts_bronze_ingest` → confirm `silver_alts_documents`'s routing distribution against real Claude output, not simulated ground truth.

## D-051 · 2026-07-20 · The review-queue reverse-sync design, and why this slice stops at the Postgres/Quarkus boundary

- **Context:** the one genuinely open design question flagged when Phase 6 kicked off (D-046): once a human corrects a `needs_review` document, how does that correction get back into Databricks, the platform's system of record? Weighed three shapes with the user before building anything:
  1. **A land-file reverse-sync** — mirrors the existing fetch/land contract exactly, reversed: a periodic job reads decided-but-unsynced rows from Postgres and writes them to a new landing path; Databricks merges them into silver on its next run. Keeps Delta as the single system of record (Postgres stays a disposable projection everywhere, the stated principle in `docs/ARCHITECTURE.md`), at the cost of one more moving part and non-instant latency.
  2. **Direct write from Quarkus to the Databricks SQL Statements API on approve/correct** — no landing file, no separate job, corrections land immediately. Rejected: it would give the live Java service a new Databricks credential to manage, and open a second, less auditable write path into the lakehouse outside the normal medallion flow — a real regression from the fetch/process split every other external call in this project follows.
  3. **Let Postgres be authoritative for just this one table** (skip syncing back at all) — rejected outright: it breaks the "every Postgres table is a disposable projection, rebuildable from gold at any time" invariant this project states explicitly, for the sake of skipping one job.
  - **Chosen: option 1**, confirmed with the user rather than assumed.
- **This slice (Postgres schema + Quarkus backend) ships first; the reverse-sync itself is a follow-up commit, not bundled in.** Building the queue mechanics (load, list, approve, correct, audit) and the sync job in one PR would make either piece harder to review on its own, and the sync job's design depends on nothing this slice doesn't already provide (a `synced_at` column, reserved but unused until the sync job exists to set it). Small reviewable steps over one large one.
- **Schema choice: a new non-tenant `internal` Postgres schema, migrated separately.** The review queue is firm-ops data — no advisory firm owns a review decision — so it doesn't belong in `tenant_*` (client-facing) the way every other serving table does. A new `InternalSchema` bean (mirrors `TenantSchemas`, migrating exactly one schema from a separate `db/migration_internal` location) and `InternalQuery` (mirrors `TenantQuery`'s `SET LOCAL search_path` mechanism, fixed to one schema since there's nothing to route between) keep this cleanly separate from the tenant migration set — the tenant DDL can never accidentally pick up a review-queue table, and vice versa. jOOQ codegen gained a second `<execution>` in `pom.xml` (its Maven plugin supports per-execution `<configuration>`, not just one shared block) targeting `db/migration_internal` into a separate `dev.parvum.serving.jooq.internal` package.
- **JSONB for extracted/decided fields, not a fixed wide table** — same reasoning as `bronze_alts_extractions.fields_json`: the field set differs by `doc_type`, and forcing three doc types' worth of columns into one table would mean most rows are mostly NULL.
- **No `decided_by` column.** This app has one shared credential, not per-user accounts (D-046) — there is no real identity to attribute a decision to. Recording a constant string would manufacture accountability the system doesn't actually have; the column is simply absent rather than filled with a placeholder, and its addition is named as the natural trigger for ever needing real per-user auth here.
- **A decided item can't be decided again** (`approve`/`correct` on a non-`pending` row → 409). Keeps the audit trail meaningful as a record of the *first* human decision; re-opening a decided document for a second look is a distinct, not-yet-built workflow, not silently allowed through the same endpoint.
- **Verified live, not just by test:** `mvn verify` (26/26, Testcontainers Postgres) plus a real run against the local docker-compose database — login → list → correct → confirmed `decidedFields` and `decidedAt` set correctly → re-decision attempt correctly rejected with 409. Migration confirmed applying cleanly (`Migrating schema "internal" to version "1 - alts review queue"`) in both paths.
- **Alternatives:** a `decided_by` column defaulting to a fixed string — rejected per above; reusing the tenant Flyway/jOOQ machinery for this table instead of a second schema/codegen execution — rejected because the review queue isn't tenant data and forcing it through the tenant path would either duplicate it per tenant (D-044's tradeoff, wrong here since there's real write traffic to keep consistent) or require `TenantSchemas` to special-case one non-tenant schema, muddying a mechanism that's clean today.

## D-052 · 2026-07-20 · Two LLM providers behind one interface — Anthropic direct, and OpenRouter as the default

- **Context:** the Anthropic Console account hit a zero credit balance (D-049) and repeated card-add attempts failed. Rather than block the whole extraction slice on one payment processor, the user asked for a second provider (OpenRouter — a different billing account, and a unified gateway that can still route to Claude) with the plumbing kept flexible: cheaper/default provider for routine extraction, Anthropic direct available as an explicit escalation for a genuinely harder document.
- **Choice:** an `LLMProvider` abstract base (one method, `extract(tool, document_text) -> dict`) with two implementations. `AnthropicProvider` wraps the `anthropic` SDK's native Messages/tool-use API, unchanged from D-049. `OpenRouterProvider` wraps the `openai` SDK pointed at `https://openrouter.ai/api/v1` (OpenRouter speaks the OpenAI-compatible chat-completions/function-calling shape, not Anthropic's native one) and translates the *same* tool schema (`name`/`description`/`input_schema`) into OpenAI's `{"type": "function", "function": {...}}` envelope, parsing the response's JSON-*string* arguments (`json.loads`) instead of Anthropic's already-parsed dict. `extract_document()`, `self_consistency_ok()`, and everything in `validate.py` are unchanged — they only ever see the provider-agnostic extracted-fields dict, never which provider or model produced it (beyond a `model` string carried through for lineage).
- **Default provider is now `openrouter`, not `anthropic`** — `PARVUM_LLM_PROVIDER` (env) / `--provider` (CLI), both defaulting to `openrouter`; `anthropic` is one flag/env-var away for a harder document. This is a genuine behavior change from D-049, made explicitly at the user's direction, not a quiet default flip.
- **Both providers point at a Claude Haiku-class model today** (`DEFAULT_MODELS`) — OpenRouter's value is the billing/account independence and the option to route to a *different* provider's model later, not a forced change of model quality right now. Per-document-complexity routing (e.g. auto-escalating a low-confidence extraction to a bigger model) is a natural next step but not built here — this slice provides the plumbing (a provider is just a constructor argument), not a routing policy nothing has asked for yet.
- **Verified:** 59/59 tests pass (17 new/changed) — `AnthropicProvider`/`OpenRouterProvider.extract()` each tested against a mocked SDK client (proving the request-shape/response-parsing translation), `extract_document()` tested against a small fake `LLMProvider` (decoupling the hybrid-confidence logic from either SDK's shape), `build_provider()` tested for defaults/overrides/unknown-name. No real API call in the test suite — both real integrations remain to be smoke-tested once a working key exists for either provider (D-049's Anthropic blocker is unresolved; OpenRouter is untested live pending a key).
- **A real SDK-version snag, fixed before it shipped:** the installed `openai` SDK validates credential presence at client *construction*, not just at call time — building an `OpenRouterProvider` with no key configured raised immediately, breaking a test that only wanted to check the default model string. Fixed by resolving to a harmless placeholder string when no real key is configured, so building the object never requires a credential; an actually-wrong or missing key still fails loudly at the real API call, which is the correct place for that failure, not at object construction.
- **Alternatives:** LiteLLM or a similar multi-provider abstraction library — rejected as unnecessary weight for two providers with a shared interface this small; making `OpenRouterProvider` always request a specific underlying model rather than accepting any OpenRouter-routable string — rejected, since the whole point of a gateway is not hardcoding one provider's namespace into this codebase.

## D-053 · 2026-07-20 · The Phase 6 pipeline verified live, end to end — and a real schema bug it found

- **Context:** with a working OpenRouter key, the first real extraction call succeeded — but the very first *batch* run (all 32 documents) showed every single capital-account statement coming back `self_consistent=False`, far above the ~25% `ARITHMETIC_ERROR` injection rate. That's the signature of a systematic bug, not real defects, and it was chased down rather than shrugged off as "the model being imprecise."
- **The bug:** only `_CALL_TOOL`'s `call_amount` field carried the instruction "no currency symbol or thousands separators" in its JSON-schema `description`. Every other amount field, across all three tools, had none — so the model (reasonably, un-instructed) formatted them the natural way a person would write a dollar figure: `"$750,000.00"`. `Decimal(str(value))` cannot parse that, so every statement's `beginning_balance`/`ending_balance`/etc. parsed to `None`, and `self_consistency_ok` correctly reported "can't verify" for all of them — not a model failure, a schema-authoring gap.
- **Fix, two parts:**
  1. A single `_AMOUNT_DESC` constant applied to *every* monetary field in all three tool schemas, not just one — the instruction is now structurally impossible to forget on a new field, since there's one place it's written.
  2. **Defense in depth**, not just a prompt fix: a new shared `parvum_alts_hitl.parsing.parse_decimal()` strips a stray `$`/comma before parsing, used by `extract.py`, `validate.py`, and `evaluate.py` alike (previously three independent, slightly-differently-behaved copies of `Decimal(str(value))`). A model that mostly-but-not-perfectly follows a formatting instruction is a realistic failure mode this project's own "realistic messiness" premise should expect, not something to assume away twice more elsewhere in the codebase.
- **Full pipeline verified live, for the first time, after the fix:** `make alts-extract` (32/32 real documents via OpenRouter/`anthropic/claude-haiku-4.5`) → `make alts-eval`: **100% document exact-match rate, 100% field accuracy** against the generator's own ground truth — every printed value, including the deliberately corrupted ones, read faithfully. Landed into the real Databricks volume and run through the real `alts_bronze_ingest`/`silver_alts` tasks: `silver_alts_documents`'s real routing distribution — `capital_account_statement` 11 auto_accept / 9 needs_review (avg confidence 0.965 / 0.65), `capital_call` 1 / 7 (0.98 / 0.969), `distribution` 2 / 2 (0.99 / 0.95). 14/32 auto_accept overall (43.75%) — the pipeline's first real straight-through-processing number, not a projection.
- **The `capital_call` split (1 auto_accept, 7 needs_review) is cascading working as designed, confirmed live** — the exact behavior D-050's integration test was built to model, now observed for real: a defect on an early call in a fund breaks the running-sum check for every *later* call in that fund too, even ones with no defect of their own, because their cumulative totals genuinely no longer tie out against what's actually printed on the documents. A high needs_review rate on calls is the correct signature of that cascade, not a sign the checker is miscalibrated.
- **Verified:** 59/59 tests still green after the refactor (parsing logic centralized, behavior preserved). This entry's live numbers are the first end-to-end real-data confirmation of D-046 through D-052 — every prior "not yet verified live" note in this phase is now closed.
- **Alternatives:** leave the `$`/comma parsing gap and rely solely on the prompt instruction — rejected per the defense-in-depth reasoning above; a stricter JSON schema `pattern` regex forcing decimal-only strings — considered, but OpenRouter/OpenAI's function-calling schema validation is inconsistent about enforcing `pattern` across providers, so a description plus tolerant parsing is the more portable fix.

## D-054 · 2026-07-22 · The review-queue export-side loader — upsert-and-flag, not truncate-and-reload

- **Context:** D-051 shipped the review queue's Postgres schema and Quarkus backend but deliberately stopped there — nothing yet moved a `needs_review` document from Databricks into it. This slice is that loader: the second leg of D-051's three (loader → reviewed-in-place → reverse-sync), following the same land-file-reversed design already settled for the sync job.
- **Why this can't reuse `export_gold.py`'s truncate-and-reload:** every gold table is a pure, stateless projection — reloading it can always start from an empty table because nothing downstream has written to it. `alts_review_queue` is different: once loaded, a human can approve or correct a row, and that decision (`status`, `decided_fields`, `decided_at`) is real state a later load must never destroy. The loader is therefore an upsert keyed on `(fund_id, document)`, gated by `WHERE status = 'pending'` on the `ON CONFLICT DO UPDATE` — a decided row is untouched no matter what the fresh fetch reports, by construction, not by a runtime check.
- **What happens to a pending row that drops out of the fresh needs_review set** (silver got rebuilt with a corrected extraction, or cross-document validation no longer flags it): weighed two options with the user before building — delete it (mirrors gold's philosophy: the queue should only ever reflect what currently needs review), or flag it and leave it in place. **Chosen: flag it** — a new `stale` boolean column (`V2__alts_review_queue_stale_flag.sql`), set `true` by the loader when a pending document is no longer in the fresh fetch, reset to `false` automatically if the document reappears. A reviewer may already be looking at a row; silently deleting it out from under them is worse than surfacing "this may no longer need review" and letting a human decide. `AltsReviewResource.QueueItem` gained the field so the (still-unbuilt) frontend has it to render whenever it lands.
- **The join this loader depends on:** `silver_alts_documents` (D-050) carries the routing decision but not the extracted values — those live in `bronze_alts_extractions.fields_json`. `review_queue_source.py` joins the two over the SQL Statements API (`WHERE routing = 'needs_review'`), the same pull pattern `gold_source.py` already established, with `confidence` cast to `DECIMAL` in the query itself so the existing typed-value converter handles it without a new `DOUBLE` case added just for this one column.
- **`_resolve_token` extracted into a shared `databricks_auth.py`** — the second CLI needing the identical DATABRICKS_TOKEN-or-mint-from-CLI logic was the trigger to stop duplicating it; `export_gold.py` now imports it too.
- **Verified:** 29/29 export tests pass (11 new — a pure-Python suite for the join row-shaping, plus a real-Postgres suite against a throwaway schema migrated with the real `migration_internal` DDL, covering: fresh load, pending-row refresh, decided-row immunity to both a refreshed and an absent fresh fetch, stale-flagging, un-staling on reappearance, and JSONB/date/null round-trips). `mvn verify` 26/26 (`AltsReviewResourceTest`'s 9 unaffected by the added column). `make lint`/`make fmt` clean across all four Python packages.
- **Not yet built:** the frontend to actually review anything (still nothing renders the queue), and the reverse-sync job itself (D-051's third leg) — both remain the natural next slices.
- **Alternatives:** truncate-and-reload like gold, accepting that a reload mid-review would silently wipe a pending decision — rejected outright once framed that way, not seriously considered; a `reviewed_at`/optimistic-lock column instead of the `WHERE status = 'pending'` upsert guard — rejected as more machinery for the same guarantee the guard already gives for free.

## D-055 · 2026-07-22 · The review-decision reverse-sync — the loop closes, additively

- **Context:** D-051's third and last deferred piece. A human can now approve or correct a `needs_review` document (D-054's loader put it in front of them), but that decision lived only in Postgres — a disposable projection, per this project's stated invariant, which meant a decided document's corrected values had no path back to Delta, the platform's actual system of record. This slice is that path, completing the land-file design D-051 already settled and confirmed with the user rather than reopening it.
- **Shape, mirroring the forward loader in reverse:** `review_decision_source.fetch_unsynced_decisions()` reads every `approved`/`corrected` row in `alts_review_queue` with `synced_at IS NULL`. `review_decision_sync.write_decision_files()` writes one JSON file per decision to a local staging directory (`data/alts/reviewed/<fund_id>/<document>.decision.json`), and `mark_synced()` sets `synced_at` — but only ever *after* a real land succeeds, never optimistically, so a failed or partial `databricks fs cp` leaves every decision eligible for the next run to retry, no manual cleanup needed. The CLI (`sync_review_decisions.py`) owns all three steps in one transaction-shaped sequence, the same combined shape D-054's loader already established, rather than splitting fetch/land/mark across separate `make` targets where a human could run one and forget the others.
- **Databricks side: additive, not corrective.** `bronze_alts_ingest.py` gained a third registration table, `bronze_alts_review_decisions`, extending the same `discover`/`supersede`/`register` trio the other two tables already share — no new notebook, no new job task, just a third landing subdirectory (`landing/alts/reviewed/`) walked the same way. `silver_alts_documents.py` then does a plain driver-side dict lookup (matching its existing style — it already does this for extractions, not a Spark join) to fold each document's decision, if any, into three new columns: `reviewed_status`, `final_fields_json`, `reviewed_at`, all NULL when nobody has decided yet. **Deliberately not done:** touching `routing`, `cross_document_valid`, or `validation_notes` — those keep meaning exactly what they meant before this slice, the automated pipeline's own verdict, so nothing downstream that reads them needs to change. A later slice building gold alts metrics can choose `COALESCE(final_fields_json, fields_json)` — final answer if reviewed, extracted answer otherwise — without this slice having to guess at gold's shape first.
- **Verified live for the export half, following the same discipline as D-054:** two real decisions made directly against the local dev Postgres (an approve and a correct, mirroring exactly what `AltsReviewResource.decide()` would do), `make sync-review-decisions` landed both JSON files to the real Databricks volume (confirmed via `databricks fs cat`, byte-for-byte the expected shape) and marked both `synced_at`; a second run correctly reported "nothing to sync"; local Postgres state cleaned up afterward.
- **Not verified live for the Databricks half, and it cannot be yet:** `alts_bronze_ingest`'s job runs `main`'s notebook code via `git_source`, not whatever is deployed locally from this branch — the same constraint D-048 hit first. `databricks bundle validate` confirms the (unchanged) job config is still syntactically sound; the actual `bronze_alts_review_decisions` registration and the `silver_alts_documents` join are unverified against real data until this merges and the job runs post-merge. `export` tests: 36/36 (7 new). `make lint`/`make fmt` clean across all four Python packages.
- **A live Anthropic smoke test, closing a much older loose end while here:** the Console credit balance blocking D-049 since 2026-07-20 was resolved this session (a small top-up). One real single-document call via `--provider anthropic` on `capital_call_01.pdf` succeeded — `claude-haiku-4-5-20251001`, confidence 0.98, self-consistent — and its extracted fields matched the earlier live OpenRouter run of the same document exactly, field for field. Not a full batch (deliberately, to keep spend minimal); the provider is now confirmed working, not just plumbed.
- **Alternatives:** split fetch/land/mark across three separate `make` targets — rejected for the same reason D-054 combined its steps: more places for a human to stop halfway and leave state inconsistent; overwrite `routing`/`validation_notes` in place with the reviewed outcome — rejected because it would erase the automated pipeline's own historical verdict, the exact thing an audit trail (D-051's `alts_review_audit`) exists to prevent losing.

## D-056 · 2026-07-22 · The review queue's frontend — the last piece of the HITL loop a human can actually use

- **Context:** every prior Phase 6 slice built a piece of machinery — extraction, validation, the queue backend, the loader, the reverse-sync — but nothing yet let a person actually *look at* a flagged document and decide. This closes that gap: a page in `internal/` (which already had Ops, D-044) that lists the queue, shows one document's extracted fields, and lets a reviewer approve or correct it.
- **Two pages, one app, a tab switcher instead of a route.** `internal/`'s `App.tsx` gained a `page` state (`"queue" | "ops"`) and a small `.tabs` nav reusing CSS the app already had (inherited from `web/` but unused until now) — no router library for two screens behind one auth gate. Ops's data fetch is now lazy (only fires the first time that tab is opened, not eagerly on login) since Review Queue is the new default landing page and most sessions may never open Ops at all.
- **Field editing: a per-field text input, not a full JSON editor.** `extractedFields` arrives as a JSON string whose shape varies by `doc_type` — no fixed form schema exists to build a "proper" typed form against, and building one felt disproportionate to a demo-scale reviewer tool. Instead, each field renders as one labeled text input pre-filled with its current value; on "Save correction," `coerceFieldValue()` maps the edited text back toward the field's *original* JS type (number stays a number, boolean stays a boolean, string/null stays a string) so editing one field of a multi-type document doesn't silently stringify every other field when it's re-submitted. Good enough for a reviewer correcting a misread dollar figure or date; a real production tool would eventually want per-`doc_type` typed forms, named explicitly as the next increment rather than pretended away.
- **A decided item's fields render read-only from `decidedFields`, not `extractedFields`** — checked with `field in decidedFields` rather than `?? value`, so a field a reviewer deliberately corrected *to* `null` doesn't silently fall back to showing the original extracted value (a real bug caught before it shipped, not a hypothetical).
- **The `stale` flag (D-054) gets its first UI surface**: a queue row shows a "Stale" badge (taking precedence over its pending/approved/corrected status) when the loader's last run found the document no longer needs review — exactly the situation that column was added for, now visible to the person the flag exists to inform.
- **Verified:** `internal` component tests 10/10 (4 new — list rendering, field editing, approve-then-reload, and a decided item's read-only view.) `tsc --noEmit`, `vite build`, and `prettier --check` all green. Live end-to-end against the real stack: `make load-review-queue` loaded 18 real needs_review documents, the running serving API (after fixing an unrelated local JVM/Postgres timezone mismatch — `TimeZone: "Asia/Calcutta"`, a deprecated alias Postgres doesn't recognize, fixed with `JAVA_TOOL_OPTIONS=-Duser.timezone=UTC`) served them correctly including the `stale` field, and the Vite dev proxy correctly forwarded an authenticated request through to the real backend. **Not independently confirmed by eye in a browser this session** — no browser-automation tool was available; both dev servers were left running for a manual check.
- **Alternatives:** a routing library (react-router) for the two pages — rejected as unwarranted weight for a two-tab app the same size decision D-044 already made for Ops; a modal/drawer for the detail view instead of a persistent side panel — rejected, a reviewer working through a queue benefits more from seeing the list and the detail at once than from a dialog that hides the list each time.

## D-057 · 2026-07-22 · The source PDF beside the extraction — and why its bytes sit in Postgres

- **Context:** the review queue asked a reviewer to approve or correct extracted values with no way to see the document they came from. For a human-in-the-loop step that is close to useless: "does this number match the document" is the entire question being asked, and the answer wasn't on the screen. The user asked for the raw PDF next to the data, explicitly *without* source-linking (no highlighting of the region a field came from) — a much smaller ask, and worth separating, because the two differ by an order of magnitude in effort.
- **Where the bytes come from, and why they land in Postgres.** The PDFs live in the Databricks volume (`landing/alts/raw/<fund_id>/`). Serving has no Databricks credentials and no route to that volume, deliberately — D-006's fetch/land split means data moves because the exporter *pulls* it, never because the live API reaches out. Three options were weighed: (1) mirror the bytes into Postgres via the existing exporter, (2) give serving a Databricks token and stream on demand, (3) host the PDFs statically alongside the frontend. **Chosen: (1).** (2) puts a second, less auditable credential in the live service and breaks the one-directional rule every other external call in this project follows; (3) fails because the documents are generated data, gitignored, and never in the frontend's build context. (1) costs a `bytea` column and fits the existing model exactly — Postgres is a disposable projection, refilled by the exporter, and this is one more thing it projects.
- **This is only reasonable because the corpus is tiny, and that's stated rather than assumed:** 32 documents at ~2 KB each, ~128 KB total; the 18 reviewable ones come to 37 KB. At real fund-document scale (megabytes each, thousands of them) the right answer flips to object storage plus a presigned URL, with this table reduced to metadata. The migration says so in a comment, so the next person doesn't inherit the decision without its bounds.
- **A separate `alts_documents` table, not a column on `alts_review_queue`** — mirroring how bronze already splits `bronze_alts_documents` (the PDFs) from `bronze_alts_extractions` (what was read out of them). A PDF is document content with its own lifetime, not review state; keeping it separate also means the queue's per-run upsert doesn't rewrite a blob on every load.
- **Digest-gated loading.** The loader first runs a cheap SQL query for the index (fund, document, volume path, `sha256` as landed), compares each digest against what Postgres already holds, and downloads *only* what's missing or changed. A reload is therefore one query and zero transfers. `download` is injected into `load_documents` rather than imported, so the whole skip/fetch/replace decision table is testable without touching the network.
- **The CSRF header forced the viewer's shape, and that's worth knowing before writing the UI.** `InternalAuthFilter` requires `X-Parvum-Internal` on every `/internal/**` request — and an `<iframe src="...">` cannot set headers. So the browser can't point an iframe at the endpoint directly. The frontend instead `fetch()`es the PDF, wraps the response in an object URL, and points the iframe at *that*; the URL is revoked when the selection changes, so paging through a queue doesn't leak one blob per document viewed. Rendering uses the browser's own PDF viewer — no PDF.js, no new dependency.
- **Verified live, end to end, with byte-level agreement:** probed the Databricks Files API first (2131 bytes, `%PDF-1.4`) *before* writing any code against it, per this project's standing "probe assumptions against live data" rule — and the same document came back out of the serving API at exactly 2131 bytes with the magic intact. `make load-review-queue` fetched all 18 (37,412 bytes, all 18 verified to start with `%PDF`); an immediate re-run reported `18 referenced, 0 fetched`, confirming the digest gate. `mvn verify` 29/29 (3 new: bytes round-trip through `bytea`, unknown document 404s, and the endpoint still requires a session). `export` 40/40 (4 new), `internal` 11/11 (1 new).
- **Not built, and deliberately named rather than left implied:** source-linking — click a field, see the region of the page it was read from — is the obvious next want and is a much larger piece of work (the extraction pipeline captures no positional data at all; `pypdf`'s `extract_text()` returns text with no coordinates, so it would mean changing the PDF library, re-running extraction, matching extracted values back to positioned words, and overlaying on a real PDF renderer). Parked with that scoping written down, not hand-waved.
- **Alternatives:** storing base64 text instead of `bytea` — rejected, it inflates the payload ~33% and adds an encode/decode step for nothing; mirroring all 32 documents rather than the 18 reviewable ones — rejected as bytes nobody can currently reach, and a one-line widening if a future view needs them.

## D-058 · 2026-07-22 · Rendering PDFs in the app instead of borrowing the browser's viewer

- **Context:** D-057 shipped the document pane as an `<iframe>` holding a blob URL, which delegates rendering to whatever PDF viewer the browser happens to include. That was the honest minimum at the time, and its limits were written down rather than glossed: the toolbar belongs to the browser and can't be styled, and mobile browsers handle inline PDF in an iframe inconsistently — several offer a download instead of rendering. The user's call was that a reviewer tool shouldn't depend on the end user's PDF tooling, which is the right instinct: "works on my browser" is not a property you want a review workflow to have.
- **Choice: pdf.js (`pdfjs-dist`), rendering to canvases this app owns.** The app now ships its own renderer, so a document looks the same everywhere, the surrounding chrome is ours (a page count and zoom controls built from the existing design tokens, instead of an unstyleable third-party bar), and there is no dependence on browser-supplied PDF support at all.
- **Paid for with a dynamic import, not a bigger bundle.** pdf.js is ~425 KB raw / ~127 KB gzipped — comparable to the entire rest of the app. Imported statically it would have roughly doubled first paint for every visitor, including anyone who only opens the Ops tab. Imported dynamically it lands in its own Vite chunk fetched the first time a reviewer opens a document: the main bundle went from 150.67 KB to 152.05 KB gzipped, i.e. the component only. The module promise is cached at module scope, so moving between documents loads the library once.
- **The non-obvious part, and the reason to probe before shipping: standard fonts.** Running pdf.js against a real generated document *before* wiring the UI surfaced `Ensure that the standardFontDataUrl API parameter is provided`. Checking the PDFs directly confirmed why it matters here: they reference `Helvetica`/`Helvetica-Bold` with `FontFile` absent — the base-14 fonts, which PDF permits a document to reference without embedding because the *viewer* is expected to supply them. A browser's built-in viewer has them; pdf.js ships the data but deliberately does not bundle it. Without `standardFontDataUrl` every page would have rendered **with no text at all** — a failure that looks like a broken document rather than a missing config, and one that a mocked unit test could never have caught.
- **The font data is copied out of `node_modules` at build time, not vendored.** A ~16-file, ~800 KB binary payload doesn't belong in git, and a vendored copy silently drifts from the installed pdf.js version. A ten-line Vite plugin copies `pdfjs-dist/standard_fonts` into `public/` on `buildStart` (so it works in dev and build alike), the directory is gitignored, and the viewer points at `${import.meta.env.BASE_URL}standard_fonts/` rather than a hardcoded `/` so a future non-root base path doesn't break it.
- **Rendering details worth keeping:** pages are rasterised at `fit-to-pane × zoom × devicePixelRatio` and then sized down in CSS, so text is sharp on HiDPI screens instead of upscaled and soft; all pages render into a detached fragment that's swapped in at the end, so the pane never shows a half-drawn document; the buffer is copied before being handed over because pdf.js transfers it to its worker and detaches the original, which would otherwise leave a re-render at a new zoom level with an empty buffer; `destroy()` is called on the *loading task* (checked against the v6 type definitions — it does not exist on `PDFDocumentProxy`, which is where the obvious guess would put it).
- **Verified:** pdf.js parses all three real document types — 1 page, 612×792pt, real extracted text (`"Meridian Capital Partners IV Capital Call Notice…"`). `/standard_fonts/FoxitSans.pfb` confirmed served (200) at the exact URL the viewer requests, and all 16 files confirmed emitted into `dist/`. `internal` tests 11/11 with pdf.js mocked at the module boundary — deliberately, since jsdom has no canvas backend and a test that pretended to rasterise would be testing the mock.
- **Alternatives:** keep the iframe and accept the limits — rejected on the user's judgement that a review tool shouldn't depend on the viewer's environment, which is sound; load pdf.js from a CDN to avoid the bundle cost — rejected, it trades a local asset for a third-party runtime dependency and an external request on a page that otherwise talks only to its own API; render eagerly rather than on first open — rejected as it puts the cost on every visitor including those who never open a document.

## D-059 · 2026-07-23 · A public demo credential instead of sharing the internal app's password out of band

- **Context:** the internal app's login (D-046) has one real credential, stored as an SSM secret in prod. Fine for an actual back-office tool, but wrong for a portfolio piece: pointing an employer at the link and separately messaging them a password is friction for no real payoff — the data behind the login is synthetic, and the login exists to draw the client-facing/back-office line (README), not to protect a secret.
- **Rejected: removing auth entirely.** `InternalAuthFilter` gates every `/internal/**` route by path prefix, including the review queue's mutating endpoints (`POST .../approve`, `.../correct`). Dropping the filter would leave those open to the public internet — anyone, or any bot, could alter review-queue state on the live demo. The friction being removed is "how does a viewer get in," not "should mutations require a session," so only the former should change.
- **Choice: a second, intentionally public credential.** `AuthResource.login()` now accepts either the real password or a `parvum.internal.demo-password` (checked-in default, no SSM, no per-profile override needed — there's nothing in it worth protecting). A `?demo=1` link (`App.tsx`) reads the param on mount, logs in with it automatically via the existing `/internal/auth/login` flow, and strips the param from the address bar so it doesn't linger or get echoed if the page is shared onward. The session that results is identical to a real login — same cookie, same TTL, same protection on writes — so nothing downstream had to change.
- **Verified:** `serving` 30/30 (1 new: demo password logs in and receives a valid session cookie), `internal` 12/12 (1 new: a `?demo=1` load reaches the signed-in shell without ever showing the login form, and the URL is clean afterward). `mvn verify` and `npm run build`/`typecheck`/`format:check` all clean.
- **Alternatives:** printing the password directly on the login screen — rejected, reads as unpolished on a page meant to look like a real product, and still requires the viewer to read-then-type it; a dedicated read-only role for demo sessions — rejected as unnecessary scope, since the actual risk (public unauthenticated writes) is already fully addressed by requiring *some* valid session, and this project has no per-role permission model to extend.

## D-060 · 2026-07-23 · Alts join the client dashboard's wealth number, not just a side table

- **Context:** Phase 6's alts HITL pipeline (D-046 through D-057) built the whole back-office side — extraction, review queue, reverse-sync — but the confirmed, reviewed data it produces never reached the client-facing dashboard. A client who actually holds a private-fund position would see a wealth number that quietly excludes it.
- **Choice: fold alts NAV into `gold_client_wealth.total_wealth_usd` and a new `'Alternatives'` class in `gold_asset_allocation`, plus a standalone `gold_alts_holdings` detail table.** Considered doing only the standalone table (zero risk to the already-screenshotted D-040/D-041 wealth figures) versus folding in (the honest headline number, at the cost of touching `gold_client_wealth`'s SQL and `gold_performance`'s downstream daily-return chain). Chose to fold in: a wealth dashboard that doesn't count a client's actual private-fund position isn't reporting their wealth, and the standalone table alone would leave that gap invisible rather than just accepted.
- **Only confirmed documents count.** A document still in `needs_review` with no human decision contributes nothing to any of `gold_alts_holdings`, `gold_client_wealth`, or `gold_asset_allocation` — silver's new `confirmed_fields_json` (the extraction's own fields when `routing = auto_accept`, `final_fields_json` once a `needs_review` document is decided, NULL otherwise) is the single gate every downstream figure passes through. `pending_review_documents` on `gold_alts_holdings` surfaces the count of what is deliberately excluded, the same DQ-honesty stance `books_reconcile` and `dq_metrics` already take elsewhere in this project — a number nobody has signed off on doesn't get to move a client's reported wealth.
- **NAV is forward-filled, not left sparse.** Capital account statements land quarterly; wealth is reported daily. Without forward-filling, alts would flicker in and out of `total_wealth_usd` on every date that isn't an exact statement date. `alts_daily` holds the most recent confirmed NAV until the next statement supersedes it — the same thing a real reported mark does. The date grid for the forward fill is the union of `silver_position_owners`' dates and the fund's own statement dates, not just the former: the alts document history (`generate.py`'s `FUND_UNIVERSE`, ten quarters back to 2024) runs well before the custodial feed's own reporting window, so a statement that landed before the window even starts still has to seed the fill or its NAV would read as \$0 for the whole window.
- **The side effect is named, not hidden.** On the day a new statement's date falls inside the reporting window, `daily_twr_return` will show a real, not fake, jump — a private-markets NAV mark landing all at once, exactly how these assets actually report. This is the same "flat-then-a-jump" shape the 13F price data already produces elsewhere in this project (the earlier chart-marker fix, folded into `BUILD_LOG.md` only, addressed the visual version of the identical phenomenon) — not a defect to engineer around.
- **Amounts are treated as USD outright, not converted.** The two-fund universe is USD-only today (`FundCommitment.currency`) and none of the three document schemas (`CapitalCallNotice`, `DistributionNotice`, `CapitalAccountStatement`) carries a currency field to convert from. A non-USD fund would need that field added to the extraction schema and the gold SQL, not silently assumed away — recorded as a real limitation, not solved for a case that doesn't exist yet.
- **The asset class is named `'Alternatives'`, not `'Private Markets'`**, to land on the color slot `web/src/palette.ts`'s `ASSET_CLASS_SLOT` already reserved for exactly this — a fixed, stable hue rather than falling through to the arbitrary-index fallback.
- **Migration is additive only.** `V5__alts.sql` adds `client_wealth.alts_usd` via `ALTER TABLE ... ADD COLUMN ... DEFAULT 0` and a new `alts_holdings` table; V1-V4 are untouched, since Flyway checksums an already-applied migration and editing one breaks every environment (local dev, RDS prod) that has already run it.
- **Verified:** `export` 41/41, `mvn verify` 32/32, `web` 9/9, `make lint`/`make test` clean across ingest/reference/export/alts-hitl. **Not yet verified against live Databricks data** — the job that would run the two modified notebooks pulls from `main` via `git_source` (same mechanism D-018/D-058 already documented), so it cannot see an unpushed branch. A real run against the lakehouse — actual MOIC values, whether the forward-fill's date-union logic behaves as designed against the real statement/wealth-date overlap — is still owed before this is trustworthy in a demo.
- **Alternatives:** a standalone `gold_alts_holdings` table with no change to wealth/allocation/performance — rejected per above, it leaves the dashboard's headline number silently incomplete; converting non-USD funds preemptively — rejected, no such fund exists in the current universe and the document schemas don't carry currency at all.

## D-061 · 2026-07-23 · A three-fund, three-template, two-currency alts corpus

- **Context:** the user's own critique, raised 2026-07-22 and revived this session — every alts PDF was one reportlab layout, one vocabulary, one locale, so extraction scored 100% field accuracy. That number is a property of the fixture, not evidence the extractor works: the review queue has only ever caught the arithmetic/sequence defects `defects.py` deliberately injects, never a genuine misreading, which is exactly the failure a HITL queue exists to catch. Built as the user's own recommendation had it: multiple layouts + vocabulary drift + locale/currency, together, as the cheap slice with no new dependencies.
- **`DocTemplate` (render.py):** one fund administrator's conventions — title wording, field labels, money/date formatting, light visual styling — bundled into a dataclass. Three instances: `PLAIN` (the original layout, kept as the default so every existing test keeps passing unmodified), `DRAWDOWN` (vocabulary drift only — "Drawdown Notice", "Cumulative Drawn" — isolating "does extraction generalise past one wording" from currency), `EURO` (European number/date formatting *and* its own vocabulary, on a genuinely EUR-denominated third fund).
- **The third fund is real money, not a skin.** `FUND-EU01` ("Alpenrose Capital Fund III", €1,500,000 commitment) rolls up to `FQ5521` — deliberately the same account as `FUND-VC01` (Bramwell): a real LP can hold more than one fund interest through one custody account, and FQ5521's own base currency is already EUR (`accounts.py`), so pairing them tells a coherent story rather than an arbitrary one. Choosing a genuine EUR fund over EUR-*looking* USD figures was the point: it exercises `parse_decimal` (D-053 already found one real bug there) and the extraction schema's now-added date-normalisation instruction (`DD/MM/YYYY`, genuinely ambiguous against US ordering for any day ≤ 12 — real extraction difficulty, not an injected defect) against something that actually has to convert correctly downstream, not just look different.
- **Currency threads through every layer, not just the document.** `CapitalCallNotice`/`DistributionNotice`/`CapitalAccountStatement` gain a `currency` field (from `FundCommitment.currency`, already present); every tool schema in `extract.py` gains a required `currency` property; `silver_alts_documents` gains a `currency` column (sourced from the original extraction's own fields, same reasoning as `account_id` — reliable even for a document still awaiting review); `gold_alts_holdings` converts every native-currency figure to USD via the existing `fx` view, at the rate for that figure's own as-of date (the latest confirmed statement, or the fund's earliest document date if none is confirmed yet); `gold_client_wealth`'s alts fold-in (D-060) and `gold_asset_allocation`'s `'Alternatives'` class convert the same way, per statement date. MOIC needs no conversion — a ratio's numerator and denominator scale by the same rate, so it cancels regardless of which currency it started in.
- **A real encoding bug, found and fixed along the way:** reportlab's standard Helvetica font has no embedded ToUnicode mapping for `€` or accented Latin characters, so `pypdf`'s extractor — the same path production uses — silently read them back as U+FFFD. This would have fed a mangled amount string to the LLM without ever raising an error. Fixed by using `EUR` as a prefix instead of the `€` glyph (which also happens to be exactly the ISO 4217 code the schema asks for, visible verbatim in the text) and dropping the accented character from the EUR template's letterhead. The same class of bug turned out to already exist in the *original* PLAIN/DRAWDOWN templates too (an em-dash in the statement title) — fixed there as well, a one-line drive-by since it was the identical root cause just found.
- **The FX rate floor had to move.** `parvum_reference.ecb._FLOOR` was `2026-01-01` — "just before the earliest data this project generates," true when written, when only the custodial feed's short window needed covering. The alts corpus's document history starts `2024-03-31`; converting the EUR fund's NAV at its own statement dates needs ECB rates that far back too. Moved to `2024-01-01`. `gold_reports.py`'s FX date range widened correspondingly, using `silver_alts_documents.period_end`'s MIN/MAX unioned with the custodial feed's own range — sufficient by construction, not by luck: `parvum_alts_hitl.book`'s statement quarters (1 through 10) are already a superset of the call quarters (1,2,4,6) and distribution quarters (7,9), so every call/distribution date already falls within some statement's own date range.
- **Not yet verified against live Databricks data, for two independent reasons.** First, the usual one (D-018/D-058/D-060): the job runs `main` via `git_source`, so it cannot see this branch pre-merge. Second, new this time: re-fetching the ECB history to the widened floor requires a network call to `ecb.europa.eu` that this session's sandbox could not complete — every attempt (Python `urllib`, `curl`, with retries and long timeouts) had the connection reset mid-download, most likely a security-policy/proxy restriction in this environment rather than a code problem (the same session also hit a blocked `.exe` spawn from an Application Control policy). **`make fetch-fx && make land-fx` needs to succeed once, from an environment with a working path to that host, before the gold job can run past the FX-widening change without raising** (`fill_forward` refuses to start before the earliest rate it has).
- **Verified:** `alts-hitl` 65/65 (6 new: currency threads through `book.py`, both new templates' vocabulary/formatting, a regression test that no rendered template extracts with U+FFFD anywhere, the extraction schemas all require `currency`, the fund universe spans more than one currency). `reference` 31/31 (fixture adjusted for the new floor). `export` 41/41, `mvn verify` clean (currency added to `alts_holdings` via an additive `V6__alts_currency.sql`, same reasoning as V5 — V1-V5 are already applied and Flyway checksums them). `web` 10/10 (a EUR fund gets a `(EUR)` annotation on its dashboard row, a USD one doesn't). `make lint`/`make test` clean across all four Python packages. The regenerated 48-document corpus was inspected by hand — all three templates render correctly, all extract cleanly with zero mojibake.
- **Alternatives:** scan-based OCR difficulty (rendering to a low-DPI image with skew/speckle/artifacts, so the document needs real OCR before extraction) — deferred as its own future slice: it needs a new dependency (an OCR engine), produces a real accuracy drop, and probably needs a confidence-model rethink, none of which belongs alongside the cheap layout/vocabulary/locale changes here; skipping the currency-conversion plumbing and treating the EUR fund as USD — rejected, having just built D-060's gold-layer wealth fold-in this same session, that would be knowingly shipping a silent mispricing rather than an acknowledged limitation.

## D-062 · 2026-07-23 · The review queue and its PDFs go stale without a laptop — automate the other half of export

- **Context:** `export-gold.yml` (D-039) automated the client dashboard's data path months ago, but the alts review queue's export path never got the same treatment. `load-review-queue` (D-054) and `sync-review-decisions` (D-055) stayed local-only `make` targets. Surfaced concretely this session: D-061's new-template PDFs were live in the lakehouse after `export-gold` ran, but the internal app kept showing the old ones — because `export-gold` only ever touched the four wealth/allocation/performance-shaped projections, never the review-queue schema or the mirrored PDF bytes (D-057). Left alone, this was going to keep drifting: every future needs_review document, every reviewer decision, would sit invisible until someone happened to run two make targets from this machine.
- **Choice: `sync-review-queue.yml`, mirroring `export-gold.yml`'s shape exactly.** Same OIDC role, same SSM-sourced RDS password, same weekday schedule (offset 10 minutes later, purely so the two workflows' logs don't interleave — there is no ordering dependency between them). One job, two steps: `sync-review-decisions` first, `load-review-queue` second with `if: always()` so a sync failure never blocks the queue/PDF refresh — the two read and write disjoint state and have no reason to be coupled.
- **A real, separate bug this surfaced: the alts job's file-arrival trigger only ever watched `landing/alts/raw/`.** `bronze_alts_ingest.py` has always read three subdirectories — `raw/`, `extracted/`, `reviewed/` — but the trigger (`databricks.yml`) was scoped to the first. Landing a decision file into `reviewed/` (what `sync-review-decisions` now does on a schedule) would never have fired `parvum-alts-ingest` at all, automated or not — the exact D-018 blind spot this project already knows about, just not yet found in this corner. Fixed by widening the trigger `url` to the `landing/alts/` parent. Config-only (no new task, no new notebook), so per this project's own deploy-order rule it was safe to `databricks bundle deploy` ahead of merge to verify — `databricks bundle validate` confirmed the change is syntactically sound; the live redeploy is recorded in BUILD_LOG once run.
- **A decision synced today is not guaranteed to be `reviewed_status`-visible until tomorrow's run**, same shape as export-gold's own one-day lag relative to daily-feeds: landing a decision file only makes it eligible for the alts job's file-arrival trigger to pick up; that job's own run time, plus this workflow's own next scheduled firing, is what actually surfaces it as `confirmed_fields_json` on the dashboard or `reviewed_status` in the queue. Accepted rather than engineered around — chaining a wait-for-the-Databricks-run step into this workflow would trade a same-day gap for a much longer, flakier one (Databricks Free Edition serverless start-up time is not bounded tightly enough to poll against inside a GitHub Actions job with any confidence).
- **Verified:** `databricks bundle validate` clean against the widened trigger. Not yet verified end-to-end against a live scheduled firing of `sync-review-queue.yml` (first run is the next weekday 08:10 UTC after merge, or an immediate `workflow_dispatch`) — recorded once confirmed, same as every other CI-only-verifiable change in this project.
- **Alternatives:** folding these two steps into `export-gold.yml` itself — rejected, the two exports serve different consumers (client dashboard vs. internal review app) on different Postgres schemas, and keeping them as separate workflows means a slow or failing review-queue sync can never delay or fail the wealth-data refresh a demo viewer is more likely to be looking at; three separate file-arrival triggers, one per subdirectory — rejected, Databricks Jobs API allows exactly one trigger per job, and a single parent-path trigger already covers all three landing subdirectories correctly.

## D-063 · 2026-07-24 · A fourth alts fund closes Aldergate's empty Alternatives tab

- **Context:** `FUND_UNIVERSE` (D-046, widened by D-061) had three funds, all rolling up to `X4478210` or `FQ5521` — both owned by Okafor/Reyes, i.e. Stonefield. Aldergate (the Hartwell family) had zero private-fund commitments, so its Alternatives tab was correctly empty, not broken — but since Aldergate/Hartwell is first in `TENANTS` and loads by default, anyone opening the client dashboard saw an apparently-missing feature unless they switched tenants. Surfaced by the user ahead of sharing the demo link externally.
- **Choice: a fourth fund, `FUND-PE02` ("Wraithmoor Endowment Partners III", $3,000,000 USD commitment), rolling up to `FQ9007`** — the one account owned outright by the Hartwell Family Foundation (`ownership.py`), not the Trust that owns Hartwell's other two accounts. Reuses the `PLAIN` template rather than adding a fourth document convention: the corpus already spans three layouts/vocabularies/locales (D-061), and a fourth teaches extraction nothing new.
- **Why `FQ9007` specifically:** it's Hartwell's only account with a single, uncomplicated owner — no trust/foundation split to reason about (unlike `60011234`/`60018852`) and no cross-family entanglement (unlike Meridian LLC's Reyes/Okafor 60/40 split on `X4478210`). The cleanest possible story for a fourth fund: one family, one foundation, one fund.
- **USD-only was a deliberate constraint, not an oversight.** `gold_reports.py` only invokes the `fx` join when `currency != 'USD'`; a USD fund needs no ECB rate history at all. D-061's live run had been blocked for hours by an inability to reach `ecb.europa.eu` from this environment to widen the FX floor for its EUR fund — choosing USD for this fund sidesteps that entire class of failure.
- **Purely additive, verified byte-level.** `generate.py`'s `FUND_UNIVERSE` is a tuple; the new entry is appended last, so `fund_index` for the existing three funds (0, 1, 2) — and therefore every seeded defect/injection decision — is unchanged. Regenerating all 64 documents and diffing extracted text (not raw bytes — reportlab embeds a real `CreationDate`/`ModDate` per run, so byte hashes differ on every generation regardless of content) against a same-session control run confirmed the three existing funds' rendered text is identical; only `FUND-PE02`'s 16 documents are new.
- **Verified live, end to end, same session:** `alts-hitl` 65/65 (unchanged — tests are parametric over `FUND_UNIVERSE`, no hardcoded fund count). Extraction via `--provider anthropic` (`claude-haiku-4-5-20251001`): 16/16 documents, 100% field accuracy and exact-match against the generator's own ground truth (`parvum-eval-alts-extraction`). Landed to the alts volume and processed by the already-merged, fund-count-agnostic `parvum-alts-ingest` job: 8 of 16 documents routed `auto_accept` (including the latest statement, 2026-06-30), 8 `needs_review` — no code changes to `bronze_alts_ingest.py`/`silver_alts_documents.py` were needed, since neither hardcodes a fund list. Ran `parvum-ingest` (the main bronze→silver→dq_recon→gold chain, also unmodified) to fold the confirmed documents into gold: `gold_client_wealth` for `CLI-HARTWELL` gained `alts_usd = $1,859,884.15`; `gold_alts_holdings` shows `FUND-PE02` at MOIC 4.13x. Ran `export-gold` (manually dispatched via GitHub Actions, since the RDS password only resolves inside that runner via OIDC+SSM, D-039) and confirmed against the live production API: `GET /tenants/aldergate/wealth` returns `altsUsd: 1859884.15` for Hartwell; `GET /tenants/stonefield/wealth` returns Okafor/Reyes figures byte-identical to before this change — confirming the addition is genuinely additive in production, not just in the lakehouse.
- **A local environment blocker, unrelated to this change but worth recording:** this session's `uv`-managed Python 3.12 interpreter (and a Chocolatey-installed one) could no longer load their own `_socket` extension module — a newly-encountered Windows Application Control policy blocks the DLL outright, while the officially-signed system Python (`C:\Python314`) is unaffected. This would have blocked `pytest`, LLM extraction, and `export-gold` entirely. Worked around by building a throwaway venv on the system interpreter (`uv venv --python "C:\Python314\python.exe"`) and installing the same packages into it — a session-local workaround, not a project change; nothing in the repo depends on it.
- **Alternatives:** `60011234` or `60018852` (Hartwell Trust-owned) — passed over for `FQ9007`'s cleaner single-owner story; a EUR or second-currency fund for variety — rejected given the FX-floor blocker D-061 already hit, and variety-of-currency was already this corpus's point, not this fund's; adding two funds instead of one — rejected as more than needed to make the tab non-empty, and more LLM spend than the fix requires.

## D-064 · 2026-07-28 · The reconcile badge and the alts pending chip get real numbers, not just a verdict

- **Context:** the user opened the client dashboard and flagged two spots where the UI states a verdict with no way to interpret it: the Overview header's "Reconciliation variance" badge is rendered from a single boolean (`books_reconcile`, an `every()` across every account a client owns — one broken account-day turns the whole client's badge red with no way to tell which account or by how much), and the Alternatives tab's "N pending review" chip is a bare count with only a tooltip-free `title` attribute (the explanatory paragraph above the table is easy to scroll past). Both signals were already DQ-honest in the sense D-043/D-060 established — they don't hide a problem — but neither said enough to be useful standalone.
- **Choice: add the missing detail to gold, not just to the presentation layer.** `gold_client_wealth` gains `reconcile_break_accounts` (count of this client's accounts failing `dq_cash_integrity.conformed_consistent` on this date) and `reconcile_variance_usd` (this client's ownership-prorated share of those accounts' own `|delta_conformed|`, converted to USD at the day's rate) — both computed in the same `quality` CTE that already produces `books_reconcile`, so a FALSE verdict can now say how many accounts and how much, not just yes/no. `gold_alts_holdings` gains `pending_review_doc_types` (distinct `doc_type`s among a fund's undecided `needs_review` documents, e.g. "capital_account_statement, distribution") and `pending_review_latest_period` (the latest `period_end` among pending statements) — computed in Python alongside the existing `pending_review_documents` count, from the same `silver_alts_documents` query, just not collapsed to a bare `COUNT(*)` before it reaches gold.
- **Why gold and not just labels:** a UI-only fix (better copy, a real tooltip) would have made the mystery look better dressed without answering it — the user's actual question was "variance of what" and "pending review of what," which only the underlying detail can answer. Presentation-only was considered (scope A in the options given to the user) and rejected for exactly this reason.
- **A full per-account exceptions table (scope C: a new `gold_reconciliation_exceptions` table + endpoint, click-to-drill-down) was considered and deferred**, not rejected outright. The two new gold columns answer "how many, how much, what's waiting" at the client-dashboard's existing grain; the per-account arithmetic (which account, which day, opening vs. closing) is a back-office diagnostic, and this project already has a place for that class of detail — the internal app's existing DQ/Ops tooling — if it's ever needed. Adding a new table and endpoint for a client-facing screen that only needs a rollup would be over-building for the audience actually looking at it.
- **This does not reopen D-044's internal/external line.** D-044 kept `dq_metrics` — pipeline-wide freshness/completeness/accuracy/exceptions, an operational-health concern for the firm's own staff — off the client dashboard and on the internal app's Ops tab. What's added here is different in kind: it's the client's *own* account-level reconciliation status and their *own* fund's pending-document detail, the same category of information `books_reconcile` and `pending_review_documents` already surfaced on the client dashboard before this change — this just fills in the "how much" and "what" that were already implied but not computed. No pipeline-wide metric crosses the tenant boundary as a result of this decision.
- **Verified against live Databricks data and live production, post-merge, same day.** `make deploy-job` + `make run-job` against merged `main` produced real, non-default numbers on both new column pairs — e.g. Hartwell's `books_reconcile=false` now carries `reconcile_break_accounts=1, reconcile_variance_usd=$22.50` (a real, small cash-integrity gap), and every alts fund's `pending_review_documents` carries real `pending_review_doc_types`/`pending_review_latest_period`. `export-gold.yml` reloaded production RDS; the live API returns the same numbers as the lakehouse (`GET /tenants/aldergate/wealth`, `GET /tenants/aldergate/alts-holdings`). The structural gap D-018/D-060/D-061/D-062 describe (a feature branch's notebook edits are invisible to `parvum-ingest` until merged, since its tasks pull from `main` via `git_source`) held true here too — the pre-merge run in this same decision's earlier note executed the old notebook, exactly as expected.
- **Alternatives:** presentation-only (better copy/tooltips, no new gold columns) — rejected per above, doesn't answer the question that was actually asked; a full per-account exceptions table and endpoint — deferred rather than rejected, belongs in the internal app if a genuine back-office drill-down need shows up later; hiding `reconcile_variance_usd` when it's small (a materiality threshold) — rejected as unnecessary complexity for a synthetic demo dataset with no real materiality policy to encode.

## D-065 · 2026-07-28 · The per-account drill-down D-064 deferred turns out to be exactly what the client wants; the doc-type taxonomy turns out to be exactly what they don't

- **Context:** live within hours of D-064 shipping, the user pushed back on both spots it had just fixed. The reconcile badge's "1 of 3 accounts · $23" answered "how many, how much" but not "which one" — a real gap, since a client (or their advisor) can't act on "one of your accounts has a problem" without knowing which. The alts chip's "7 pending review · capital_account_statement, capital_call · through 31 Mar 2026" answered "what kind of document," but that's this project's own internal document taxonomy, not a concept a wealth client has any reason to know — the client cares whether a number is current, not what artifact is sitting in a review queue.
- **D-064 explicitly deferred the per-account table** ("belongs in the internal app if a genuine back-office drill-down need shows up later") on the assumption that per-account arithmetic was inherently a back-office concern, mismatched to the client-dashboard audience. That assumption didn't survive contact with an actual person looking at the actual screen: a client-facing consumer directly asked "which account," the exact question the deferred scope would have answered. Recorded here rather than quietly reversed, because the reasoning that led to deferring it was sound *given what was known at the time* — it just turned out to be wrong once someone reacted to the shipped feature rather than the mockup of it.
- **Choice: build the per-account table after all, scoped tighter than D-064's original "scope C."** `gold_reconciliation_exceptions` — one row per (client, account) currently failing the conformed cash check, latest date only (same "baked in at gold, no query-time filtering" pattern as `gold_top_holdings`), carrying a *signed* delta (unlike the aggregate's `ABS`-summed `reconcile_variance_usd`, a single account's own gap direction is real information once you're looking at just that account). Not the fuller "which day, which movement" diagnostic scope C sketched — just enough to answer "which account, how much on that one," which is what was actually asked for.
- **The reconcile badge becomes a real `<button>`, not a decorative `<span>` with a `title`.** Two problems, one fix: the user also reported the hover tooltip only seemed to fire over the small dot, not the message text next to it. Rather than debug the exact CSS/DOM cause, converting the whole badge to a single `<button>` wrapping both the dot and the text makes the entire pill one hoverable *and* clickable target unconditionally — clicking toggles an inline panel (not a second tooltip; a list needs real layout, and tooltips don't work on touch at all) listing each broken account and its signed USD delta. The "ok" (clean) state stays a plain `<span>` — nothing to click into when there's no detail to show, and turning an inert good-state badge into a button would be pointless affordance.
- **Doc types removed, not just hidden.** `gold_alts_holdings.pending_review_doc_types` (D-064) and its counterparts through serving/export/web are deleted outright rather than left wired-but-unused: nothing reads it after this change, and per this project's own standing practice, an unused column carried through four layers is dead weight, not a hedge for later. The alts chip now reads plain language — "Newer figures pending · through 31 Mar 2026" — using `pending_review_latest_period` (kept; it answers "how stale," which the client does care about) with no enumeration of what internal document types are behind it.
- **One branch, not two.** D-064 shipped as two stacked PRs (gold-only, then serving/export/web) because the total diff was large. This follow-up touches the same four layers but is comparably small, and the git_source live-verification gap (spark changes are invisible to the Databricks job until merged) applies identically whether it's one branch or two — splitting for that reason alone would have been process without benefit here.
- **Verified:** `mvn verify` 32/32, `export` 42/42 (new round-trip test for a *negative* signed delta, proving the column isn't silently assumed unsigned), `web` 12/12 (new test: the panel is absent until clicked, present with the right account and amount after). Visually confirmed in a real browser: clicking the badge reveals "Accounts behind this variance / X4478210 · $1,875" instantly, no tooltip precision required; the alts chip shows "Newer figures pending · through 30 Sept 2026" with zero mention of document types.
- **Verified against live Databricks data and live production, post-merge, same day.** `make deploy-job` + `make run-job` against merged `main` produced one real row in `gold_reconciliation_exceptions`: Hartwell's account `60018852`, a $22.50 delta — the exact account behind the aggregate `reconcile_break_accounts=1, reconcile_variance_usd=$22.50` D-064 already exposed. `gold_alts_holdings` confirmed down to 17 columns with `pending_review_doc_types` gone. `export-gold.yml` reloaded production RDS; `GET /tenants/aldergate/reconciliation-exceptions` returns the same account/figure as the lakehouse, and `GET /tenants/aldergate/alts-holdings` carries no `pendingReviewDocTypes` field. Same structural gap as every prior gold-layer change in this project (D-018/D-060/D-061/D-062/D-064) — the pre-merge run in this decision's earlier note necessarily executed the old notebook.
- **Alternatives:** a fuller exceptions table carrying the actual day-by-day arithmetic (opening/movements/closing per broken day, not just the current gap) — rejected as more than the question asked for; keeping `pending_review_doc_types` and just hiding it behind a feature flag or config toggle for "client vs. internal" audiences — rejected, this project has no internal app consumer for it either, so there's nothing to preserve it for; a hover-only rich tooltip (CSS `:hover` panel, no click/JS state) instead of a real click-to-expand — rejected, it doesn't work on touch devices at all and this is a wealth-management demo, not a desktop-only internal tool.

## D-066 · 2026-08-17 · A new Berkshire 13F position broke the share-divisor guard D-015 built for exactly this

- **Context:** the daily GitHub Action failed at `make generate`. Berkshire's 2026-Q2 13F (period 2026-06-30, filed 2026-08-14 — the daily job's routine fetch pulled it the moment it existed) opened a 3,564-share D.R. Horton position, smaller than the previous binding constraint (NVR's 11,112 shares, D-015) by a wide margin. Both Berkshire-seeded accounts' divisors — `60011234` Growth Portfolio at 10,000, `60018852` Retirement at 20,000 — round 3,564 shares to zero, and `_seed_position` raised exactly as D-015 designed it to: "a future filer that doesn't suit the divisor forces a conscious decision," not a hole discovered later in the data. This is that decision, not a bug fix to the guard itself.
- **Choice:** lower both divisors to `2,000` (`60011234`) and `4,000` (`60018852`) — the same 1:2 ratio the original 10k:20k pair carried, so the two accounts stay "about half" of each other exactly as `test_same_filer_two_accounts_differ_only_in_scale` already asserts, and Growth Portfolio stays the larger-notional of the pair. Chosen with real margin, not just enough to clear the rounding threshold: D.R. Horton's 3,564 shares round to 2 at the new Growth divisor and to 1 at the new Retirement divisor with room (`3564/4000 = 0.891`, vs. the previous pair's `11112/20000 = 0.556` — tighter margin against the *old* constraint) before the next quarter's trim reopens this same failure.
- **Why not just patch the guard to floor at 1 share instead of raising:** that was the alternative D-015 explicitly rejected — floors and silent minimums turn a real scale mismatch into a data quality bug nobody notices until it shows up as a suspiciously round position downstream. The whole point of the loud failure is that recalibrating the divisor is a choice about what scale these two synthetic portfolios represent, and that choice belongs in a decision record, not a magic-number floor buried in `_seed_position`.
- **Verified:** `make generate` (the exact failing CI step, 90-day/64-business-day window spanning the new filing regime) completes clean; `ingest` 118/118, `reference` 31/31 (+1 skip); `make fmt`/`make lint` clean across all four workspace packages.
- **Scope note:** this only affects days on or after the new filing's effective date (2026-08-14 onward, via `filing_in_effect`) — already-landed historical days are untouched, and every non-Berkshire account (`FQ5521`, `X4478210`, `FQ9007`) is unaffected, since they seed from different filers.
- **Alternatives:** float the two divisors independently to whatever value best fits each account's own margin — rejected in favor of preserving the existing 1:2 ratio, since nothing about this incident calls the *relationship* between the two accounts into question, only their absolute scale; widen `_COST_BASIS_MIN_FACTOR`-style tolerance instead of touching the divisor — not applicable, cost basis is a separate concern from share-count scaling; a smaller divisor cut (e.g. ~7,000/14,000, right at the D.R. Horton rounding edge) that would have kept notional scale closer to the original — rejected in favor of real margin against the next quarter's trim, accepting the scale jump described below as the cost of that margin.
- **Verified against live Databricks data and live production, same day.** Merged as PR #97; user re-ran `daily-feeds.yml`, whose new `date=2026-08-17` landing fired `parvum-ingest` automatically (D-018) — all five tasks `SUCCESS`. `silver_positions` confirmed real, non-zero D.R. Horton rows: `60011234` quantity 2, `60018852` quantity 1. `export-gold.yml` reloaded production RDS; the live API returns `asOf: "2026-08-17"`.
- **A real, sizeable side effect, not a defect:** since `market_value ≈ real_filing_value / divisor`, cutting both divisors 5x scales every position in those two accounts by roughly the same factor. Hartwell — the sole owner of both accounts — moved from `totalWealthUsd` ~$41.1M to ~$221.2M on this run. This is the direct arithmetic consequence of choosing margin over scale-preservation above; recorded explicitly because any external reference material that quotes the old figure verbatim would now be visibly stale against the live site.

## D-067 · 2026-08-20 · A Critical Data Element register, enforced by CI rather than by good intentions

- **Context:** the platform had good *quality* — reconciliation graded against defect manifests, a declarative `dq_metrics` rollup, catalog descriptions on all 295 published columns — and no *governance*. Nothing recorded which of those columns actually matter, who is accountable for one, or which control tests it. Every one of those facts lived in whoever last touched the code. That is the gap that turns a data platform into a data estate nobody can hand over: quality answers "is this number right", governance answers "who owes an answer when it isn't".
- **Choice:** a Critical Data Element register (`governance/cde_registry.yml`) covering **every** published column across bronze, silver, dq and gold, plus a CI gate (`parvum-check-governance`, its own status check) that reconciles the register against what the Spark jobs really publish. Each column carries a tier and an owner; `critical` additionally owes a business definition, a named SLO, and either the quality rules that test it or a stated `control_gap`. Five rules fail the build: `unclassified`, `orphan`, `missing_description`, `incomplete_obligation`, `invalid_reference`.
- **Why every column and not just gold:** a register of only the obvious things proves nothing about the rest. Classification is cheap — a `supporting` or `operational` column owes nothing but an owner, and shared names (`rebuilt_at`, `client_id`, `ownership_pct`) are declared once in `common_columns` rather than 20 times. What the wide scope buys is that *silence becomes impossible*: there is no unclassified corner of the estate for a new column to hide in.
- **Why tiers, and why the critical list is short:** 28 of 295 columns (9.5%) are critical. Criticality stops at the layer the business consumes — `gold_client_wealth.total_wealth_usd` is critical, the `silver_positions.market_value` it is computed from is `supporting`, which is exactly what that tier name means. Letting criticality propagate upstream would make ~all of silver and bronze critical, and a register where everything is critical says nothing at all.
- **Why the tier obligations live in `registry.py` and not in the YAML:** a register able to relax its own rules is not a control. If the obligation matrix were data, the same pull request that adds an unclassified column could also delete the rule that catches it, in one diff, and the gate would go green.
- **Why the gate parses the Spark jobs instead of reading Unity Catalog:** the `COLUMN_COMMENTS` dicts are upstream of the catalog — the jobs apply them with `ALTER COLUMN ... COMMENT` — so they are the authoritative statement of what gets published, and they are available in a pull request *before* anything is deployed. Reading them needs `ast.literal_eval` rather than an import, because these files call `spark.sql` at module scope and cannot be imported outside a cluster. A gate that needed a live warehouse would also be a gate that fails when the network does.
- **Why a critical element may declare a `control_gap`:** forcing every critical column to name a quality rule would have produced citations of loosely-related metrics — the register would read as fully covered and mean nothing. Allowing an explicit, argued gap instead produced an honest first number: **10 of 28 critical elements have a real control (35.7%); 18 carry a named gap.** The gaps are specific and actionable, and three of them are genuine findings this exercise surfaced: nothing re-checks a landed ECB FX rate (a wrong rate would misstate every EUR figure while every other control passed); no `dq_metrics` rule rolls up the alts chain's silver-level validation; nothing recomputes the TWR/Dietz/IRR figures independently on a schedule. The ownership graph's own invariants are a fourth — proven at build time, never surfaced as a daily signal.
- **Alternatives:** *put the register in Delta and query it from CI* — rejected: governance metadata belongs in version control, where changing an owner is a reviewable diff rather than an `UPDATE` nobody sees, and where the gate needs no credentials. *Extend `COLUMN_COMMENTS` with structured metadata in place* — rejected: it would put governance fields in seven notebooks and make the gate's own source its subject, and notebook diffs are the worst place to review an ownership change. *Fold the checker into `parvum_reference`* — rejected: a control should not live inside the thing it controls, and a separate `governance` status check fails visibly on its own line.
- **Deliberately deferred:** publishing the register into the lakehouse as a queryable table joined into `dq_metrics` as coverage %, and surfacing it on the Ops page. Both want the register to exist and settle first; SLO *attainment* (as opposed to SLO declaration) is a separate slice with its own runbook and error-budget questions.

## D-068 · 2026-08-20 · The register becomes queryable without stopping being reviewable

- **Context:** D-067 put the Critical Data Element register in the repo as YAML and enforced it in CI. That makes an ownership change a reviewable diff, which is the property that matters most — but it also means the register is invisible to anyone who does not read pull requests. Nobody can trend governance coverage, nobody can join a column's owner to the data itself, and the honest 35.7% control-coverage figure lives only in a CI log. A register nobody can query is a register nobody consults.
- **Choice:** keep the YAML as the single source of truth and land a **resolved snapshot** of it in the Unity Catalog volume alongside `fx_rates.json` and `securities_master.json`, using the same pull-not-push contract every other reference feed already uses (D-006). `parvum-publish-registry` writes JSON Lines; `make land-registry` uploads it; the daily Action lands it beside the FX rates; `spark/dq_recon.py` reads it into `governance_cde_registry` and rolls four metrics into `dq_metrics` under a new **`governance` dimension**.
- **Why the snapshot holds every *published* column rather than every *registered* one:** so the lakehouse computes its own classification coverage from the rows instead of being handed a number. An unclassified column arrives with a NULL tier and drags `columns_classified_rate` below 1.0. The CI gate is what keeps it at 100%; this measures it independently, and two independent mechanisms agreeing is worth more than one asserting.
- **Why `governance_` is a new layer prefix, and the recursion that follows:** the register is not a check, so `dq_` would have been the wrong name for it — D-067 argued that governance sits beside the pipeline rather than inside it, and the table naming should say the same thing. The consequence is that **the register has to classify its own table**: adding `governance_cde_registry`'s `COLUMN_COMMENTS` made the gate fail with 14 `unclassified` findings until those 14 columns were entered in the very file they describe. That is the correct behaviour and a pleasing property — the control is subject to itself — not an accident to work around.
- **Why it is built inside `dq_recon.py` rather than a new notebook:** a new notebook means a new bundle task, and the standing rule (learned twice) is that a task pointing at a new notebook must not be deployed before merge, because the job runs `main` via `git_source` and a pre-merge trigger fires against code that does not exist yet. Folding the cell into an existing task removes that hazard entirely, and `dq_metrics` — two cells down — is the natural consumer anyway. The cost is that a job named for reconciliation now also publishes a governance table; the cell's own prose says so plainly.
- **Why `quality_rules` is a comma-joined string and not an array:** a Delta `array<string>` is the truer type, but this table exists to be *read* — through the exporter, into Postgres, onto a screen — and an array stops the exporter's wire-format conversion dead (unknown type = loud stop, D-029). `quality_rule_count` carries the one thing flattening would otherwise cost: counting without parsing. The SLO is flattened into the row for the same reason — one table that answers a question beats two that need joining.
- **Why the control-coverage target is 80% when the estate delivers 35.7%:** a target set to today's number is not a target. `critical_control_coverage_rate` therefore ships `passed = false`, and the Ops page will show governance failing — which is true, and is exactly the signal D-011's deliberate defect rate and the honest 20–100% accuracy figures already established as this project's house style. The 18 stated control gaps are the work list that closes it.
- **Why publishing refuses to run when the gate fails:** a snapshot of a broken register would put wrong ownership into the lakehouse and onto a screen. Publishing nothing is the safer failure.
- **Alternatives:** *read the YAML directly from the Databricks Git folder* — rejected: it would make the Spark job depend on repo file layout and on PyYAML being present in the cluster, and it would bypass the landing contract every other reference input already honours. *Make the register a Delta table people edit in Databricks* — rejected for the reason D-067 gave: an `UPDATE` nobody reviews is not governance. *Publish only the summary counts rather than the rows* — rejected: the interesting artefact is the 18 named control gaps, and a summary cannot show them.

## D-069 · 2026-08-20 · Governance reaches a screen, and the screen shows the gaps rather than the score

- **Context:** D-068 made the register queryable in the lakehouse. That is where an analyst would find it, but it is not where anyone *looks* — the Ops page in the internal app is. This is the projection half: `V9__governance_registry.sql`, a `cde_registry` table in every tenant schema, a `/internal/tenants/{id}/cde-registry` endpoint, an exporter entry, and a Governance section on the Ops page. Same split as D-043 (gold layer) and D-044 (serving/export/web) before it.
- **The check constraint earned its keep before any of this shipped.** `V4__dq_metrics.sql` pinned `dimension` to four values with an inline check. Adding a fifth in the lakehouse meant the very first export would have failed on that constraint rather than silently loading a dimension the schema did not know about. That is the constraint working, not an obstacle: **V9 extends the allowed set deliberately** instead of dropping the check, because a column whose allowed values are written down is a column whose meaning cannot drift.
- **Why the drop is `if exists`, which looks like defensiveness and is not:** jOOQ generates its classes by replaying this DDL through an in-memory H2, and H2 does not give V4's inline check the `dq_metrics_dimension_check` name Postgres does. Without `if exists`, code generation dies before reaching the table it actually needs. The real drop-and-recreate is therefore proven where it matters — an export test inserts a `governance` row against a real Postgres migrated with this same DDL, and would fail if the old constraint survived.
- **Choice: the page leads with the named gaps, not the coverage percentage.** Two rates and two counts render as tiles, and `critical_control_coverage_rate` shows red against its 80% target — but the substance is the table below it, listing every critical element with no automated control, its owner, and the written statement of what is missing and what would close it. A percentage is a verdict; the list is a work item. This is the same correction D-065 made to the reconciliation badge, arrived at from the same instinct: a number with no way to interrogate it tells a reader they have a problem and nothing about what to do next.
- **Why `cde_registry` is duplicated into every tenant schema:** identical reasoning to `dq_metrics` (D-044) at a similar size (~300 rows). It describes the pipeline every firm's data flows through, not any one firm's clients. Building a second, non-tenant schema-management path for one small read-only table would cost more than the duplication does.
- **Why the two fetches are one `Promise.all` rather than a lazy second load:** a page that renders the coverage rate before the gaps arrive is briefly a verdict with no evidence, which is precisely the failure this design is trying to avoid. Both halves land together or the page keeps showing its loading state.
- **Nullable `tier` and `owner` in the projection, on purpose:** the register covers every column the platform *publishes*, so an unclassified column must survive the round trip as NULLs rather than being filtered out — that row is exactly what `columns_classified_rate` measures. An export test pins that behaviour.
- **Alternatives:** *show the whole 309-row register on the page* — rejected for now: the gaps are the actionable subset, and a searchable full register is a bigger feature than this slice needs. The data is all present in `cde_registry` for whenever it is wanted. *Put governance on its own page* — rejected: it answers "how much of the estate do we actually stand behind", which is the same question the rest of the Ops page asks about a given day, and splitting it would hide it. *Drop the dimension check entirely to avoid future migrations* — rejected: the check is the reason this change could not land silently.

## D-070 · 2026-08-23 · A restatement is not a return: declaring book changes, and bounding the ones nobody declared

- **Context:** D-066 recalibrated both Berkshire share divisors (10,000 → 2,000 and 20,000 → 4,000) so a new 3,564-share position would not round to zero. It was the right fix. It also rescaled every Berkshire-seeded position fivefold on 2026-08-17, and the performance chain read that step as market movement: a **single daily time-weighted return of +414.12%** on zero external flow, a since-inception TWR of **+392.70%** on the live client dashboard, and an IRR of NULL because +392% over three months annualises past the solver's +1000% bracket. Every one of those figures was arithmetically correct. All of them were false.
- **This is the same bug class for the third time, at a third layer.** D-016 fixed restatement handling at bronze, content-keyed. D-018 found the identical blind spot one layer up, at the file-arrival trigger. This is gold. The lesson D-018 recorded — that fixing a bug class at one layer says nothing about the others — held again, and is worth restating rather than quietly re-learning.
- **The register named the right area and the wrong remedy, and that correction is the honest part.** The CDE register (D-067) carried a `control_gap` against all five performance columns reading *"nothing recomputes them independently on a schedule, so a silent regression in the chaining or flow handling would not raise a metric."* An independent recomputation **would not have caught this.** The formula was never wrong; a second implementation fed the same wealth series reproduces +414.123% faithfully. What changed was the *meaning of an input*. Closing a gap by doing the thing the gap statement proposed would have left the actual failure uncaught, so the gap text is now rewritten to say what it really covers and what remains uncovered.
- **Choice: two halves, because either alone is unsound.** *Declaration* — `parvum_reference.restatements` records each book restatement (effective date, account, divisor before and after, reason, decision reference), publishes through `parvum-publish-restatements`, lands beside the FX rates and the CDE register on the same pull-not-push contract (D-006), and `gold_reports.py` reads it under an explicit schema. On a declared day `daily_twr_return` is NULL, the chained index links straight through, and the whole non-flow move is booked to a new `restatement_adjustment_usd` column with `restatement_detail` naming the account, the divisors and the decision. *Detection* — `dq_return_plausibility` recomputes the raw non-flow move from the wealth series for every client-day and flags anything outside a 25% band **that has no declaration**, rolling up into `dq_metrics` as `daily_return_plausibility_rate`.
- **Why declaration alone would be a licence:** a mechanism that lets the book label any inconvenient number a restatement, with nothing arguing back, is worse than no mechanism. The plausibility bound is what makes the declaration falsifiable — and it is deliberately computed from the raw wealth series rather than from `daily_twr_return`, so a restatement cannot hide inside the NULL it causes.
- **Why detection alone would be wrong:** on 2026-05-15 all three clients moved 4–6% on zero flow. That is a new 13F filing regime landing a quarter of movement on one date — real return, arriving lumpily because 13F data is a quarterly snapshot. A pure threshold rule would either swallow it (band too wide) or cry wolf every quarter (band too narrow). The two events are identical in shape and opposite in meaning; only the book knows which is which, so the book says so.
- **Why the band is 25%:** the largest legitimate one-day move this data has ever produced is 5.8%. A structural break arrives one to three orders of magnitude out (+414%), never at 26%. The band is stated as a judgement in the job's own prose rather than tuned to the data, because a bound fitted to observed history is a bound that will not catch the next unobserved thing.
- **Why the entire non-flow move goes to the restatement, knowing that overstates it:** 2026-08-17 was *also* a filing-regime boundary, so part of that step was genuine return — Okafor, who owns no Berkshire account, moved +2.13% that day on the same boundary. Splitting Hartwell's step into "restatement" and "real return" would require guessing at a ratio, and a client-facing performance figure is the wrong place to guess. Real books exclude a restated period and disclose it rather than partially crediting it; forfeiting one day's genuine return is the cheaper error.
- **Why a restatement enters Dietz and IRR exactly where a flow does, yet is reported separately:** arithmetically it plays the identical role — value that entered the account without the market putting it there, which must leave the numerator or the return absorbs it. Semantically it is the opposite of a flow: it is not the client's money. So it is weighted like a flow in Modified Dietz, enters the IRR series like a contribution, and gets its own disclosed column in both `gold_performance` and `gold_performance_summary` rather than being folded into net flow where no reader would ever find it.
- **Why the plausibility metric is appended to `dq_metrics` from the gold job rather than computed in `dq_recon`:** the task order is bronze → silver → `dq_recon` → gold. A metric derived from gold, computed in `dq_recon`, would report the *previous* run's numbers as today's. The alternative — a sixth bundle task — would reintroduce the deploy-before-merge hazard this project has hit twice, for two rows. So gold appends the rows only it can compute, preceded by a `DELETE` of exactly those two metric names so a gold-only re-run cannot double-count. The governance gate's metric scanner now reads both jobs, and fails loudly if either stops publishing.
- **Why a test asserts the register matches the live divisors:** declaration is worthless if someone can change a divisor and forget. `test_register_matches_the_live_divisors` compares each declared `divisor_after` against what `accounts.py` actually carries, so an undeclared change fails the suite rather than reaching the dashboard as performance. The publisher refuses to write a snapshot in the same situation. Three independent things now have to fail before a silent rescale reaches a client: the test, the publisher, and the plausibility bound.
- **Verified against the live lakehouse before merge**, by extracting the job's own SQL and running it on the warehouse rather than a retyped copy. Every pre-run prediction matched: Hartwell 2026-08-17 `daily_twr_return` NULL, `restatement_adjustment_usd` **$178,175,109.88**, `restatement_detail` naming both accounts and D-066, and `twr_index_since_inception` unchanged across the boundary at 0.95831694. Since-inception figures move from **TWR +392.70% / Dietz +393.41% / IRR NULL** to **TWR −4.17% / Dietz −3.69% / IRR −10.54%**. Okafor and Reyes are untouched, as they must be. `dq_return_plausibility` reports 0 breaks and 100% on every day; the negative control — the same query with the declaration removed — turns 2026-08-17 into a break, which is the evidence that the detector bites rather than rubber-stamping whatever is declared.
- **A side effect worth naming:** Hartwell's TWR and Dietz now differ by 48bp where Okafor's and Reyes' agree to within 4bp. `docs/PERFORMANCE_METHODOLOGY.md` predicted exactly this — Modified Dietz's linear day-weighting approximates poorly when a mid-period flow is large relative to the portfolio, and a $178M adjustment against a $45M opening balance is as large as that gets. The doc's own prediction coming true on real data is a better demonstration of why three methodologies are published than the original small-flow case was.
- **Alternatives:** *restate history, regenerating all prior dates at the new divisor* — rejected: it rewrites landed raw files, destroying the immutable-evidence property D-016 established, and it would do nothing about the quarterly filing-regime steps that will keep arriving. *Infer restatements from the data alone* — rejected above. *Recompute the returns independently, as the original control gap proposed* — rejected as the sole remedy for the reason given above; it remains a genuine second-order gap (a small silent regression still passes the band) and is recorded as such rather than declared closed. *Treat the restatement as an external flow* — rejected: it would make the client appear to have contributed $178M, which is a different false statement, and would corrupt `net_external_flow_usd`, a figure clients reconcile against their own records.

- **Verified live 2026-08-23**, after merge, with predictions recorded first and all matched: Hartwell 2026-08-17 carries `restatement_adjustment_usd` = 178,175,109.88, a NULL `daily_twr_return`, and a `twr_index_since_inception` identical to 2026-08-14 (0.95833621) — the chain links through rather than compounding. Since-inception moved from +392.70% / +393.41% / NULL to **TWR −4.167% / Dietz −3.693% / IRR −10.543%**; Okafor and Reyes unchanged, with Okafor's genuine +2.13% on the same date preserved. `dq_return_plausibility`: 270 client-days, 0 breaks, 3 first-date NULLs; `daily_return_plausibility_rate` 1.000000 and passing on all 89 days. The `DELETE`-then-`INSERT` idempotency guard was checked, not assumed — no (as_of, metric) pair appears twice.

## D-071 · 2026-08-23 · Disclosing the restatement, because the contradiction is already on the screen

- **Context:** the projection half of D-070, same split as D-043/D-044 and D-068/D-069. `V10__restatement_disclosure.sql` adds `restatement_adjustment_usd` and `restatement_detail` to `performance` and `restatement_adjustment_usd` to `performance_summary`; the two DTOs carry them; the client dashboard's Performance tab gains one tile.
- **This migration is not optional dressing.** The exporter runs `SELECT * FROM workspace.parvum.{table}` and the loader inserts by source column name — its own docstring says "a drift between lakehouse and serving schemas fails loudly at INSERT". So the moment D-070's gold columns exist, `export-gold` fails until Postgres has somewhere to put them. Gold and serving have to move together; that coupling is deliberate drift detection, not an inconvenience.
- **The disclosure was questioned, and the question was the right one to ask:** does surfacing a $178M adjustment invite questions that are hard to answer? The answer turned out to be settled by what the page *already* renders. The Performance tab shows a "Net external flow" tile reading `$44,722,729 → $221,166,594` with a net flow of `$137,500`, directly beside a TWR of `−4.17%`. Wealth quintuples, almost no client money arrives, and the return is negative. Those three figures cannot all be true together without a fourth number, and a reader who spots that reads it as a broken dashboard.
- **So the disclosure removes a question rather than adding one.** With the tile the row reconciles exactly: `44,722,729 + 137,500 + 178,175,110 − 1,868,745 = 221,166,594`, where the last term is the market loss the −4.17% describes. The tile is one line of plain language — "Change of scale, not performance — excluded from all three returns" — and needs no supporting explanation to do its job.
- **Conditional, not always-on.** The tile renders only when the adjustment is non-zero, which is almost never. A disclosure showing `$0` on every ordinary client would train readers to ignore it, and the one time it mattered they would. A test pins both halves: present when restated, absent otherwise.
- **The provenance rides along as a `title`, not a panel.** `restatement_detail` lives on the daily series, not the summary, so the dashboard joins the two and hangs the result on the tile as a hover: which account, which divisors, which decision. D-065 turned a badge into a click-to-expand panel because the underlying data was a *list* the reader might need to act on; this is a single sentence of provenance, and an expandable panel would be more machinery than the content justifies. Different content, different affordance — not an inconsistency.
- **Why `dq_return_plausibility` is not projected:** the Ops page derives its accuracy metrics generically (`[...new Set(accuracy.map(r => r.metric))]`), and `dq_metrics` already loads into every tenant schema unscoped (D-044). So `daily_return_plausibility_rate` and `return_plausibility_breaks_count` reach the screen with no code change at all. Projecting the per-client detail table as well would add a migration, an endpoint and a view for something nothing yet asks for; the rollup is the part a Data Operations reader consumes, and the detail stays queryable in the lakehouse.
- **Why the columns are `not null default 0` rather than nullable:** the adjustment is a quantity that is genuinely zero on an ordinary day, not a fact that is unknown. `NULL` would force every consumer to decide what an absent adjustment means, and at least one would decide wrong. `restatement_detail` *is* nullable, because on an ordinary day there is genuinely nothing to say.
- **Alternatives:** *fold the adjustment into `net_external_flow_usd`* — rejected for the reason D-070 gives: a client reconciling reported flows against their own bank records must not find $178M they never sent. *Show it only in the internal app* — rejected: the wrong figure was on the *client* dashboard, and moving the explanation somewhere the affected reader cannot see it fixes the number while leaving the confusion. *Suppress the pre-restatement history so the step disappears* — rejected: it would make the chart tidy by deleting the evidence, which is the same instinct D-070 rejected when it declined to rewrite the landed raw files.

- **Verified live 2026-08-23 (lakehouse half).** The gold columns exist and carry the expected values; `V10` applied cleanly through real Flyway, confirmed by the serving suite running green against a real Postgres (32/32) once Docker was available. **The production RDS reload and visual confirmation on the live dashboard remain outstanding** — until `export-gold.yml` runs, the lakehouse is correct and the served figures are not, which is precisely the gap D-045 was written about.
- **A latent bug this migration flushed out:** the export loader fixtures applied migrations with a filename sort while Flyway orders by parsed version number. Those agree only while every version is a single digit, so `V10` sorted directly after `V1` and altered `performance` three migrations before it existed — twelve `UndefinedTable` errors in CI. Production was never affected; the divergence was in the fixture, which is exactly the bug these tests exist to prevent, since a fixture that applies the same DDL in a different order than Flyway stops being evidence that one schema serves both sides (D-029). Fixed by parsing the version, with an unparseable filename raising rather than sorting somewhere arbitrary.

## D-072 · 2026-08-23 · MOIC from one document and one moment

- **Context:** a question about the client dashboard — the Alternatives tab showed every fund at a positive multiple (4.13× to 4.65×) while every client's since-inception return was negative. The two are genuinely unrelated (MOIC is one fund, alts only, since that fund's 2024 inception, over capital *called*; the return figures are whole-portfolio, over four months in 2026, over portfolio *value*), and the decomposition confirmed there was no contradiction to reconcile: within the window alts actually rose (+2.0% Okafor, +1.6% Reyes) while the listed book fell (−6.4%, −11.7%) and outweighed them. But chasing the question found that MOIC was wrong for an unrelated reason.
- **The defect: a ratio assembled from two different states of the world.** `moic = (distributed + NAV) / called`. The NAV came from the latest confirmed capital account statement — a document that reports the fund as it actually stands, and therefore already embeds *every* call the fund ever made. The denominator summed only the capital-call notices a reviewer had confirmed. Gold's "only confirmed values count" rule (deliberate, and right) was being applied per document type *independently*, so a fully-loaded numerator sat over a partially-confirmed denominator.
- **The proof that it measured review progress rather than fund performance.** The generator calls 60% of commitment over a fund's life. FUND-EU01 had 2 of 4 call notices confirmed → 35% called → MOIC 1.90×. Every other fund had 1 of 4 → 15% called → MOIC 4.13–4.65×. **Approving a pending capital call would have pushed MOIC down** — a fund appearing to perform worse because somebody did their job. A performance metric that moves when nothing about the fund has changed is not a performance metric.
- **A second symptom that had been visible in gold the whole time and unnoticed:** `called_to_date_usd + unfunded_commitment_usd` did not equal `total_commitment_usd` for a single fund. Wraithmoor was short by $1,350,000. `unfunded` came from the statement while `called` came from the notices, so the two never had to agree — and nothing checked.
- **Choice: every term of MOIC comes from the same confirmed statement.** `called` is now `total_commitment − unfunded_commitment` read off that statement, not a sum of notices. The notices remain the source only when no statement is confirmed yet, where they are the sole account of the fund that exists and are therefore internally consistent rather than mismatched.
- **Distributions are now bounded by the statement's own date, too.** A distribution dated after the NAV snapshot has not yet been deducted from that carried-forward NAV, so adding it counts the same money twice. This was live: FUND-EU01's latest confirmed statement is 2025-12-31 while its latest confirmed distribution is dated 2026-03-31. Restricting to distributions on or before the period end is what makes "one moment" true rather than merely stated.
- **Why not simply confirm the outstanding notices instead:** that would fix today's numbers and leave the defect. The queue will always have unprocessed documents — that is what a review queue *is* — so a metric that is only correct when the queue is empty is a metric that is usually wrong.
- **Why no new quality rule was added for it:** the obvious candidate, "called + unfunded must equal commitment", is now true *by construction* — a tautology, and a control that cannot fail is not a control. The genuinely useful check is cross-document: statement-derived called versus notice-derived called, flagged when they disagree. That is the register's existing control gap on the alts chain ("validated document-to-document in silver but none of it rolls up into `dq_metrics`"), it fires by design whenever the queue is non-empty, and `pending_review_documents` already surfaces that on the same row. Left to that gap's own slice rather than bolted on here.
- **Verified before merge** by extracting the confirmed documents from the live lakehouse and running both the old and the new logic over them. The old path reproduced the four live MOICs to six decimal places, which is what makes the new figures trustworthy rather than merely plausible: **1.900249 → 1.028479, 4.653076 → 1.163269, 4.133076 → 1.033269, 4.333076 → 1.083269**, with `called + unfunded − commitment = 0.00` for every fund. Multiples of ~1.03–1.16× are what a fund a couple of years in with modest markups should look like.
- **No schema change, so no migration and no exporter change.** Column names and types are untouched; only the values are right now. That is the whole change surface.
- **Alternatives:** *keep summing notices and caveat the number on the tab* — rejected: a caveat on a wrong number is still a wrong number, and this one was wrong by 4×. *Compute MOIC only when every notice is confirmed* — rejected: it would blank the metric almost always, and blanking is not the same as fixing. *Sum the per-period `distributions` field across confirmed statements to get cumulative distributions* — rejected: statements report a period's distributions, so gaps from unconfirmed statements would silently understate, trading a visible error for an invisible one.

## D-073 · 2026-08-23 · Cross-field invariants, and a register that nearly graded itself

- **Context:** D-070 and D-072 were both found by a person noticing something odd on a screen, not by any control. They share a shape: **each figure was individually correct, and two figures disagreed about what had happened.** Every check in the estate validates a number against *its own* source — a position against the other feed's copy, a cash balance against its own movements, a return against a plausibility bound. Nothing asked whether two numbers rendered side by side told the same story. D-072's symptom had been visible in gold for weeks (`called + unfunded` never equalled `commitment`) because nobody adds three adjacent columns unless they already suspect something.
- **Choice:** `dq_cross_field_invariants` — one row per (date, invariant, scope) asserting that an aggregate equals the parts that reconstruct it, carrying both sides, the delta and the tolerance in force. Seven identities to start; adding another is one more `SELECT` in the `UNION ALL`, never a schema change, the same declarative shape `dq_metrics` uses. Rolled up as `cross_field_invariant_rate` (accuracy) and `cross_field_invariant_breaks_count` (exceptions).
- **Why this class of check is worth more than its cost:** writing one requires no understanding of the mechanism that would break it. You do not need to know that two document types confirm independently — only that a commitment splits into called and unfunded. **A check that catches a bug whose cause you have not thought of yet is worth more than one that re-tests a cause you already understand**, and that is exactly the property D-070's plausibility bound and this table share.
- **The rate counts invariants, not rows, deliberately.** `position_owner_proration_sums` contributes 10,210 of 11,033 rows. Row-weighting would let it swallow the total failure of any other invariant without visibly moving the number. "7 of 7 held" is a KPI; "11,032 of 11,033 rows held" is a shrug.
- **Not all seven are equally strong, and saying which is part of the record.** `account_ownership_totals_one` is genuine — `silver_account_owners` is materialised through the ownership bridge, so a bad join or a dropped edge breaks it. `allocation_value_matches_wealth` is genuine and the most valuable: `gold_asset_allocation` and `gold_client_wealth` aggregate the same silver sources at *different grains*, so agreement is two independent paths agreeing. `position_owner_proration_sums` catches a dropped owner row. `reconcile_variance_matches_exceptions` ties the dashboard badge to its own drill-down. But `wealth_components_sum` and `allocation_weights_sum_one` are **near-tautological** — `total_wealth_usd` is *computed* as the sum of its three parts, so the check can only ever catch independent rounding of the four columns. `alts_commitment_splits` became tautological the moment D-072 fixed it, and is kept as a regression guard against reintroducing independent sourcing. Cheap rounding guards are worth having; they are not worth *claiming*, which is the next point.
- **The mistake worth recording: the register nearly graded itself, and the person who wrote its rules made the error.** Citing the new rule against every column that carried the alts control gap took coverage from 59.4% to **97.0%** and gaps from 13 to 1 — a number that felt wrong immediately and was. The invariant does not touch `distributed_to_date_usd`, `current_nav_usd`, `moic` or `gold_client_wealth.alts_usd`; those citations were loosely-related metrics dressed as controls. D-067 had warned about precisely this failure mode in its own words — *"forcing every critical column to name a quality rule would have produced citations of loosely-related metrics — the register would read as fully covered and mean nothing"* — and it still happened. Walked back to the four columns the invariant genuinely tests. **Honest coverage: 28 of 33, 84.8%, with five stated gaps.**
- **The lesson generalises past this register:** a coverage metric is only as honest as the weakest citation in it, and the incentive to cite loosely is strongest exactly when a number is close to its target. The defence is not good intentions — it is asking, for every citation, *which specific row of the check would fail if this column were wrong?* Four of ten citations had no answer.
- **The ownership control gap genuinely closes.** It has read, since D-067: *"the graph proves itself acyclic and fully allocated when it is built, and a unit test holds a shared account to reassembling at 100%, but neither assertion reaches `dq_metrics` — so there is no daily measured signal. Closing it needs an ownership dimension in the DQ rollup."* `account_ownership_totals_one` is that signal, daily. The gap is removed rather than narrowed, because the closure it asked for is the closure it got.
- **Coverage crosses the 80% target for the first time (84.8% against 35.7% at D-067).** Worth stating plainly that the target did not move to meet the number: 80% was set when the estate delivered 35.7% and has been untouched since. `critical_control_coverage_rate` will therefore flip to `passed = true` on the Ops page — the first time that tile has been green, and earned.
- **Verified before merge** by extracting the table's own SQL and running it against the live lakehouse: **11,033 rows across 7 invariants, 0 breaks**, worst delta 0.000001 (a sum of rounded allocation weights, inside its stated epsilon). Every candidate invariant was probed against live data *before* being written, which is how one of them was corrected during design: `allocation_value_matches_wealth` was first drafted against `positions + alts` and failed on all 270 rows, because `gold_asset_allocation` carries Cash as an asset class and therefore reconstructs total wealth. The probe was wrong, not the data.
- **Alternatives:** *put these in `dq_recon` with the other checks* — impossible: `dq_recon` runs before gold, so a gold-layer invariant computed there would assert against the previous run's tables. Gold appends its own rows, guarded by the same `DELETE`-then-`INSERT` as D-070. *Emit only breaking rows* — rejected: the rate needs a denominator, and a table that is empty when healthy cannot distinguish "all good" from "never ran". *One boolean per invariant instead of per scope* — rejected: the scope column is what makes a break actionable, and 11,033 rows is nothing for Delta.

- **Verified live 2026-08-24**, predictions recorded first and all matched: **11,155 rows across 7 invariants, 0 breaks**, the only non-zero delta being 0.000001 on a sum of rounded allocation weights and inside its stated epsilon. `cross_field_invariant_rate` 1.000000 and passing on all 91 days; the `DELETE`-then-`INSERT` guard still holds now that it covers four metric names. **`critical_control_coverage_rate` reads 0.848485 with passed = true** — the first green that tile has shown, against a target unmoved since it was set at 35.7% coverage. Control gaps 18 → 5. No regression: the restatement adjustment, all five MOICs and 0 plausibility breaks all hold.

## D-074 · 2026-08-28 · A semantic layer as a governed object, not a BI convenience

- **Context:** gold publishes the same tables to every consumer, but not the same *definitions*. "Total wealth" is `SUM(total_wealth_usd)` at the right grain, and the dashboard, the exporter, an analyst's notebook, and a Genie answer each re-derive that independently. The CDE register (D-067) governs the *columns*; nothing governed the *measures* built from them. A semantic layer closes that, and it also makes the governance work legible outside the repo for the first time — a measure a person can question in plain language, rather than a coverage number in CI.
- **Choice:** a Unity Catalog **metric view**, `workspace.parvum.wealth_metrics` — a YAML spec of six measures and three dimensions over `gold_client_wealth`, defined in [`spark/metric_views/wealth_metrics.sql`](../spark/metric_views/wealth_metrics.sql), applied with `make metric-views`. An AI/BI **Genie space** ("Client Wealth Analytics") reads the metric view so plain-language questions resolve to the governed measure and answers cite it as their source. Write-up and screenshots: [`docs/SEMANTIC_LAYER.md`](SEMANTIC_LAYER.md).
- **Why a metric view rather than a plain view:** a metric view separates the *aggregation* (fixed in the spec) from the *grain* (chosen by the query through `MEASURE()`), so there is one definition of "total wealth" and a consumer cannot accidentally sum it wrong. A plain `CREATE VIEW` would bake in one `GROUP BY` and every other grain would go back to hand-written SQL. It is also a first-class catalog object — lineage-tracked, comment-carrying, permissioned — which a saved query or a dbt model on Free Edition is not.
- **Why it is defined in the repo, not clicked together:** a metric view is a catalog object, not a Delta table, so the bronze → gold job (`databricks.yml`) does not build it — left in the workspace it would be the one piece of the estate with no reviewable definition and no way to rebuild after a workspace reset. `apply.py` re-creates it through the SQL Statements API, the same pull path the freshness gate and exporter use. Not wired into CI: it needs live warehouse credentials, like the `land-*` and `deploy-job` targets.
- **The comments are the point, not decoration.** Each measure and dimension carries a `COMMENT` that is the *business* definition ("the headline number a client statement leads with"), not the catalog description ("owner-prorated sum in USD"). That text is what a Genie space — or any AI reading the catalog — uses to bind a term. A metric view with no comments is a slightly tidier `SUM`; with them it is a semantic contract.
- **Scope held honestly:** the metric view's measures are **not** in the CDE register. They are 1:1 derivations of already-classified gold columns, so they inherit classification, but the register's gate does not see them — bringing the semantic layer under the same gate is a real extension and is not claimed here. The Genie space's instruction text also lives in the workspace, not git; only the metric view it reads is versioned.
- **Verified live** against the Free Edition workspace: `wealth_metrics` created; a `MEASURE()` query on the "Total wealth" measure returns Hartwell $221,164,643.82 / Okafor $6,575,195.46 / Reyes $3,567,622.02 — matching `gold_client_wealth` on the latest date. Lineage shows the metric view and the Genie agent downstream of `gold_client_wealth`. Genie answered "total wealth by client for the latest date" (bar chart, cited the metric view) and "which clients' books don't reconcile" (per-client breakdown off the same view's DQ columns).
- **Alternatives:** *a dbt semantic model* — rejected: adds a build tool and a second definition site for a five-table serving layer, and Free Edition already has the native object. *A materialised gold table shaped for BI* — rejected: it would be another restatement-prone copy, and the metric view stores no data. *Leave "total wealth" defined in each consumer* — the status quo, and exactly the drift a semantic layer exists to remove: the exporter, the dashboard, and a Genie answer would keep agreeing only by luck.

## D-075 · 2026-08-29 · Evaluated dbt for the transform layer; not adopted

- **Context:** dbt is the default tool of the analytics-engineering world and its absence is a fair thing to question in a reference project. The question asked here was narrow: is there a place in *this* pipeline — bronze → silver → gold on Databricks, plus the gold → Postgres projection in `export/` — where dbt earns its keep?
- **Choice:** do not adopt dbt. Keep the transforms as PySpark notebook tasks orchestrated by the asset bundle (`databricks.yml`), the quality layer as `dq_recon.py`, the column metadata under the CDE register + gate (D-067), and the semantic layer as the Unity Catalog metric view (D-074). If dbt coverage is ever wanted as a demonstration, the contained options are a `dbt-postgres` project over the `export/` tenant projection, or a docs-only `dbt-databricks` overlay that models the gold layer as `ref()`-linked views alongside the notebooks — neither replacing the live pipeline. Record which, if built.
- **Why the fit is weak here:**
  - **The transforms are not SQL.** `silver_positions`, `silver_cash`, `gold_reports` and `dq_recon` import the repo's own tested packages — `parvum_reference.ownership` (owner proration), `parvum_reference.ecb` (FX fill-forward), IRR in Python, `parvum_alts_hitl` (LLM extraction), pydantic models — and round-trip small data through `createDataFrame`. dbt models are SQL; `dbt-databricks` Python models are constrained PySpark and cannot cleanly call these. Adoption would split the DAG into dbt for the SQL fragments and notebooks for everything else, with two orchestrators.
  - **The DAG is five tasks.** `ref()`-resolved build ordering earns its keep at dozens of models; hand-wiring `depends_on` for five is not a burden dbt would lift.
  - **`dq_recon.py` is already more expressive than `dbt test`.** Cross-field invariants (D-073), cash continuity, return-plausibility bands (D-070), and the `dq_metrics` rollup tied to defect-manifest ground truth are well past `not_null` / `unique` / `relationships` / `dbt-expectations`. Moving to dbt tests would lose expressiveness.
  - **dbt's doc model works against the project's differentiator.** The CDE gate exists precisely because column comments are normally *where nothing checks them*. dbt's `schema.yml` descriptions are that same unchecked kind unless the gate is extended to police them — more surface, same problem.
  - **One environment on purpose (D-007).** dbt targets assume dev/prod separation the project deliberately does not have.
- **What dbt would genuinely add, and why it is not enough:** a browsable model-level lineage site (`dbt docs`) — largely covered by Unity Catalog lineage + the register, which is the more opinionated artifact; snapshots (SCD2) for mutable reference data — but the securities master and FX are append-mostly external snapshots already and `book_restatements.json` is a hand-curated declaration, not a CDC feed; portfolio recognition — weighed against a target team that is Databricks-native (AI/BI + Genie + Metric Views + Unity Catalog), where a reasoned "why not dbt" reads stronger than a token model.
- **Alternatives:** *port the whole transform layer to `dbt-databricks`* — rejected: relocates working, tested code and creates the two-orchestrator split above. *`dbt-postgres` over `export/` now* — deferred, not rejected: it is the cleanest isolated demo (the projection is ~4 truncate-reload tables and touches nothing in the lakehouse), but ~4 tables barely justify the tool and the tenant-schema loop is already Python. *Adopt dbt purely for `schema.yml` docs* — rejected: it would duplicate the register while reintroducing unchecked metadata.

## D-075 · 2026-08-29 · Service levels that can be missed, not just quoted

- **Context:** the register has named seven service levels since D-067, each with an objective, a metric that evidences it, and a target sentence. None of them was ever *measured*. A target written only in prose can be quoted in a review and never missed, which makes it decoration with a compliance shape. The register's own comment admitted as much — "these are objectives the estate is held to, not claims about current attainment" — and left it there.
- **Choice:** each SLO gains a machine-readable half — `attainment_objective` (the share of measured days its metric must pass) and `window_days` — and `gold_reports.py` computes `dq_slo_attainment` from those against the `dq_metrics` series: one row per service level with attainment, the error budget, and how much of it is spent. Projected to Postgres (`V11`), served at `/internal/tenants/{id}/slo-attainment`, and rendered as a **Service levels** section on the Ops page, breaches first. `docs/RUNBOOK.md` turns each alert and each breach into a named first action, a decision, and an escalation point.
- **Why the SLO definitions ride in the existing register snapshot rather than a second landed file:** the flattened `cde_registry.json` already carries `slo`/`slo_measured_by`/`slo_target` per column, so adding three more fields gives the lakehouse everything it needs to derive the SLO set with no new feed, no new landing step in the daily Action, and no second file to drift out of step with the first. The cost is that the SLO set is derived from what critical elements *cite* — which is why the gate now has a rule about exactly that.
- **A sixth gate rule, `unheld_slo`, and it found two on its first run.** A declared SLO that no critical element cites is the mirror of `orphan`: a promise nobody is on the hook for, and — given the derivation above — one that would silently never be measured. `feed_completeness` and `cash_continuity` had been declared since D-067 and held by nothing. Both were real promises, so both were assigned rather than deleted: `gold_client_wealth.total_wealth_usd` moved from `gold_freshness` to `feed_completeness` (a stale headline is wrong by a day; one computed from a day whose files did not all land is wrong by an unknown amount and looks fine), and `gold_performance.external_flow_usd` from `cash_ledger_integrity` to `cash_continuity` (a continuity break is precisely the case where a recorded movement never reaches wealth, which is what that SLO's objective says in so many words). The test applied was the D-073 one, pointed at SLOs instead of rules: *if this service level is breached, is this element's value affected?*
- **An objective of 1.0 has no error budget, and that is stated rather than smoothed.** `cross_field_consistency` and `return_plausibility` are correctness invariants, not availability targets: "how many contradictions per month are acceptable" has no non-zero answer. Their `error_budget_days` is 0 and `budget_remaining_pct` is NULL, and the screen says "None — objective is total". Reporting 0% or 100% there would both be false, and setting the objective to 0.999 to avoid the degenerate case would have been a fudge dressed as rigour.
- **Too little history is not the same as passing.** Below seven measured days, `meets_objective` is NULL and the badge reads "Not enough history" in grey, not amber — it is an absence of evidence, not a warning about the estate, and colouring it as a problem trains people to ignore the colour that means one. This is not hypothetical: `bronze_days_behind` is published as a single as-of-now row rather than a daily series, so `gold_freshness` lands there today with one day of history. That is a real limitation of that metric's shape, surfaced instead of averaged into a fake 100%.
- **The window ends at the metric's own latest day, not today.** A pipeline that stopped running last week would otherwise shrink its denominator toward zero and read as "no breaches" — the failure mode where the alarm goes quiet because the sensor died. Staleness is `gold_freshness`'s job; this table refuses to disguise it.
- **Two SLOs ship breached and are meant to stay breached.** `holdings_agreement` reads 0.0% and `cash_ledger_integrity` 45.5%, because the feed generator injects defects at a rate no real custodian would (D-011). The objective is what a real deployment would hold the feed to; the breach is a property of the corpus. They are left visibly red rather than exempted, because an exemption list is where uncomfortable numbers go to be forgotten — and the runbook says plainly that those two reading *green* is the thing that would actually be wrong.
- **Not rolled into `dq_metrics`.** There is no honest home for it among freshness/completeness/accuracy/exceptions/governance, and inventing a sixth dimension to hold a differently-shaped fact would have cost a migration and bought a category error: `dq_metrics` is one row per (date, dimension, metric), while attainment is one row per service level over a window.
- **Verified before merge** by extracting the table's own SQL and running it against the live warehouse with the seven objectives substituted as literals, predictions recorded first. All matched: 22 business days inside the 30-day window; `feed_completeness`, `cash_continuity`, `cross_field_consistency` and `return_plausibility` at 1.000000 and met; `holdings_agreement` 0.000000 with 22 days consumed against a 1.10-day budget; `cash_ledger_integrity` 0.454545; `gold_freshness` at one day, `insufficient_history` true and no verdict. Both designed edge cases behaved: a zero budget produced NULL rather than a number, and insufficient history produced NULL rather than a pass.
- **Alternatives:** *parse the target sentence* ("99% of account-days or better") — rejected: a regex over prose is a control that breaks when someone improves the wording. *Attainment over all history rather than a window* — rejected: an SLO that averages in April tells you nothing about this month, and error budgets are meaningless without a window to spend them in. *Exempt the two deliberately-breached SLOs* — rejected for the reason above; the runbook explains them instead, which is where an operator will actually look.

## D-076 · 2026-08-29 · Naming the personas, and publishing the roadmap on a cadence

- **Context:** the platform already had four surfaces — the client dashboard, the repo and its gate, the internal Ops page, and the semantic layer — serving four genuinely different people. None of that was written down, which meant the surfaces existed by accretion rather than by design, and "add it to the dashboard" was the default answer to every new request. Separately, the roadmap lived entirely in private notes: a public repo with no published plan asks a reader to infer intent from a commit log.
- **Choice:** [`docs/PERSONAS.md`](PERSONAS.md) names four personas — consumer, producer, operator, analyst — and gives each a primary surface, an enablement path, and an explicit **self-serve ceiling**. [`docs/ROADMAP.md`](ROADMAP.md) publishes the plan with a stated quarterly review date, a changelog, a parked list carrying the trigger that would revive each item, and an *explicitly not doing* section.
- **Why the ceiling is the load-bearing column:** what a persona *cannot* self-serve is the design decision. A consumer cannot ask a new question, and that is deliberate — an unanticipated question from a client is a request for a new tab, not a reason to hand them a query tool. Writing the ceiling down is what makes the enablement path a boundary rather than an aspiration.
- **Four personas, not five.** An executive is a real and distinct persona in a larger organisation — the summary-and-exception reader. Here they are a consumer of the Ops tiles and the dashboard headline, and inventing a surface for an audience of none would be architecture theatre. Named as a deliberate omission rather than left as an oversight, because the next reader will otherwise wonder.
- **"Explicitly not doing" is separate from "parked", and the distinction is the point.** Parked items carry a revival trigger; the not-doing list carries commitments — real client data, production-scale benchmarks, a second orchestrator. A roadmap that only ever grows is a wish list, and the fastest way to make one honest is to write down what it has refused.
- **The review date is a date, not a cadence adjective.** "Reviewed quarterly" with no next date is how a roadmap rots; the file names 2026-10-01 and carries a changelog so a reader can see whether the promise was kept.
- **Alternatives:** *fold personas into ARCHITECTURE.md* — rejected: architecture answers "how is it built", personas answer "who is it for", and merging them buries the second. *Leave the roadmap private* — rejected: it is the one governance artefact whose whole value is that other people can read it, and nothing in it is sensitive. *Generate the roadmap from the private backlog* — rejected: the private notes carry context that has no business in a public repo, and a filtered export would drift the moment either side was edited.

## D-077 · 2026-08-29 · Column contracts the gate verifies, rather than comments that rot

- **Context:** the register said what every column *means* and who owns it, but nothing about how the tables fit together — what one row represents, which columns join to which, whether a join can fan out, or what a table is actually for. That is precisely the metadata an analyst needs before using a dataset unaided, and precisely what a model needs before it can generate a correct join. The conventional home for it is the catalog comment, which is also the problem: a comment saying `account_id joins to silver_account_owners` reads as authoritative, is checked by nothing, and rots the first time a column is renamed — with no signal to the reader that it has.
- **Choice:** tables in the register now declare three contracts — `grain` (the columns that make one row unique), `foreign_keys` (column, `table.column` reference, and a cardinality from a closed set), and `context` (what the table is for, in prose) — and a seventh gate rule, `broken_contract`, resolves all of it against the scanned inventory on every pull request. A declared grain column the table does not publish, a foreign key pointing at a column no job publishes, an unknown cardinality, or a table carrying a critical element with no context: each fails the build.
- **The whole point is the verb.** Describing a join is cheap and everybody does it. *Verifying* it costs one rule and turns the description into a control: both ends of every declared join must resolve against the columns the Spark jobs actually publish, in a pull request, before anything is deployed. This is the same move the register made for ownership — the difference between a document and a control is whether something fails when it stops being true.
- **Cardinality is a closed set (`many_to_one`, `one_to_one`), not free text.** The reason to declare cardinality at all is so a consumer knows whether a join can fan out and silently multiply a sum. Free text would answer that question differently every time it was written, which is no answer.
- **Context is required only for tables carrying a critical element.** A table nobody consumes directly can be read from its column comments; one whose figures a person or a model is making decisions on cannot, because a column list does not say what the table is *for*. Sixteen tables owe a paragraph; the rest owe nothing. Requiring it everywhere would have produced sixteen useful sentences and sixty filler ones, which is how a metadata field becomes noise.
- **Grain is declared rather than inferred** because it is the most load-bearing property a table has (D-023) and it is not visible in a schema. `silver_position_owners` and `silver_positions` have nearly the same columns and completely different grains, and mixing them is exactly how a shared account gets double-counted.
- **Verified by negative control**, not only by the suite: renaming one end of a real declared join (`gold_ownership.account_id` → `.acct_id`) in a copy of the register produced exactly one finding, naming the broken reference. Five unit tests pin the same behaviours — a resolving contract passes, and a broken FK, a phantom grain column, an unknown cardinality and a missing context each fail.
- **Not projected to the lakehouse, deliberately.** The contracts are verified in CI and stop there for now; pushing them into `COLUMN_COMMENTS` so an AI reading Unity Catalog inherits them is the obvious next step and is *not* claimed here. Saying that plainly is better than implying the estate's catalog already carries verified join metadata when only the repo does.
- **Alternatives:** *put the contracts in the Spark jobs' `COLUMN_COMMENTS`* — rejected: that dict is the inventory the gate checks *against*, so contracts living there would be self-certifying, and the gate would be marking its own homework. *Infer foreign keys from column-name matches* — rejected: `client_id` appears in eighteen tables and inferring a join between every pair of them would generate confident nonsense. *Full referential-integrity checks against live data* — a different and more expensive control; this one is a schema contract, checkable before deployment, which is the point at which a gate can still prevent something.

## D-078 · 2026-08-29 · Closing two control gaps, and declining to close two more

- **Context:** five critical elements carried stated control gaps, collapsing into two root causes the register had named since D-067: the alts chain validated every document against the rest of its fund and none of that reached `dq_metrics`, and nothing re-checked a landed ECB FX rate once it was in the lakehouse. Both had been on the Ops page work list for weeks, which is what a gap statement is for.
- **Choice:** `dq_recon.py` rolls the alts chain's existing `cross_document_valid` verdict into `dq_metrics` as `alts_cross_document_valid_rate` (accuracy) and `alts_documents_unconfirmed_count` (exceptions). `gold_reports.py` gains `dq_fx_plausibility` — a day-over-day band on the rate plus the distance the published rate was carried forward — rolled up as `fx_rate_plausibility_rate` and `fx_rate_stale_days_count`, and a new `fx_integrity` service level that `gold_client_wealth.fx_rate_used` is now held to instead of the generic freshness one.
- **The FX check is honest about its own reach, in the notebook and in the register.** It catches a corrupted rate (a moved decimal point, a transposition) through the band, and a silently stale one through the carry distance. It does **not** catch a rate wrong by a small amount — only fetching the published rate a second time would, and the cluster has no egress by design (D-006), so that half belongs in the GitHub Action that lands the rates, not in Spark. Claiming a re-check against source here would have been the easiest overstatement in the slice.
- **Both thresholds were calibrated against the real distribution before being written, not guessed.** The largest single-day move in this project's own EUR/USD series is 2.72% (2025-04-03) against a 5% band; the ECB's publication calendar over 2024-01 to 2026-07 produces carries of 0, 1, 2, 3 and 4 days only — four occurring four times, across Christmas and Easter runs — and never five, against a 4-day staleness threshold. So neither can fire on a real market or a real holiday. A check that cries wolf is worse than no check, and the way to avoid one is to look at the distribution first.
- **Two gaps were deliberately *not* closed, and this is the part worth reading.** The obvious move was to cite the new alts rule against all four columns that carried the alts gap, taking coverage to 100%. Applying D-073's test — *which specific row of this check fails if this column is wrong?* — only two survive:
  - `current_nav_usd` and `distributed_to_date_usd` are read straight off a confirmed capital account statement, and the cross-document check is exactly what validates that statement against its fund's chain. Genuine; cited.
  - `moic` is a *ratio*, and the check does not evaluate ratios. **D-072 is the proof this distinction is real rather than pedantic:** every input was individually confirmed and the multiple was still four times wrong, because numerator and denominator described different moments. A check that would not have caught the bug that actually happened is not a control for that column. Gap kept, rewritten to say precisely what is missing.
  - `gold_client_wealth.alts_usd` carries different risks entirely — owner proration and forward-fill — neither of which the cross-document check touches. Gap kept, rewritten.
- **The resulting number is 34 of 36, 94.4%, with two stated gaps**, rather than the 36 of 36 that two more citations would have produced. Coverage went up because two elements gained real controls and one new critical element arrived with one; it did not go to 100% because two elements still have nothing testing them, and saying so is the only thing that makes the other 94.4% worth reading.
- **The alts rate ships red, and correctly.** 39 of 64 documents currently reconcile against the rest of their fund; the other 25 are the deliberately-defective corpus doing its job, and 27 documents have no confirmed values because gold is properly declining to report them (D-060). A green number here would mean the defect injector had stopped.
- **Verified against live data before merge:** the alts figures above come from querying `silver_alts_documents` directly; the FX figures from replaying `fill_forward` over the landed rate series in Python. Production carry distance was checked separately against `gold_client_wealth` (max 1 day, 0 stale), which also caught a false alarm worth recording — the local rate file was a month out of date and produced 33 apparent stale days that do not exist in the lakehouse. The check was right; the input was stale. Reporting that as a finding would have been a fabricated one.
- **Alternatives:** *cite the alts rule everywhere and report 100%* — rejected, above. *Fetch the ECB rate again inside Spark to compare* — impossible by design (D-006), and worth stating rather than quietly omitting. *Make the FX band tight enough to catch small errors* — rejected: at any band tight enough to catch a 0.5% error, ordinary market days fire constantly, and the check becomes noise within a week.

## D-079 · 2026-08-29 · A semantic layer that refuses unsafe measures, and a gate that governs it

- **Context:** D-074 built one metric view over one gold table and left two things open in writing — the layer covered only client wealth, and its measures were not governed the way columns are. Both are addressed here, and the second turned out to be the more interesting.
- **Choice:** two more metric views (`allocation_metrics`, `performance_metrics`), and an eighth gate rule, `undefined_measure`, that fails the build when any metric view publishes a measure or dimension with no business definition. `governance/metric_views.py` parses the YAML spec and the `COMMENT ON COLUMN` statements out of each `spark/metric_views/*.sql`.
- **The refusal is the design decision.** `gold_performance` carries `daily_twr_return` and `twr_index_since_inception`, and neither is exposed as a measure. A time-weighted return over a period is the **chain-linked product** of its daily factors, not their sum and not their average, and a metric view measure is an aggregate expression. `AVG(daily_twr_return)` would produce a number at any grain a consumer picked and that number would be wrong in a way nothing on the screen would reveal. The view exposes the additive components instead — wealth, flows, restatement adjustment — and the chained figures stay in `gold_performance_summary`, computed once by the job that knows how. `allocation_metrics` makes the same refusal about `weight`.
- **A measure that is correct at one grain and silently wrong at others is worse than no measure**, because it looks like an answer. Declining to expose one is the semantic layer doing its job, not a gap in its coverage — and it is exactly the kind of judgement that cannot be delegated to whoever writes the next dashboard.
- **Why the gate had to reach the semantic layer.** A measure named `Total wealth` looks self-explanatory and is nothing of the sort: it is owner-prorated, it includes forward-filled private-fund NAV, and it is in USD at a specific day's rate. An AI reading the catalog binds the term to whatever text sits beside it, so an uncommented measure is one a model will guess about — and guess plausibly. Requiring the definition is what makes this a semantic *contract* rather than a naming convention, and it is the same argument D-067 made for columns, one layer up.
- **The scanner fails loudly rather than silently**, like every other scan in the package: a file with no `$$ ... $$` body raises, and an empty directory raises rather than passing vacuously. A gate that quietly stops governing a surface is worse than one that was never written, because the coverage number keeps reading green.
- **Verified live**: all three views applied to the workspace and queried through `MEASURE()`. `performance_metrics` returns Hartwell's restatement adjustment as exactly $178,175,109.88, matching the figure D-070 established; `allocation_metrics` totals $231,306,079 across the four asset classes on the latest date, which is the three clients' wealth to the dollar. Summing `Wealth` across 95 days returns $5.9bn for Hartwell — meaningless, exactly as that measure's own comment warns, which is the case for writing the warning into the comment rather than a wiki.
- **Alternatives:** *expose the returns as measures with a warning comment* — rejected: the comment is read after the number is believed. *Compute chain-linking inside the metric view* — not expressible: a metric view measure is a single aggregate over the source rows, which is precisely why the chained figures live in a table computed by a job. *Require every measure to be classified in the register's tier model* — deferred honestly rather than half-done: definitions are enforced now, tiering the semantic layer is a real design question about what "critical" means for a derived measure.

## D-080 · 2026-08-29 · Measuring what the metadata is worth, and publishing the unflattering number

- **Context:** every claim in this project's governance layer rests on an assumption — that better metadata produces better analytics — and the assumption was carried entirely by other people's numbers. The figures quoted for this at conferences (21% to 95% accuracy, and similar) are real measurements of somebody else's estate, which makes them evidence about that estate and rhetoric about ours.
- **Choice:** `parvum-governance-eval` — eight questions with hand-written ground-truth SQL, each put to the same model twice at temperature 0: once with column *names* only, once with the catalog descriptions, register definitions and semantic-layer measures. Both answers execute against the real warehouse and are compared at the cent. Write-up: [`docs/GOVERNANCE_EVAL.md`](GOVERNANCE_EVAL.md).
- **Result: bare 7/8 (88%), governed 8/8 (100%).** One question of eight. That is a far smaller gap than the numbers usually cited, and publishing it that way is the entire point of having measured rather than asserted.
- **The single failure is the whole argument in miniature.** Asked for total dividend income, the bare arm wrote `WHERE type = 'dividend'` and matched nothing; the governed arm wrote `'DIVIDEND'`, because the column comment says "DIVIDEND or INTEREST". A schema says a column exists; it does not say what values it takes. Controlled vocabulary is exactly the kind of knowledge a name cannot carry and a comment can.
- **The failure mode matters as much as the failure.** It returned *no answer*, not a confident wrong one. The dangerous case — a plausible number that is quietly wrong — did not occur in this run, and the write-up says so rather than implying otherwise.
- **The experiment understates the value, and the reason is worth stating.** The bare arm is not as bare as a real ungoverned warehouse: these column names are clear, and clear names did most of the work. But they are clear *because* the gate has required a description on every published column since D-067, and it is hard to write a description for a badly-named column without renaming it. So the honest conclusion is not "metadata is worth twelve points" but "on an estate where naming is already disciplined, explicit metadata closes the residual gap, and the residual is concentrated exactly where a name cannot reach". Both that and the conference numbers can be true, of estates at different starting points.
- **Built so it could fail.** Questions each record the trap they test, so a reader can judge fairness rather than take it on trust; a wrong-but-runnable answer scores wrong rather than erroring; ground truth is checkable SQL in the question set; and the limits section names the sample size, the single model, and the fact that the questions were written by the person who built the metadata.
- **Two providers behind one interface**, mirroring D-052: the run was made against Anthropic because OpenRouter returned HTTP 402 mid-session. Same prompt, same temperature, so the arms stay comparable regardless of which has credit.
- **Alternatives:** *use an LLM to judge the answers* — rejected: an LLM-as-judge introduces exactly the ambiguity the ground-truth SQL removes, and the answers here are numbers, which compare exactly. *Ask questions a schema already answers* — rejected: they inflate both arms equally and measure nothing. *Report only the governed score* — rejected as the obvious way to make this meaningless.

## D-081 · 2026-08-29 · Agent skills as repo artefacts, not personal tooling

- **Context:** two procedures had been carried out repeatedly in this project and existed only as habit — working out what an unfamiliar table means and is safe for, and investigating a figure that looks wrong. Both were done well enough to be worth repeating and were written down nowhere, which makes them one person's practice rather than a method.
- **Choice:** `skills/` — agent skills versioned in the repo. `dataset-discovery-brief` produces a stakeholder-facing account of a table; `wrong-number-triage` is the investigation method derived from D-070, D-072 and D-073.
- **Why in the repo rather than in personal configuration:** exactly the argument the register makes about ownership. A procedure living in one person's tooling is that person's habit; a procedure in the repo is reviewable in a pull request, correctable when it turns out to be wrong, and survives the person. It is also the only version an AI working in this checkout will actually find.
- **Both skills are instructed to start from the estate's own metadata** — the register, the jobs' `COLUMN_COMMENTS`, the metric views. That is the point rather than a convenience: the governance work exists so that a person *or a model* arriving cold can find out what a figure means without asking anyone, and a skill that ignored all of it and guessed would be evidence the metadata was not worth writing.
- **The discovery brief's load-bearing section is "what you cannot learn from this alone".** A dataset handed over without its limits gets used past them, and the resulting wrong answer is confident and traceable to nobody. It is instructed to quote any `control_gap` the register records on the table's columns verbatim — the estate's own admission that nothing checks a figure is precisely what a consumer deserves before relying on it.
- **The triage skill exists because the same defect shape occurred three times**, so it is a pattern rather than an anecdote: every figure individually correct, two figures disagreeing about what happened. It carries the question that found D-072 (*does this number move when the thing it claims to measure has not moved?*), the "looks wrong, isn't" table so an investigation does not start by re-deriving that deliberate defect rates are deliberate, and D-073's citation test.
- **It also carries a correction rather than only successes.** D-070's entry records that the register had flagged the right columns and proposed the *wrong* remedy — independent recomputation would have reproduced the wrong figure faithfully, because the formula was never wrong. A skill that only encoded what went right would teach the wrong lesson about how much to trust a register.
- **Calibrated depth, stated in the skill.** The discovery brief names four situations and how much of the seven-phase method each warrants, because the failure mode of a thorough procedure is a thorough document nobody reads.
- **Alternatives:** *keep them as prompts in the private notes* — rejected: they are technical method with nothing sensitive in them, and the public repo is where a reader would look for how this project works. *Write one general "data analysis" skill* — rejected: a skill that covers everything gets loaded for everything and helps with nothing; these two match distinct triggers. *Generate the briefs and commit them* — rejected: a brief is generated against live data at the moment it is needed, and a committed one is stale the next time the pipeline runs.

## D-082 · 2026-08-29 · A tile that lied, and the gate that stops the third occurrence

- **Context:** a screenshot of the live Ops page showed `ALTS CROSS DOCUMENT VALID RATE — 0%`. The actual rate is 60.9% (39 of 64 documents). The tile was not wrong about its own arithmetic — the accuracy tiles render *SLA attainment*, the share of days a metric passed — but the metric had exactly one day of history, so attainment was `0/1`, rendered as a percentage, directly beneath a label naming a rate. Two correct numbers, one of them answering a different question, which is the defect shape this project has now met four times.
- **Root cause, and it had two symptoms:** `fx_rate_plausibility_rate` and `alts_cross_document_valid_rate` were both published as a single `CURRENT_DATE` row rather than a daily series. That produced the misleading tile *and* left `fx_integrity` permanently reading "not enough history" on the Service levels table one section below — the same limitation `bronze_days_behind` has, except this one was self-inflicted on data that is per-date already.
- **Choice, in four parts:**
  1. **`fx_rate_plausibility_rate` becomes a daily series**, restricted to the dates the estate actually reports on. `dq_fx_plausibility` is per-date already, so this is a join rather than new computation — and it fixes the tile and the SLO together.
  2. **The tile stops reporting attainment below seven days** and shows the metric's own latest value and detail instead, mirroring `MIN_DAYS_FOR_VERDICT` in the gold job. Alts documents are episodic and have no daily grain, so that arm is permanent rather than transitional.
  3. **Curated labels** for the four unnamed metrics, and a `SLO_LABELS` map beside the existing metric one.
  4. **A ninth gate rule, `unlabelled_metric`**, failing the build when the DQ layer publishes a metric, or the register declares a service level, that the Ops page has no label for.
- **Why the fallback was not enough, which is the lesson.** The 2026-08-25 fix humanised the label fallback so an unnamed metric read as a phrase rather than an identifier. That made the symptom cosmetic and left the cause alone — and four days later the same author shipped four more unnamed metrics. Worse, a humanising fallback *cannot* be right: it has no way to know `fx` is an acronym or that `gold` names a medallion layer, so `fx_integrity` reached a live screen as "Fx integrity" and `gold_freshness` as "Gold freshness", which reads to an operator as a property of gold rather than of the data. **A fallback makes forgetting survivable; only a gate makes it not happen.**
- **The rule reads the frontend, which is new and deliberate.** `governance` already parses `spark/*.py` for its inventory; it now parses `internal/src/format.ts` for the label maps. It still imports nothing from the workspace — it reads the estate and judges it, which is the relationship a control should have to the thing it controls, and the label map is as much a published surface as a column comment.
- **Verified by negative control:** removing one metric label and one SLO label from a copy of `format.ts` produced exactly two findings naming both. The scanner also refuses to run silently — a renamed map or a shape it cannot parse raises rather than passing vacuously, because a gate that quietly stops checking is worse than one never added.
- **`gold_freshness` is labelled "Data freshness".** The identifier is right for the register and wrong for a reader; a medallion layer is the pipeline's vocabulary, not the operator's.
- **Alternatives:** *make the alts metric a daily series too* — rejected: alts documents are episodic, arriving in batches with period ends of their own, and a fabricated daily grain would be a worse lie than the tile was. *Show the metric value on every tile instead of attainment* — rejected: attainment over 95 days is the more useful number where a series exists, and the Ops page is a service-level view. *Leave the labels to the fallback* — rejected, above; that is what produced this.

## D-083 · 2026-08-29 · One page, two questions — and a tile row that had stopped answering either

- **Context:** the user's screenshot of the live Ops page came with a plain observation: *"this ops page is quite cluttered now tbh"*, at 60% browser zoom. Looking properly, the clutter was a symptom. Nine tiles sat above a Service levels table that **restated eight of them with a different number** — the tile counted attainment over all history, the table counted it over the window the SLO declares. `CASH CONSISTENCY 35%` sat two inches above `Cash ledger integrity 45.5%`, about the same metric. Two correct figures answering different questions, adjacent, with nothing saying which was which: the shape this estate has now met five times.
- **Root cause is a sequencing one, not a design one.** The tile row predates the Service levels table by one day. When D-075 added the table it superseded what the tiles were for, and nobody deleted the tiles — so the page accumulated rather than being redesigned. The correct response to "cluttered" was therefore to *remove* something, not to rearrange it or add a navigation pane to hide it.
- **Choice — a rule the row did not previously have:** **a tile says what is true now; the Service levels table says whether we are meeting what we promised.** A metric a service level already reports on gets no tile; one no service level covers keeps its tile. Nine tiles become three: freshness (today's staleness), completeness (today's files), and alts document validity — the one accuracy metric with no service level, because its data is episodic and cannot carry an attainment window.
- **Why freshness and completeness stay even though both have SLOs.** They are not duplicates: the tiles state *today* ("1d behind", "11 of 11 files parsed"), the SLO rows state *the window* ("attained on 22 of 22 days"). Different questions with different answers, which is exactly the distinction the accuracy tiles had lost.
- **It degrades toward showing more, not less.** If the service-levels request fails, `slos` is empty and every accuracy metric gets its tile back. A page that silently hides quality information because a second request failed would be a worse failure than a brief repetition.
- **The chart legend still names every metric, deliberately.** The Accuracy trend chart is the history view and plotting a covered metric there is its whole purpose. The test says so explicitly, because "it still appears once, in the legend" is otherwise indistinguishable from a half-finished change.
- **A left-hand nav pane was considered and rejected** (the user raised it): splitting Ops into sub-pages would hide the contradiction rather than resolve it, cost a click for every question, and break the property that makes this page useful in a demo — that one scroll shows the whole quality and governance story.
- **Alternatives:** *keep both and add explanatory captions* — rejected: a caption explaining why two numbers differ is an admission that one of them should not be there. *Show the metric's raw value on the tiles instead of attainment* — rejected: it would remove the contradiction but leave nine tiles restating a table, and the value is already one click away in the trend chart. *Make the tiles the 30-day window too* — rejected: then they would agree with the table and be pure duplication, which is the same problem with better manners.
