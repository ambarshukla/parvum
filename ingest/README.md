# ingest/

Feed generator + wire-format parsers (Python). Phase 1 — not yet built.
Emits the same synthetic portfolios as semt.002, MT535 and camt.053 with
configurable injected defects, and parses them back to a common model.
